[metadata]
name = "Optional Type Tests"
category = "optional-types"

# =============================================================================
# Basic Optional
# =============================================================================

[[test]]
name = "optional_param"
description = "Optional parameter type"

python = '''
from typing import Optional

def greet(name: Optional[str]) -> str:
    if name is None:
        return "Hello, stranger"
    return f"Hello, {name}"
'''

rust = '''
pub fn greet(name: &Option<String>) -> String {
    if name.is_none() {
        return "Hello, stranger".to_string();
    }
    return format!("Hello, {}", {
        match &name {
            Some(v) => format!("{}", v),
            None => "None".to_string(),
        }
    });
}
'''

[[test]]
name = "optional_return"
description = "Optional return type"

python = '''
from typing import Optional

def find_item(items: list[int], target: int) -> Optional[int]:
    for item in items:
        if item == target:
            return item
    return None
'''

rust = '''
pub fn find_item(items: &Vec<i32>, target: i32) -> Option<i32> {
    for item in items.iter().cloned() {
        if item == target {
            return Some(item);
        }
    }
    return None;
}
'''

[[test]]
name = "optional_is_none"
description = "Check if value is None"

python = '''
from typing import Optional

def is_empty(value: Optional[str]) -> bool:
    return value is None
'''

rust = '''
pub fn is_empty(value: &Option<String>) -> bool {
    return value.is_none();
}
'''

[[test]]
name = "optional_is_not_none"
description = "Check if value is not None"

python = '''
from typing import Optional

def has_value(value: Optional[str]) -> bool:
    return value is not None
'''

rust = '''
pub fn has_value(value: &Option<String>) -> bool {
    return value.is_some();
}
'''

[[test]]
name = "optional_unwrap_or"
skip = true
description = "Unwrap with default value"

python = '''
from typing import Optional

def get_or_default(value: Optional[int]) -> int:
    return value if value is not None else 0
'''

rust = '''
pub fn get_or_default(value: &Option<i32>) -> i32 {
    return if value.is_some() { value } else { 0 };
}
'''

[[test]]
name = "optional_unwrap_or_default"
skip = true
description = "Unwrap with type default"

python = '''
from typing import Optional

def safe_string(value: Optional[str]) -> str:
    return value if value is not None else ""
'''

rust = '''
pub fn safe_string(value: &Option<String>) -> String {
    return if value.is_some() { value } else { "".to_string() };
}
'''

[[test]]
name = "optional_if_let"
skip = true
description = "if let pattern for Optional"

python = '''
from typing import Optional

def process(value: Optional[int]) -> int:
    if value is not None:
        return value * 2
    return 0
'''

rust = '''
pub fn process(value: &Option<i32>) -> i32 {
    if value.is_some() {
        return value.clone().unwrap() * 2;
    }
    return 0;
}
'''

[[test]]
name = "optional_arithmetic"
skip = true
description = "Arithmetic on optional value"

python = '''
from typing import Optional

def double(value: Optional[int]) -> Optional[int]:
    if value is None:
        return None
    return value * 2
'''

rust = '''
pub fn double(value: &Option<i32>) -> Option<i32> {
    if value.is_none() {
        return None;
    }
    return Some(value.clone().unwrap() * 2);
}
'''

[[test]]
name = "optional_method_call"
description = "Method call on optional value"

python = '''
from typing import Optional

def upper(value: Optional[str]) -> Optional[str]:
    if value is None:
        return None
    return value.upper()
'''

rust = '''
pub fn upper(value: &Option<String>) -> Option<String> {
    if value.is_none() {
        return None;
    }
    return Some(value.as_ref().unwrap().to_uppercase());
}
'''

[[test]]
name = "optional_in_list"
skip = true
description = "Optional values in list"

python = '''
from typing import Optional, List

def filter_none(items: List[Optional[int]]) -> List[int]:
    return [x for x in items if x is not None]
'''

rust = '''
pub fn filter_none(items: &Vec<Option<i32>>) -> Vec<i32> {
    return items
        .clone()
        .into_iter()
        .filter(|x| x.is_some())
        .map(|x| x)
        .collect::<Vec<_>>();
}
'''

[[test]]
name = "optional_default_param"
description = "Optional parameter with default"

python = '''
from typing import Optional

def greet(name: Optional[str] = None) -> str:
    if name is None:
        return "Hello!"
    return f"Hello, {name}!"
'''

rust = '''
pub fn greet(name: &Option<String>) -> String {
    if name.is_none() {
        return "Hello!".to_string();
    }
    return format!("Hello, {}!", {
        match &name {
            Some(v) => format!("{}", v),
            None => "None".to_string(),
        }
    });
}
'''

[[test]]
name = "optional_and_then"
description = "Chaining optional operations"

python = '''
from typing import Optional

def parse_and_double(s: Optional[str]) -> Optional[int]:
    if s is None:
        return None
    return int(s) * 2
'''

rust = '''
pub fn parse_and_double(s: &Option<String>) -> Option<i32> {
    if s.is_none() {
        return None;
    }
    return Some(s as i32 * 2);
}
'''

[[test]]
name = "optional_string_or_default"
skip = true
description = "Optional string with 'or' default pattern"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Person:
    name: Optional[str]

def get_name(p: Person) -> str:
    return p.name or "Unknown"
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Person {
    pub name: Option<String>,
}
impl Person {
    pub fn new(name: Option<String>) -> Self {
        Self { name }
    }
}
pub fn get_name(p: &Person) -> String {
    return p
        .name
        .clone()
        .clone()
        .unwrap_or_else(|| "Unknown".to_string());
}
'''

[[test]]
name = "optional_variable_or_default"
description = "Optional variable with 'or' default"

python = '''
from typing import Optional

def get_value(x: Optional[str]) -> str:
    return x or "default"
'''

rust = '''
pub fn get_value(x: &Option<String>) -> String {
    return x.clone().unwrap_or_else(|| "default".to_string());
}
'''

[[test]]
name = "optional_int_or_default"
skip = true
description = "Optional int with 'or' default zero"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Counter:
    value: Optional[int]

def get_value(c: Counter) -> int:
    return c.value or 0
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Counter {
    pub value: Option<i32>,
}
impl Counter {
    pub fn new(value: Option<i32>) -> Self {
        Self { value }
    }
}
pub fn get_value(c: &Counter) -> i32 {
    return c.value.clone().unwrap_or_else(|| 0);
}
'''

[[test]]
name = "chained_optional_or"
skip = true
description = "Chained optional 'or' expressions"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Person:
    nickname: Optional[str]
    name: Optional[str]

def get_display(p: Person) -> str:
    return p.nickname or p.name or "Anonymous"
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Person {
    pub nickname: Option<String>,
    pub name: Option<String>,
}
impl Person {
    pub fn new(nickname: Option<String>, name: Option<String>) -> Self {
        Self { nickname, name }
    }
}
pub fn get_display(p: &Person) -> String {
    return p
        .nickname
        .clone()
        .clone()
        .or_else(|| p.name.clone().clone())
        .unwrap_or_else(|| "Anonymous".to_string());
}
'''

[[test]]
name = "non_optional_or_unchanged"
description = "Non-optional 'or' remains boolean operator"

python = '''
def check_flags(a: bool, b: bool) -> bool:
    return a or b
'''

rust = '''
pub fn check_flags(a: bool, b: bool) -> bool {
    return (a) || (b);
}
'''

[[test]]
name = "optional_or_in_assignment"
skip = true
description = "Optional 'or' in variable assignment"

python = '''
from typing import Optional

def process(name: Optional[str]) -> str:
    result = name or "fallback"
    return result
'''

rust = '''
pub fn process(name: &Option<String>) -> String {
    let _cse_temp_0 = name.clone().unwrap_or_else(|| "fallback".to_string());
    let result = _cse_temp_0;
    return result;
}
'''

[[test]]
name = "optional_field_or_method"
skip = true
description = "Optional field with 'or' default string"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Config:
    prefix: Optional[str]

def get_prefix(c: Config) -> str:
    return c.prefix or "default_"
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Config {
    pub prefix: Option<String>,
}
impl Config {
    pub fn new(prefix: Option<String>) -> Self {
        Self { prefix }
    }
}
pub fn get_prefix(c: &Config) -> String {
    return c
        .prefix
        .clone()
        .clone()
        .unwrap_or_else(|| "default_".to_string());
}
'''

[[test]]
name = "is_none_dataclass_field"
skip = true
description = "Optional dataclass field is None check"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Person:
    age: Optional[int]

def is_missing(p: Person) -> bool:
    return p.age is None
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Person {
    pub age: Option<i32>,
}
impl Person {
    pub fn new(age: Option<i32>) -> Self {
        Self { age }
    }
}
pub fn is_missing(p: &Person) -> bool {
    return p.age.is_none();
}
'''

[[test]]
name = "is_not_none_dataclass_field"
skip = true
description = "Optional dataclass field is not None check"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Person:
    age: Optional[int]

def has_age(p: Person) -> bool:
    return p.age is not None
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Person {
    pub age: Option<i32>,
}
impl Person {
    pub fn new(age: Option<i32>) -> Self {
        Self { age }
    }
}
pub fn has_age(p: &Person) -> bool {
    return p.age.is_some();
}
'''

[[test]]
name = "optional_variable_is_none"
description = "Optional variable is None check"

python = '''
from typing import Optional

def check_none(x: Optional[int]) -> bool:
    return x is None
'''

rust = '''
pub fn check_none(x: &Option<i32>) -> bool {
    return x.is_none();
}
'''

[[test]]
name = "optional_variable_is_not_none"
description = "Optional variable is not None check"

python = '''
from typing import Optional

def check_not_none(x: Optional[int]) -> bool:
    return x is not None
'''

rust = '''
pub fn check_not_none(x: &Option<i32>) -> bool {
    return x.is_some();
}
'''

[[test]]
name = "optional_if_condition"
skip = true
description = "Optional field in if condition"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Person:
    name: Optional[str]

def check_name(p: Person) -> bool:
    if p.name:
        return True
    return False
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Person {
    pub name: Option<String>,
}
impl Person {
    pub fn new(name: Option<String>) -> Self {
        Self { name }
    }
}
pub fn check_name(p: &Person) -> bool {
    if p.name.as_ref().is_some_and(|s| !s.is_empty()) {
        return true;
    }
    return false;
}
'''

[[test]]
name = "optional_is_some_and"
skip = true
description = "Optional with is_some_and check"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Person:
    age: Optional[int]

def is_adult(p: Person) -> bool:
    if p.age and p.age >= 18:
        return True
    return False
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Person {
    pub age: Option<i32>,
}
impl Person {
    pub fn new(age: Option<i32>) -> Self {
        Self { age }
    }
}
pub fn is_adult(p: &Person) -> bool {
    let _cse_temp_0 = p.age.unwrap() >= 18;
    let _cse_temp_1 = (p.age.unwrap().is_some()) && (_cse_temp_0);
    if _cse_temp_1 {
        return true;
    }
    return false;
}
'''

[[test]]
name = "optional_field_add_assign"
skip = true
description = "Augmented += on Optional field requires unwrap/rewrap"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Counter:
    value: Optional[int]

def increment(c: Counter) -> None:
    c.value += 1
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Counter {
    pub value: Option<i32>,
}
impl Counter {
    pub fn new(value: Option<i32>) -> Self {
        Self { value }
    }
}
pub fn increment(c: &mut Counter) {
    let _cse_temp_0 = c.value.unwrap() + 1;
    c.value = Some(_cse_temp_0);
}
'''

[[test]]
name = "optional_field_sub_assign"
skip = true
description = "Augmented -= on Optional field requires unwrap/rewrap"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Counter:
    value: Optional[int]

def decrement(c: Counter) -> None:
    c.value -= 1
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Counter {
    pub value: Option<i32>,
}
impl Counter {
    pub fn new(value: Option<i32>) -> Self {
        Self { value }
    }
}
pub fn decrement(c: &mut Counter) {
    let _cse_temp_0 = c.value.unwrap() - 1;
    c.value = Some(_cse_temp_0);
}
'''

[[test]]
name = "optional_field_mul_assign"
skip = true
description = "Augmented *= on Optional field requires unwrap/rewrap"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Numbers:
    factor: Optional[int]

def double(n: Numbers) -> None:
    n.factor *= 2
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Numbers {
    pub factor: Option<i32>,
}
impl Numbers {
    pub fn new(factor: Option<i32>) -> Self {
        Self { factor }
    }
}
pub fn double(n: &mut Numbers) {
    let _cse_temp_0 = n.factor.unwrap() * 2;
    n.factor = Some(_cse_temp_0);
}
'''

[[test]]
name = "unary_neg_optional_field"
skip = true
description = "Unary negation on Optional field requires unwrap"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Numbers:
    value: Optional[int]

def negate(n: Numbers) -> int:
    return -n.value
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Numbers {
    pub value: Option<i32>,
}
impl Numbers {
    pub fn new(value: Option<i32>) -> Self {
        Self { value }
    }
}
pub fn negate(n: &Numbers) -> i32 {
    return (-n.value.unwrap());
}
'''

[[test]]
name = "unary_not_optional_field"
skip = true
description = "Unary not on Optional bool field requires unwrap"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Flags:
    active: Optional[bool]

def invert(f: Flags) -> bool:
    return not f.active
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Flags {
    pub active: Option<bool>,
}
impl Flags {
    pub fn new(active: Option<bool>) -> Self {
        Self { active }
    }
}
pub fn invert(f: &Flags) -> bool {
    return !f.active.unwrap();
}
'''

[[test]]
name = "unary_neg_optional_variable"
description = "Unary negation on Optional variable requires unwrap"

python = '''
from typing import Optional

def negate_var(x: Optional[int]) -> int:
    return -x
'''

rust = '''
pub fn negate_var(x: &Option<i32>) -> i32 {
    return (-x.unwrap());
}
'''

[[test]]
name = "len_on_optional_list"
skip = true
description = "len() on Optional list requires as_ref().unwrap()"

python = '''
from dataclasses import dataclass
from typing import Optional, List

@dataclass
class Container:
    items: Optional[List[int]]

def count_items(c: Container) -> int:
    return len(c.items)
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Container {
    pub items: Option<Vec<i32>>,
}
impl Container {
    pub fn new(items: Option<Vec<i32>>) -> Self {
        Self { items }
    }
}
pub fn count_items(c: &Container) -> i32 {
    return c.items.clone().as_ref().unwrap().len() as i32;
}
'''

[[test]]
name = "method_on_optional_string"
skip = true
description = "String method on Optional string requires unwrap"

python = '''
from dataclasses import dataclass
from typing import Optional

@dataclass
class Person:
    name: Optional[str]

def upper_name(p: Person) -> str:
    return p.name.upper()
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Person {
    pub name: Option<String>,
}
impl Person {
    pub fn new(name: Option<String>) -> Self {
        Self { name }
    }
}
pub fn upper_name(p: &Person) -> String {
    return p.name.clone().as_ref().unwrap().to_uppercase();
}
'''

[[test]]
name = "append_on_optional_list"
skip = true
description = "Mutating method on Optional list uses as_mut().unwrap()"

python = '''
from dataclasses import dataclass
from typing import Optional, List

@dataclass
class Container:
    items: Optional[List[int]]

def add_item(c: Container, item: int) -> None:
    c.items.append(item)
'''

rust = '''
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Container {
    pub items: Option<Vec<i32>>,
}
impl Container {
    pub fn new(items: Option<Vec<i32>>) -> Self {
        Self { items }
    }
}
pub fn add_item(c: &mut Container, item: i32) {
    c.items.as_mut().unwrap().push(item);
}
'''

[[test]]
name = "optional_list_indexing"
skip = true
description = "Indexing Optional list requires as_ref().unwrap()"

python = '''
from dataclasses import dataclass
from typing import Optional, List

@dataclass
class Container:
    items: Optional[List[int]]

def get_first(c: Container) -> int:
    return c.items[0]
'''

rust = '''
#[derive(Debug, Clone)]
pub struct IndexError {
    message: String,
}
impl std::fmt::Display for IndexError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "index out of range: {}", self.message)
    }
}
impl std::error::Error for IndexError {}
impl IndexError {
    pub fn new(message: impl Into<String>) -> Self {
        Self {
            message: message.into(),
        }
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Container {
    pub items: Option<Vec<i32>>,
}
impl Container {
    pub fn new(items: Option<Vec<i32>>) -> Self {
        Self { items }
    }
}
pub fn get_first(c: &Container) -> i32 {
    return c
        .items
        .clone()
        .as_ref()
        .unwrap()
        .get(0usize)
        .cloned()
        .unwrap();
}
'''

[[test]]
name = "optional_dict_indexing"
skip = true
description = "Indexing Optional dict requires as_ref().unwrap()"

python = '''
from dataclasses import dataclass
from typing import Optional, Dict

@dataclass
class Config:
    settings: Optional[Dict[str, int]]

def get_setting(c: Config, key: str) -> int:
    return c.settings[key]
'''

rust = '''
#[derive(Debug, Clone)]
pub struct IndexError {
    message: String,
}
impl std::fmt::Display for IndexError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "index out of range: {}", self.message)
    }
}
impl std::error::Error for IndexError {}
impl IndexError {
    pub fn new(message: impl Into<String>) -> Self {
        Self {
            message: message.into(),
        }
    }
}
#[derive(Debug, Clone, PartialEq, Default)]
pub struct Config {
    pub settings: Option<HashMap<String, i32>>,
}
impl Config {
    pub fn new(settings: Option<HashMap<String, i32>>) -> Self {
        Self { settings }
    }
}
pub fn get_setting(c: &Config, key: String) -> i32 {
    return c
        .settings
        .clone()
        .as_ref()
        .unwrap()
        .get(&key)
        .cloned()
        .unwrap();
}
'''

[[test]]
name = "none_placeholder_simple_if_else"
skip = true
description = "Simple None placeholder pattern with if-else reassignment"
compile_check = true

python = '''
def test_func():
    flag = True
    result = None
    if flag:
        result = "yes"
    else:
        result = "no"
    return result
'''

rust = '''
pub fn test_func() -> String {
    let flag = true;
    let mut result: Option<String> = None;
    if flag {
        result = Some("yes".to_string());
    } else {
        result = Some("no".to_string());
    }
    result.unwrap()
}
'''

[[test]]
name = "none_placeholder_elif_chain"
skip = true
description = "None placeholder with multiple elif branches"
compile_check = true

python = '''
def test_func():
    a = True
    b = False
    c = False
    value = None
    if a:
        value = 1
    elif b:
        value = 2
    elif c:
        value = 3
    else:
        value = 4
    return value
'''

rust = '''
pub fn test_func() -> i32 {
    let a = true;
    let b = false;
    let c = false;
    let mut value: Option<i32> = None;
    if a {
        value = Some(1);
    } else {
        if b {
            value = Some(2);
        } else {
            if c {
                value = Some(3);
            } else {
                value = Some(4);
            }
        }
    }
    value.unwrap()
}
'''

[[test]]
name = "none_placeholder_multiple_variables"
skip = true
description = "Multiple variables with None placeholder pattern"
compile_check = true

python = '''
def test_func():
    condition = True
    x = None
    y = None
    if condition:
        x = "a"
        y = "b"
    else:
        x = "c"
        y = "d"
    return (x, y)
'''

rust = '''
pub fn test_func() -> (String, String) {
    let condition = true;
    let mut x: Option<String> = None;
    let mut y: Option<String> = None;
    if condition {
        x = Some("a".to_string());
        y = Some("b".to_string());
    } else {
        x = Some("c".to_string());
        y = Some("d".to_string());
    }
    (x.unwrap(), y.unwrap())
}
'''

[[test]]
name = "none_placeholder_nested_if"
skip = true
description = "Nested if with None placeholder in both scopes"
compile_check = true

python = '''
def test_func():
    x = True
    y = False
    outer = None
    if x:
        outer = "x"
        inner = None
        if y:
            inner = "y"
        else:
            inner = "z"
    else:
        outer = "not-x"
    return outer
'''

rust = '''
pub fn test_func() -> String {
    let x = true;
    let y = false;
    let mut outer: Option<String> = None;
    if x {
        outer = Some("x".to_string());
        let mut inner: Option<String> = None;
        if y {
            inner = Some("y".to_string());
        } else {
            inner = Some("z".to_string());
        }
    } else {
        outer = Some("not-x".to_string());
    }
    outer.unwrap()
}
'''

[[test]]
name = "none_placeholder_function_parameter"
skip = true
description = "None placeholder with function parameter condition"
compile_check = true

python = '''
def compute(x: int) -> str:
    result = None
    if x > 0:
        result = "positive"
    elif x < 0:
        result = "negative"
    else:
        result = "zero"
    return result
'''

rust = '''
pub fn compute(x: i32) -> String {
    let mut result: Option<String> = None;
        if x > 0 {
        result = Some("positive".to_string());
    } else {
                if x < 0 {
            result = Some("negative".to_string());
        } else {
            result = Some("zero".to_string());
        }
    }
    result.unwrap()
}
'''

[[test]]
name = "option_truthiness_check"
skip = true
description = "Python truthiness check on Option - must use .is_some()"

python = '''
import os

def check_config():
    config_file = os.environ.get("CONFIG_FILE")
    if config_file:
        print("Config set")
    else:
        print("No config")
'''

rust = '''
use std as os;
pub fn check_config() {
    let config_file = std::env::var("CONFIG_FILE").ok();
    if config_file.is_some() {
        log::info!("{}", "Config set");
    } else {
        log::info!("{}", "No config");
    }
}
'''

[[test]]
name = "nested_option_truthiness"
skip = true
description = "Nested Option truthiness checks handled correctly"

python = '''
import os

def check_multiple():
    var1 = os.environ.get("VAR1")
    var2 = os.environ.get("VAR2")
    if var1:
        if var2:
            return "both"
        return "only_var1"
    return "none"
'''

rust = '''
use std as os;
pub fn check_multiple() -> String {
    let var1 = std::env::var("VAR1").ok();
    let var2 = std::env::var("VAR2").ok();
    if var1.is_some() {
        if var2.is_some() {
            return "both".to_string();
        }
        return "only_var1".to_string();
    }
    return "none".to_string();
}
'''

[[test]]
name = "string_method_type_consistency"
skip = true
description = "String method returns consistent type (String not &str)"

python = '''
def process_format(use_json: bool, format_str: str) -> str:
    if use_json:
        output_format = "json"
    else:
        output_format = format_str.lower()
    return output_format
'''

rust = '''
pub fn process_format(use_json: bool, format_str: String) -> String {
    let output_format;
    if use_json {
        output_format = "json".to_string();
    } else {
        output_format = format_str.to_lowercase();
    }
    return output_format;
}
'''

[[test]]
name = "type_narrowing_attribute_access"
skip = true
description = "Type narrowing allows attribute access on unwrapped Option"
compile_check = true

python = '''
from typing import Optional

class Player:
    def __init__(self, name: str, score: int):
        self.name = name
        self.score = score

def find_player(players: list[Player], name: str) -> Optional[Player]:
    for p in players:
        if p.name == name:
            return p
    return None

def update_score(players: list[Player], name: str, bonus: int) -> None:
    player = find_player(players, name)
    if player is not None:
        player.score = player.score + bonus
'''

rust = '''
#[derive(Debug, Clone)]
pub struct Player {
    pub name: String,
    pub score: i32,
}
impl Player {
    pub fn new(name: String, score: i32) -> Self {
        Self { name, score }
    }
}
pub fn find_player(players: &Vec<Player>, name: String) -> Option<Player> {
    for p in players.iter().cloned() {
        if p.name.clone() == name {
            return Some(p);
        }
    }
    return None;
}
pub fn update_score(players: Vec<Player>, name: String, bonus: i32) {
    let mut player = find_player(&players, name);
    if player.is_some() {
        let _cse_temp_0 = player.as_ref().unwrap().score + bonus;
        player.as_mut().unwrap().score = _cse_temp_0;
    }
}
'''

[[test]]
name = "type_narrowing_method_call"
description = "Type narrowing allows method calls on unwrapped Option"
compile_check = true

python = '''
from typing import Optional

def get_name() -> Optional[str]:
    return "hello"

def process_name() -> str:
    name = get_name()
    if name is not None:
        return name.upper()
    return "default"
'''

rust = '''
pub fn get_name() -> Option<String> {
    return Some("hello".to_string());
}
pub fn process_name() -> String {
    let name = get_name();
    if name.is_some() {
        return name.as_ref().unwrap().to_uppercase();
    }
    return "default".to_string();
}
'''

[[test]]
name = "type_narrowing_with_else"
skip = true
description = "Type narrowing with else branch"
compile_check = true

python = '''
from typing import Optional

def find_value(data: dict[str, int], key: str) -> Optional[int]:
    return data.get(key)

def process(data: dict[str, int], key: str) -> int:
    value = find_value(data, key)
    if value is not None:
        return value * 2
    else:
        return -1
'''

rust = '''
use std::collections::HashMap;
pub fn find_value(data: &HashMap<String, i32>, key: String) -> Option<i32> {
    return data.get(&key).cloned();
}
pub fn process(data: HashMap<String, i32>, key: String) -> i32 {
    let value = find_value(&data, key);
    if value.is_some() {
        return value.clone().unwrap() * 2;
    } else {
        return -1;
    }
}
'''

[[test]]
name = "type_narrowing_multiple_attributes"
skip = true
description = "Type narrowing allows multiple attribute accesses"
compile_check = true

python = '''
from typing import Optional

class Config:
    def __init__(self, timeout: int, retries: int):
        self.timeout = timeout
        self.retries = retries

def get_config() -> Optional[Config]:
    return Config(30, 3)

def print_config() -> None:
    config = get_config()
    if config is not None:
        print(config.timeout)
        print(config.retries)
'''

rust = '''
#[derive(Debug, Copy, Clone]
pub struct Config {
    pub timeout: i32,
    pub retries: i32,
}
impl Config {
    pub fn new(timeout: i32, retries: i32) -> Self {
        Self { timeout, retries }
    }
}
pub fn get_config() -> Option<Config> {
    return Some(Config::new(30, 3));
}
pub fn print_config() {
    let config = get_config();
    if config.is_some() {
        log::info!("{}", config.as_ref().unwrap().timeout);
        log::info!("{}", config.as_ref().unwrap().retries);
    }
}
'''

[[test]]
name = "type_narrowing_nested_calls"
skip = true
description = "Type narrowing with nested function calls"
compile_check = true

python = '''
from typing import Optional

def parse_int(s: str) -> Optional[int]:
    if s.isdigit():
        return int(s)
    return None

def double_parsed(s: str) -> int:
    result = parse_int(s)
    if result is not None:
        return result * 2
    return 0
'''

rust = '''
pub fn parse_int(s: String) -> Option<i32> {
    if s.chars().all(|c| c.is_numeric()) {
        return Some(s.parse::<i32>().unwrap());
    }
    return None;
}
pub fn double_parsed(s: String) -> i32 {
    let result = parse_int(s);
    if result.is_some() {
        return result.clone().unwrap() * 2;
    }
    return 0;
}
'''

[[test]]
name = "type_narrowing_list_operations"
skip = true
description = "Type narrowing allows list operations on unwrapped Option"

python = '''
from typing import Optional

class Container:
    def __init__(self):
        self.items: list[int] = []

def find_container(containers: list[Container], idx: int) -> Optional[Container]:
    if idx < len(containers):
        return containers[idx]
    return None

def add_item(containers: list[Container], idx: int, item: int) -> None:
    container = find_container(containers, idx)
    if container is not None:
        container.items.append(item)
'''

rust = '''
#[derive(Debug, Clone)]
pub struct IndexError {
    message: String,
}
impl std::fmt::Display for IndexError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "index out of range: {}", self.message)
    }
}
impl std::error::Error for IndexError {}
impl IndexError {
    pub fn new(message: impl Into<String>) -> Self {
        Self {
            message: message.into(),
        }
    }
}
#[derive(Debug, Clone)]
pub struct Container {}
impl Container {
    pub fn new() -> Self {
        Self {}
    }
}
pub fn find_container(containers: &Vec<Container>, idx: i32) -> Option<Container> {
    let _cse_temp_0 = containers.len() as i32;
        if idx < _cse_temp_0 {
        return Some(containers.get(idx as usize).cloned().unwrap());
    }
    return None;
}
pub fn add_item(containers: Vec<Container>, idx: i32, item: i32) {
    let mut container = find_container(&containers, idx);
    if container.is_some() {
        container.as_ref().unwrap().items.push(item);
    }
}
'''

[[test]]
name = "optional_var_not_in_list"
skip = true
description = "Optional variable checked with 'not in' uses .contains() not .get()"
compile_check = true

python = '''
from typing import Optional

def find_number(target: int) -> Optional[int]:
    if target > 0:
        return target
    return None

def add_if_missing(numbers: list[int], target: int) -> None:
    value = find_number(target)
    if value is not None:
        if value not in numbers:
            numbers.append(value)
'''

rust = '''
pub fn find_number(target: i32) -> Option<i32> {
        if target > 0 {
        return Some(target);
    }
    return None;
}
pub fn add_if_missing(numbers: &mut Vec<i32>, target: i32) {
    let value = find_number(target);
    if value.is_some() {
                if !numbers.contains(&value.clone().unwrap()) {
            numbers.push(value.clone().unwrap());
        }
    }
}
'''

[[test]]
name = "optional_var_in_list"
skip = true
description = "Optional variable checked with 'in' uses .contains()"
compile_check = true

python = '''
from typing import Optional

def get_id() -> Optional[int]:
    return 42

def check_membership(ids: list[int]) -> bool:
    id = get_id()
    if id is not None:
        return id in ids
    return False
'''

rust = '''
pub fn get_id() -> Option<i32> {
    return Some(42);
}
pub fn check_membership(ids: &Vec<i32>) -> bool {
    let id = get_id();
    if id.is_some() {
        return ids.contains(&id.clone().unwrap());
    }
    return false;
}
'''

[[test]]
name = "optional_var_multiple_uses"
description = "Optional variable used multiple times avoids move errors"
compile_check = true

python = '''
from typing import Optional

def get_value() -> Optional[str]:
    return "test"

def process_twice() -> str:
    value = get_value()
    if value is not None:
        first = value.upper()
        second = value.lower()
        return first + second
    return ""
'''

rust = '''
pub fn get_value() -> Option<String> {
    return Some("test".to_string());
}
pub fn process_twice() -> String {
    let value = get_value();
    if value.is_some() {
        let first = value.as_ref().unwrap().to_uppercase();
        let second = value.as_ref().unwrap().to_lowercase();
        return format!("{}{}", first, second);
    }
    return "".to_string();
}
'''

[[test]]
name = "optional_var_push_to_list"
description = "Optional variable pushed to list uses clone().unwrap()"
compile_check = true

python = '''
from typing import Optional

class Item:
    def __init__(self, name: str):
        self.name = name

def find_item(name: str) -> Optional[Item]:
    if name:
        return Item(name)
    return None

def collect_items(names: list[str]) -> list[Item]:
    items: list[Item] = []
    for name in names:
        item = find_item(name)
        if item is not None:
            items.append(item)
    return items
'''

rust = '''
#[derive(Debug, Clone)]
pub struct Item {
    pub name: String,
}
impl Item {
    pub fn new(name: String) -> Self {
        Self { name }
    }
}
pub fn find_item(name: String) -> Option<Item> {
    if !name.is_empty() {
        return Some(Item::new(name));
    }
    return None;
}
pub fn collect_items(names: &Vec<String>) -> Vec<Item> {
    let mut items: Vec<Item> = vec![];
    for name in names.iter().cloned() {
        let item = find_item(name);
        if item.is_some() {
            items.push(item.clone().unwrap());
        }
    }
    return items;
}
'''

[[test]]
name = "optional_var_contains_and_append"
skip = true
description = "Optional variable used in both contains check and append"
compile_check = true

python = '''
from typing import Optional

def get_name() -> Optional[str]:
    return "Alice"

def add_unique_name(names: list[str]) -> None:
    name = get_name()
    if name is not None:
        if name not in names:
            names.append(name)
'''

rust = '''
pub fn get_name() -> Option<String> {
    return Some("Alice".to_string());
}
pub fn add_unique_name(names: &mut Vec<String>) {
    let name = get_name();
    if name.clone().is_some() {
                if !names.contains(&name.clone().unwrap()) {
            names.push(name.clone().unwrap());
        }
    }
}
'''

[[test]]
name = "optional_nested_attribute_access"
skip = true
description = "Optional variable unwrapped when accessing nested properties"

python = '''
class Inner:
    value: int

class Outer:
    inner: Inner

def test_optional_access(opt: Outer | None) -> int:
    if opt is not None:
        return opt.inner.value
    return 0
'''

rust = '''
#[derive(Debug, Copy, Clone]
pub struct Inner {
    pub value: i32,
}
impl Inner {
    pub fn new(value: i32) -> Self {
        Self { value }
    }
}
#[derive(Debug, Clone)]
pub struct Outer {
    pub inner: Inner,
}
impl Outer {
}
pub fn test_optional_access(opt: &Option<Outer>) -> i32 {
    if opt.is_some() {
        return opt.as_ref().unwrap().inner.value;
    }
    return 0;
}
'''

[[test]]
name = "optional_assignment_then_access"
skip = true
description = "Optional variable assigned then accessed for nested fields"

python = '''
class Scores:
    total: int
    tries: int

class PeriodStatistic:
    scores: Scores

class State:
    period_statistics: list[PeriodStatistic]

def test_function(state: State) -> None:
    extra_time_stats: PeriodStatistic | None = None
    
    if state is not None:
        extra_time_stats = state.period_statistics[2]
        print(extra_time_stats.scores.tries)
'''

rust = '''
#[derive(Debug, Clone)]
pub struct IndexError {
    message: String,
}
impl std::fmt::Display for IndexError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "index out of range: {}", self.message)
    }
}
impl std::error::Error for IndexError {}
impl IndexError {
    pub fn new(message: impl Into<String>) -> Self {
        Self {
            message: message.into(),
        }
    }
}
#[derive(Debug, Copy, Clone]
pub struct Scores {
    pub total: i32,
    pub tries: i32,
}
impl Scores {
    pub fn new(total: i32, tries: i32) -> Self {
        Self { total, tries }
    }
}
#[derive(Debug, Clone)]
pub struct PeriodStatistic {
    pub scores: Scores,
}
impl PeriodStatistic {
}
#[derive(Debug, Clone)]
pub struct State {
    pub period_statistics: Vec<PeriodStatistic>,
}
impl State {
}
pub fn test_function(state: &State) {
    let mut extra_time_stats: Option<PeriodStatistic> = None;
    if state.is_some() {
        extra_time_stats = Some(
            state
                .period_statistics
                .clone()
                .get(2usize)
                .cloned()
                .unwrap(),
        );
        log::info!("{}", extra_time_stats.as_ref().unwrap().scores.tries);
    }
}
'''

# =============================================================================
# Optional Type Unwrapping in Binary Operations
# Python allows direct access to Optional fields - operations on None fail at runtime.
# Rust needs explicit unwrapping at compile time.
# =============================================================================

[[test]]
name = "optional_in_binary_addition"
description = "Optional value in addition operation generates unwrap"

python = '''
from typing import Optional

def add_to_optional(value: Optional[int], amount: int) -> int:
    return value + amount
'''

rust = '''
pub fn add_to_optional(value: &Option<i32>, amount: i32) -> i32 {
    return value.unwrap() + amount;
}
'''

[[test]]
name = "optional_field_method_call"
skip = true
description = "Method call on Optional field generates unwrap or ?"

python = '''
from typing import Optional

def get_length(text: Optional[str]) -> int:
    return len(text)
'''

rust = '''
pub fn get_length(text: &Option<String>) -> i32 {
    return text.as_ref().unwrap().len() as i32;
}
'''

[[test]]
name = "optional_in_collection_lookup"
skip = true
description = "x in Optional collection handles Optional lookup"

python = '''
from typing import Optional

def contains_item(items: Optional[list], target: int) -> bool:
    return target in items
'''

rust = '''
pub fn contains_item(items: &Option<Vec<i32>>, target: i32) -> bool {
    return items.as_ref().unwrap().contains(&target);
}
'''

[[test]]
name = "optional_chain_access"
skip = true
description = "Optional chain a.b.c where b is Optional needs proper unwrapping"

python = '''
from typing import Optional

class Inner:
    value: int

class Outer:
    inner: Optional[Inner]

def get_value(obj: Outer) -> int:
    return obj.inner.value
'''

rust = '''
#[derive(Debug, Copy, Clone]
pub struct Inner {
    pub value: i32,
}
#[derive(Debug, Clone)]
pub struct Outer {
    pub inner: Option<Inner>,
}
pub fn get_value(obj: &Outer) -> i32 {
    return obj.inner.as_ref().unwrap().value;
}
'''

[[test]]
name = "optional_none_check_before_operation"
description = "None check before operation allows direct access"

python = '''
from typing import Optional

def safe_add(value: Optional[int]) -> int:
    if value is not None:
        return value + 1
    return 0
'''

rust = '''
pub fn safe_add(value: &Option<i32>) -> i32 {
    if value.is_some() {
        return value.unwrap() + 1;
    }
    return 0;
}
'''

[[test]]
name = "optional_comparison_operation"
description = "Optional value in comparison operation"

python = '''
from typing import Optional

def is_greater(value: Optional[int], threshold: int) -> bool:
    return value > threshold
'''

rust = '''
pub fn is_greater(value: &Option<i32>, threshold: i32) -> bool {
    return value.unwrap() > threshold;
}
'''

[[test]]
name = "optional_string_concatenation"
skip = true
description = "Optional string in concatenation operation"

python = '''
from typing import Optional

def greet(name: Optional[str]) -> str:
    return "Hello, " + name + "!"
'''

rust = '''
pub fn greet(name: &Option<String>) -> String {
    return format!("Hello, {}!", name.as_ref().unwrap());
}
'''

[[test]]
name = "optional_multiplication"
description = "Optional value in multiplication"

python = '''
from typing import Optional

def double_optional(value: Optional[float]) -> float:
    return value * 2.0
'''

rust = '''
pub fn double_optional(value: &Option<f64>) -> f64 {
    return value.unwrap() * 2.0;
}
'''