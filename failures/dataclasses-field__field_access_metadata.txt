  dataclasses-field.toml :: field_access_metadata
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't have runtime field metadata access
−     #[derive(Debug)]
−     struct Config {
−         timeout: i32,
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Config {
+     pub timeout: i32,
+ }
+ impl Config {
+     pub fn new() -> Self {
+         Self {
+             timeout: Default::default(),
+         }
      }
− 
−     let c = Config { timeout: 30 };
−     let result = true;
+ }
────────────────────────────────────────────────────────────