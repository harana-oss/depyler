  async-comprehensions.toml :: async_set_comp
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
+ use serde_json;
  use std::collections::HashSet;
− 
− #[tokio::main]
− async let result: HashSet<i32> = stream::iter(0..5).collect().await;
− let mut sorted: Vec<_> = result.into_iter().collect();
− sorted.sort();
− log::info!("{:?}", sorted);
+     #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncRangeState {
+     state: usize, n: serde_json::Value
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_range(n: & serde_json::Value) -> impl Iterator<Item = i32>{
+     AsyncRangeState {
+     state: 0, n: n
+ }
+ } impl Iterator for AsyncRangeState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _i in 0..self.n {
+     return Some(i);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test() {
+     let result = async_range(5).iter().cloned().collect::<HashSet<_>>();
+     return {
+     let mut __sorted_result = result.clone();
+     __sorted_result.sort();
+     __sorted_result
+ }
+ }
────────────────────────────────────────────────────────────