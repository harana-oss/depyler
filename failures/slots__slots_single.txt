  slots.toml :: slots_single
    Output mismatch

────────────────────────────────────────────────────────────
− struct Single {
−     x: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Single {
+     pub x: serde_json::Value,
  }
− 
  impl Single {
−     fn new(x: i32) -> Self {
−         Single { x }
+     pub const __slots__: String = "x".to_string();
+     pub fn new(x: serde_json::Value) -> Self {
+         Self { x }
      }
  }
− 
− let s = Single::new(42);
− log::info!("{}", s.x);
────────────────────────────────────────────────────────────