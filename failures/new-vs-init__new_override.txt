  new-vs-init.toml :: new_override
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone)]
+ #[derive(Debug, Copy, Clone)]
  pub struct Parent {}
  impl Parent {
      pub fn new() -> Self {
          Self {}
      }
  }
− #[derive(Debug, Clone)]
+ #[derive(Debug, Copy, Clone)]
  pub struct Child {}
  impl Child {
      pub fn new() -> Self {
          Self {}
      }
  }
  pub fn create() -> Child {
      return Child::new();
  }
────────────────────────────────────────────────────────────