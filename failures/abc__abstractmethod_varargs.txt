  abc.toml :: abstractmethod_varargs
    Output mismatch

────────────────────────────────────────────────────────────
− trait Base {
−     fn process(&self) -> i32;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Base {}
+ impl Base {
+     pub fn new() -> Self {
+         Self {}
+     }
+     pub fn process(&self) -> i32 {
+         {}
+     }
  }
  #[derive(Debug, Copy, Clone)]
  pub struct Concrete {}
  impl Concrete {
      pub fn new() -> Self {
          Self {}
      }
−     pub fn process(&self) -> i32 {
+ }
+ impl Base for Concrete {
+     fn process(&self) -> i32 {
          return sum(args) + kwargs.len();
      }
  }
  pub fn test_abstractmethod_varargs() -> i32 {
      let obj = Concrete::new();
−     return obj.process();
+     return obj.process(1, 2, 3, 4, 5);
  }
────────────────────────────────────────────────────────────