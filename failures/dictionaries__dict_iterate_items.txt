  dictionaries.toml :: dict_iterate_items
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  use std::collections::HashMap;
− pub fn print_items(d: &HashMap<String, i32>) {
+ pub fn print_items(d: &HashMap<serde_json::Value, serde_json::Value>) {
      for (key, value) in d
          .iter()
          .map(|(k, v)| (k.clone(), v.clone()))
          .collect::<Vec<_>>()
      {
          log::info!("{}", format!("{}: {}", key, value));
      }
  }
────────────────────────────────────────────────────────────