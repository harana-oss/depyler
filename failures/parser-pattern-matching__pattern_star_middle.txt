  parser-pattern-matching.toml :: pattern_star_middle
    Output mismatch

────────────────────────────────────────────────────────────
− fn get_first_last(items: Vec<i64>) -> String {
−     match items.as_slice() {
−         [] | [_] => "too short".to_string(),
−         [first, .., last] => format!("{} to {}", first, last),
−         _ => "too short".to_string(),
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
      }
  }
− 
− fn main() {
−     let result = get_first_last(vec![1, 2, 3, 4, 5]);
−     println!("{}", result);
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn get_first_last(items: &Vec<serde_json::Value>) -> Result<String, IndexError> {
+     let length = items.len() as i32;
+     if length < 2 {
+         return Ok("too short".to_string());
+     }
+     let first = items.get(0usize).cloned().unwrap();
+     let last = items.last().cloned().unwrap();
+     return Ok(format!("{} to {}", first, last));
  }
────────────────────────────────────────────────────────────