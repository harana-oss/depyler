  optional-types.toml :: append_on_optional_list
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Container {
      pub items: Option<Vec<i32>>,
  }
  impl Container {
      pub fn new(items: Option<Vec<i32>>) -> Self {
          Self { items }
      }
  }
  pub fn add_item(c: &mut Container, item: i32) {
−     c.items.as_mut().unwrap().push(item);
+     c.items.push(item);
  }
────────────────────────────────────────────────────────────