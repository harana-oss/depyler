  optional-types.toml :: optional_field_mul_assign
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, PartialEq, Default)]
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
  pub struct Numbers {
      pub factor: Option<i32>,
  }
  impl Numbers {
      pub fn new(factor: Option<i32>) -> Self {
          Self { factor }
      }
  }
  pub fn double(n: &mut Numbers) {
−     let _cse_temp_0 = n.factor.unwrap() * 2;
−     n.factor = Some(_cse_temp_0);
+     n.factor *= 2;
  }
────────────────────────────────────────────────────────────