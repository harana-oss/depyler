  optional-types.toml :: type_narrowing_list_operations
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct IndexError {
      message: String,
  }
  impl std::fmt::Display for IndexError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "index out of range: {}", self.message)
      }
  }
  impl std::error::Error for IndexError {}
  impl IndexError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− #[derive(Debug, Clone)]
+ #[derive(Debug, Copy, Clone)]
  pub struct Container {}
  impl Container {
      pub fn new() -> Self {
          Self {}
      }
  }
− pub fn find_container(containers: &Vec<Container>, idx: i32) -> Option<Container> {
−     let _cse_temp_0 = containers.len() as i32;
−     if idx < _cse_temp_0 {
−         return Some(containers.get(idx as usize).cloned().unwrap());
+ pub fn find_container(
+     containers: &Vec<Container>,
+     idx: i32,
+ ) -> Result<Option<Container>, IndexError> {
+     if idx < containers.len() as i32 {
+         return Ok(Some(containers.get(idx as usize).cloned().unwrap()));
      }
−     return None;
+     return Ok(None);
  }
− pub fn add_item(containers: Vec<Container>, idx: i32, item: i32) {
−     let mut container = find_container(&containers, idx);
+ pub fn add_item(
+     containers: &Vec<Container>,
+     idx: &i32,
+     item: i32,
+ ) -> Result<(), Box<dyn std::error::Error>> {
+     let container = find_container(&containers, idx)?;
      if container.is_some() {
          container.as_ref().unwrap().items.push(item);
      }
+     Ok(())
  }
────────────────────────────────────────────────────────────