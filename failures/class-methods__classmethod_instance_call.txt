  class-methods.toml :: classmethod_instance_call
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Copy, Clone)]
  pub struct MyClass {}
  impl MyClass {
      pub const value: i32 = 100;
      pub fn new() -> Self {
          Self {}
      }
      pub fn get_value() {
          return Self::value;
      }
  }
− let obj = MyClass::new();
− log::info!("{}", obj.get_value());
− log::info!("{}", MyClass::get_value());
────────────────────────────────────────────────────────────