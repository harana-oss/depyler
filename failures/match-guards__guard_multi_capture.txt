  match-guards.toml :: guard_multi_capture
    Output mismatch

────────────────────────────────────────────────────────────
− // case [first, *rest] if len(rest) > 0: ...
−     let value = vec![1, 2, 3, 4];
−     let result = match value.as_slice() {
−         [first, rest @ ..] if rest.len() > 0 => *first == 1 && rest.len() == 3,
−         _ => false,
−     };
+ 
────────────────────────────────────────────────────────────