  functools-wraps.toml :: update_wrapper
    Output mismatch

────────────────────────────────────────────────────────────
− // update_wrapper(wrapper, wrapped)
−     // Rust doesn't have runtime wrapper updates
−     // Metadata is compile-time
−     let result = true;
+ use std::update_wrapper;
+ #[doc = "Original function"]
+ pub fn original() {}
+ pub fn wrapper() {
+     return original();
+ }
────────────────────────────────────────────────────────────