  verification-contracts.toml :: invariant_assertion
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn sum_positive(items: &Vec<i32>) -> i32 {
      let mut total = 0;
      for item in items.iter().cloned() {
          if item > 0 {
              total += item;
          }
−         assert!(total >= 0, "running total must be non-negative");
+         assert!(total >= 0, "{}", "running total must be non-negative");
      }
      return total;
  }
────────────────────────────────────────────────────────────