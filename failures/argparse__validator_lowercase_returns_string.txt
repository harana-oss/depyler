  argparse.toml :: validator_lowercase_returns_string
    Output mismatch

────────────────────────────────────────────────────────────
  use clap::Parser;
  #[derive(Debug, clap::Parser)]
  struct Args {
      #[arg(long)]
      text: Option<String>,
  }
  #[doc = "Validator that returns lowercase string."]
− pub fn lowercase_validator(value: String) -> String {
+ pub fn lowercase_validator(value: &str) -> String {
      return value.to_lowercase();
  }
  pub fn main() {}
────────────────────────────────────────────────────────────