  imports-basic.toml :: import_conditional
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust: use statements at module level only
−     // Conditional logic uses the imported module conditionally
−     use std::env;
−     let cond = true;
−     let result = if cond {
−         env::current_dir().is_ok()
−     } else {
−         false
−     };
−     assert!(result);
+ 
────────────────────────────────────────────────────────────