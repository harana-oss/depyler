  parenthesized-context.toml :: paren_multiline
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust: multiple bindings in scope
−     {
−         let a = String::from("a");
−         let b = String::from("b");
−         let result = (a.clone(), b.clone());
−         assert_eq!(result, (String::from("a"), String::from("b")));
−     }
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct CM {
+     pub value: serde_json::Value,
+ }
+ impl CM {
+     pub fn new(value: serde_json::Value) -> Self {
+         Self { value }
+     }
+     pub fn __enter__(&self) {
+         return self.value.clone();
+     }
+     pub fn __exit__(&self) {
+         {}
+     }
+ }
────────────────────────────────────────────────────────────