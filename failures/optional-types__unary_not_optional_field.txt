  optional-types.toml :: unary_not_optional_field
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, PartialEq, Default)]
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
  pub struct Flags {
      pub active: Option<bool>,
  }
  impl Flags {
      pub fn new(active: Option<bool>) -> Self {
          Self { active }
      }
  }
  pub fn invert(f: &Flags) -> bool {
−     return !f.active.unwrap();
+     return !f.active;
  }
────────────────────────────────────────────────────────────