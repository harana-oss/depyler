  modules-csv.toml :: csv_dictwriter_basic
    Output mismatch

────────────────────────────────────────────────────────────
  use csv;
− pub fn write_data(filename: String) {
+ pub fn write_data(filename: String) -> Result<(), std::io::Error> {
      let mut f = std::fs::File::create(filename)?;
      let writer = csv::Writer::from_writer(f);
      writer.writeheader();
+     Ok(())
  }
────────────────────────────────────────────────────────────