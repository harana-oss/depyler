  match-as-patterns.toml :: as_nested_binding
    Output mismatch

────────────────────────────────────────────────────────────
− // case (Point(x, y) as p1, Point(a, b) as p2): ...
−     #[derive(Debug)]
−     struct Point {
−         x: i32,
−         y: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Point {
+     pub x: serde_json::Value,
+     pub y: serde_json::Value,
+ }
+ impl Point {
+     pub fn new(x: serde_json::Value, y: serde_json::Value) -> Self {
+         Self { x, y }
      }
− 
−     let value = (Point { x: 1, y: 2 }, Point { x: 3, y: 4 });
−     let result = match value {
−         (p1 @ Point { x, y }, p2 @ Point { x: a, y: b }) => x == 1 && y == 2 && a == 3 && b == 4,
−         _ => false,
−     };
+ }
────────────────────────────────────────────────────────────