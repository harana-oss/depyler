  collection-edge-cases.toml :: empty_dict_access
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashMap;
− 
− pub fn get_or_default(key: &str) -> i32 {
−     let d: HashMap<String, i32> = HashMap::new();
−     return *d.get(key).unwrap_or(&0);
+ pub fn get_or_default(key: String) -> i32 {
+     let d: HashMap<String, i32> = {
+         let map = HashMap::new();
+         map
+     };
+     return *d.get(&key).unwrap_or(&0);
  }
────────────────────────────────────────────────────────────