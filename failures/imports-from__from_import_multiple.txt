  imports-from.toml :: from_import_multiple
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust: import multiple items
−     use std::env::current_dir;
−     use std::fs::read_dir;
−     let cwd = current_dir().is_ok();
−     let files = read_dir(".").is_ok();
−     let result = cwd && files;
−     assert!(result);
+ use std::env::current_dir;
+ use std::listdir;
────────────────────────────────────────────────────────────