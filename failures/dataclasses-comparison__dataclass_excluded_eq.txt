  dataclasses-comparison.toml :: dataclass_excluded_eq
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
−     struct Item {
−         id: i32,
−         metadata: String,
−     }
− 
−     impl PartialEq for Item {
−         fn eq(&self, other: &Self) -> bool {
−             self.id == other.id
+ pub const result: serde_json::Value =
+     Item::new(1, "old".to_string()) == Item::new(1, "new".to_string());
+ use serde_json;
+ #[derive(Debug, Clone, PartialEq, Default)]
+ pub struct Item {
+     pub id: i32,
+     pub metadata: String,
+ }
+ impl Item {
+     pub fn new(id: i32) -> Self {
+         Self {
+             id,
+             metadata: Default::default(),
          }
      }
− 
−     let result = true;
+ }
────────────────────────────────────────────────────────────