  contextlib-decorator.toml :: contextmanager_environ
    Output mismatch

────────────────────────────────────────────────────────────
− use std::env;
− 
−     fn set_env<F, R>(key: &str, value: &str, f: F) -> R
−     where
−         F: FnOnce() -> R,
−     {
−         let old_value = env::var(key).ok();
−         env::set_var(key, value);
−         let result = f();
−         match old_value {
−             Some(val) => env::set_var(key, val),
−             None => env::remove_var(key),
+ use serde_json;
+ use std as os;
+ pub const result: &str = "environ implemented";
+ #[doc = " Generator state struct"]
+ #[derive(Debug)]
+ struct SetEnvState {
+     state: usize,
+     old_value: serde_json::Value,
+     key: serde_json::Value,
+     value: serde_json::Value,
+ }
+ #[doc = " Generator function - returns Iterator"]
+ pub fn set_env(
+     key: &serde_json::Value,
+     value: &serde_json::Value,
+ ) -> impl Iterator<Item = serde_json::Value> {
+     SetEnvState {
+         state: 0,
+         old_value: Default::default(),
+         key: key,
+         value: value,
+     }
+ }
+ impl Iterator for SetEnvState {
+     type Item = serde_json::Value;
+     fn next(&mut self) -> Option<Self::Item> {
+         match self.state {
+             0 => {
+                 self.state = 1;
+                 self.old_value = std::env::var(self.key).ok();
+                 os.environ.insert(self.key, self.value);
+                 return None;
+                 if self.old_value.is_none() {
+                     std::env::vars.remove(&self.key);
+                 } else {
+                     os.environ.insert(self.key, self.old_value);
+                 }
+                 None
+             }
+             _ => None,
          }
−         result
      }
− 
−     let result = "environ implemented";
+ }
────────────────────────────────────────────────────────────