  assert-statement.toml :: assert_postcondition
    Output mismatch

────────────────────────────────────────────────────────────
− fn get_positive(x: i32) -> i32 {
+ use serde_json;
+ pub fn get_positive(x: &serde_json::Value) -> i32 {
      let result = x.abs();
−     assert!(result >= 0, "Result must be positive");
−     result
+     assert!(result >= 0, "{}", "Result must be positive");
+     return result;
  }
− 
− let value = get_positive(-5);
− let result = value == 5;
────────────────────────────────────────────────────────────