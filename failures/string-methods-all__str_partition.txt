  string-methods-all.toml :: str_partition
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: (String, String, String) = {
−     let s = "hello";
−     let sep = "l";
−     match s.find(sep) {
−         Some(pos) => (
−             s[..pos].to_string(),
−             sep.to_string(),
−             s[pos + sep.len()..].to_string(),
−         ),
−         None => (s.to_string(), String::new(), String::new()),
−     }
− };
+ 
────────────────────────────────────────────────────────────