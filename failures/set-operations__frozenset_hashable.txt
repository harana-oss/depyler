  set-operations.toml :: frozenset_hashable
    Output mismatch

────────────────────────────────────────────────────────────
− pub const fs: std::sync::Arc<HashSet<i32>> =
−     std::sync::Arc::new(vec![1, 2, 3].into_iter().collect::<HashSet<_>>());
− pub const result: (String, i32) = (d.clone().get(&fs).cloned().unwrap(), s.len() as i32);
− lazy_static! {
−     pub static ref d: HashMap<Point, String> = {
−         let mut map = HashMap::new();
−         map.insert(fs, "value".to_string());
−         map
−     };
−     pub static ref s: HashSet<Point> = {
−         let mut set = HashSet::new();
−         set.insert(fs);
−         set.insert(std::sync::Arc::new(
−             vec![4, 5].into_iter().collect::<HashSet<_>>(),
−         ));
−         set
−     };
− }
− use lazy_static::lazy_static;
− use serde_json;
− use std::collections::HashMap;
− use std::collections::HashSet;
+ 
────────────────────────────────────────────────────────────