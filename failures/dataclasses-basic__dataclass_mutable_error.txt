  dataclasses-basic.toml :: dataclass_mutable_error
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, Default)]
−     pub struct Container {
−         items: Vec<i32>,
−     }
− 
−     impl Container {
−         pub fn new() -> Self {
−             Self { items: Vec::new() }
−         }
+ use serde_json;
+ #[derive(Debug, Clone, PartialEq, Default)]
+ pub struct Container {
+     pub items: Vec<serde_json::Value>,
+ }
+ impl Container {
+     pub fn new() -> Self {
+         Self { items: Vec::new() }
      }
− 
−     let c = Container::new();
−     let result = true;
+ }
────────────────────────────────────────────────────────────