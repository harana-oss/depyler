  await-expressions.toml :: await_exception
    Output mismatch

────────────────────────────────────────────────────────────
− async fn failing_coro() -> Result<(), String> {
−     Err("failure".to_string())
+ pub async fn failing_coro() -> Result <(), RuntimeError>{
+     return Err(RuntimeError::new("failure"));
+     Ok(())
  }
− 
− async fn test_await_exception() -> bool {
−     match failing_coro().await {
−         Ok(_) => false,
−         Err(_) => true,
−     }
+ pub async fn test_await_exception() -> bool {
+     { failing_coro().await;
+     return false;
+     return true;
+    
+ }
  }
────────────────────────────────────────────────────────────