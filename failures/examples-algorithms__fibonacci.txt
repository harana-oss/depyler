  examples-algorithms.toml :: fibonacci
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashMap;
  #[derive(Debug, Clone)]
  pub struct IndexError {
      message: String,
  }
  impl std::fmt::Display for IndexError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "index out of range: {}", self.message)
      }
  }
  impl std::error::Error for IndexError {}
  impl IndexError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  #[doc = "Classic recursive fibonacci - demonstrates recursion"]
  pub fn fibonacci_recursive(n: i32) -> i32 {
      if n <= 1 {
          return n;
      }
−     fibonacci_recursive(n - 1) + fibonacci_recursive(n - 2)
+     return fibonacci_recursive(n - 1) + fibonacci_recursive(n - 2);
  }
  #[doc = "Memoized fibonacci - demonstrates optimization patterns"]
− pub fn fibonacci_memoized(n: i32, mut memo: HashMap<i32, i32>) -> Result<i32, IndexError> {
+ pub fn fibonacci_memoized(n: i32, memo: &mut HashMap<i32, i32>) -> Result<i32, IndexError> {
      if memo.is_none() {
          memo = {
              let map = HashMap::new();
              map
          };
      }
−     let _cse_temp_0 = memo.contains_key(&n);
−     if _cse_temp_0 {
−         return Ok(memo.get(n as usize).cloned().unwrap_or_default());
+     if memo.get(&n).is_some() {
+         return Ok(memo.get(&n).cloned().unwrap());
      }
−     let _cse_temp_1 = n <= 1;
      let mut result;
−     if _cse_temp_1 {
+     if n <= 1 {
          result = n;
      } else {
−         let _cse_temp_2 = fibonacci_memoized(n - 1, memo) + fibonacci_memoized(n - 2, memo);
−         result = _cse_temp_2;
+         let _cse_temp_0 = fibonacci_memoized(n - 1, &memo) + fibonacci_memoized(n - 2, &memo);
+         result = _cse_temp_0;
      }
      memo.insert(n, result);
−     Ok(result)
+     return Ok(result);
  }
  #[doc = "Iterative fibonacci - demonstrates loops and efficiency"]
  pub fn fibonacci_iterative(n: i32) -> i32 {
      if n <= 1 {
          return n;
      }
      let (mut a, mut b) = (0, 1);
      for __ in 2..n + 1 {
          (a, b) = (b, a + b);
      }
−     b
+     return b;
  }
────────────────────────────────────────────────────────────