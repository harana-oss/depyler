  init-subclass.toml :: init_subclass_basic
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't have dynamic subclass hooks
−     // Use declarative macros or compile-time code gen
−     // Conceptual equivalent:
−     trait Base {
−         fn name() -> &'static str;
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Base {}
+ impl Base {
+     pub const subclass_created: Vec<serde_json::Value> = vec![];
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     struct Sub1;
−     impl Base for Sub1 {
−         fn name() -> &'static str {
−             "Sub1"
−         }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Sub1 {}
+ impl Sub1 {
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     struct Sub2;
−     impl Base for Sub2 {
−         fn name() -> &'static str {
−             "Sub2"
−         }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Sub2 {}
+ impl Sub2 {
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     // Would need to manually register or use a macro
−     let names = vec![Sub1::name(), Sub2::name()];
−     assert_eq!(names, vec!["Sub1", "Sub2"]);
+ }
────────────────────────────────────────────────────────────