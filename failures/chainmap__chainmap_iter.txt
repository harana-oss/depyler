  chainmap.toml :: chainmap_iter
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::{HashMap, HashSet};
− 
− pub fn test_chainmap_iter() -> Vec<String> {
−     let mut dict1 = HashMap::new();
−     dict1.insert("a".to_string(), 1);
−     dict1.insert("b".to_string(), 2);
− 
−     let mut dict2 = HashMap::new();
−     dict2.insert("b".to_string(), 3);
−     dict2.insert("c".to_string(), 4);
− 
−     let maps = vec![dict1, dict2];
− 
−     let mut keys = HashSet::new();
−     for map in &maps {
−         for key in map.keys() {
−             keys.insert(key.clone());
−         }
−     }
− 
−     let mut result: Vec<String> = keys.into_iter().collect();
−     result.sort();
− 
−     return result;
+ use serde_json;
+ use std::collections::ChainMap;
+ use std::collections::HashMap;
+ pub fn test_chainmap_iter() -> Vec<serde_json::Value> {
+     let dict1 = {
+         let mut map = HashMap::new();
+         map.insert("a".to_string(), 1);
+         map.insert("b".to_string(), 2);
+         map
+     };
+     let dict2 = {
+         let mut map = HashMap::new();
+         map.insert("b".to_string(), 3);
+         map.insert("c".to_string(), 4);
+         map
+     };
+     let c = ChainMap::new(dict1, dict2);
+     return {
+         let mut __sorted_result = c.into_iter().collect::<Vec<_>>().clone();
+         __sorted_result.sort();
+         __sorted_result
+     };
  }
────────────────────────────────────────────────────────────