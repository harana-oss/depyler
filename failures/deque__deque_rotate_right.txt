  deque.toml :: deque_rotate_right
    Output mismatch

────────────────────────────────────────────────────────────
− // d.rotate(1)
−     use std::collections::VecDeque;
− 
−     let mut d = VecDeque::from(vec![1, 2, 3]);
−     if let Some(x) = d.pop_back() {
−         d.push_front(x);
−     }
−     let result = d.iter().cloned().collect::<Vec<_>>() == vec![3, 1, 2];
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────