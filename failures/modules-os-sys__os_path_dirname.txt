  modules-os-sys.toml :: os_path_dirname
    Output mismatch

────────────────────────────────────────────────────────────
  use std::path;
  pub fn get_directory(path: String) -> String {
−     return std::path::Path::new(&path)
+     return std::path::Path::new(path)
          .parent()
−         .map(|p| p.to_string_lossy().to_string())
−         .unwrap_or_default();
+         .and_then(|p| p.to_str())
+         .unwrap_or("")
+         .to_string();
  }
────────────────────────────────────────────────────────────