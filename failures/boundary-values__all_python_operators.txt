  boundary-values.toml :: all_python_operators
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ZeroDivisionError {
      message: String,
  }
  impl std::fmt::Display for ZeroDivisionError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "division by zero: {}", self.message)
      }
  }
  impl std::error::Error for ZeroDivisionError {}
  impl ZeroDivisionError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn all_operators(a: i32, b: i32) -> bool {
+ pub fn all_operators(a: i32, b: i32) -> Result<bool, ZeroDivisionError> {
      let add = a + b;
      let sub = a - b;
−     let _cse_temp_0 = a * b;
−     let mul = _cse_temp_0;
−     let _cse_temp_1 = {
+     let mul = a * b;
+     let div = {
          let a = a;
          let b = b;
          let q = a / b;
          let r = a % b;
          let r_negative = r < 0;
          let b_negative = b < 0;
          let r_nonzero = r != 0;
          let signs_differ = r_negative != b_negative;
          let needs_adjustment = r_nonzero && signs_differ;
          if needs_adjustment {
              q - 1
          } else {
              q
          }
      };
−     let div = _cse_temp_1;
−     let _cse_temp_2 = a % b;
−     let r#mod = _cse_temp_2;
−     let _cse_temp_3 = a == b;
−     let eq = _cse_temp_3;
−     let _cse_temp_4 = a != b;
−     let ne = _cse_temp_4;
−     let _cse_temp_5 = a < b;
−     let lt = _cse_temp_5;
−     let _cse_temp_6 = a <= b;
−     let le = _cse_temp_6;
−     let _cse_temp_7 = a > b;
−     let gt = _cse_temp_7;
−     let _cse_temp_8 = a >= b;
−     let ge = _cse_temp_8;
−     let _cse_temp_9 = (a != 0) && (b != 0);
−     let and_op = _cse_temp_9;
−     let _cse_temp_10 = (a != 0) || (b != 0);
−     let or_op = _cse_temp_10;
+     let r#mod = a % b;
+     let eq = a == b;
+     let ne = a != b;
+     let lt = a < b;
+     let le = a <= b;
+     let gt = a > b;
+     let ge = a >= b;
+     let and_op = (a != 0) && (b != 0);
+     let or_op = (a != 0) || (b != 0);
      let not_op = !a;
−     return (((((eq) || (ne)) || (lt)) || (le)) || (gt)) || (ge);
+     return Ok((((((eq) || (ne)) || (lt)) || (le)) || (gt)) || (ge));
  }
────────────────────────────────────────────────────────────