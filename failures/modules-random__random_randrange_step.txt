  modules-random.toml :: random_randrange_step
    Output mismatch

────────────────────────────────────────────────────────────
  use rand as random;
− use rand::Rng;
− use rand::prelude::*;
− use rand::rngs::SmallRng;
− use rand::SeedableRng;
  pub fn get_random_stepped(start: i32, stop: i32, step: i32) -> i32 {
      return {
−         thread_local! {
−             static RNG: std::cell::RefCell<SmallRng> = std::cell::RefCell::new(SmallRng::from_os_rng());
−         }
          let start = start;
          let stop = stop;
          let step = step;
          let num_steps = ((stop - start) / step).max(0);
−         let offset = RNG.with(|rng| rng.borrow_mut().gen_range(0..num_steps));
+         let offset = DEPYLER_RNG.with(|rng| rng.borrow_mut().gen_range(0..num_steps));
          start + offset * step
−     }
+     };
+ }
────────────────────────────────────────────────────────────