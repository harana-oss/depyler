  cse-optimization.toml :: simple_and_with_comparisons
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn in_range(x: i64, low: i64, high: i64) -> bool {
−     if x >= low && x <= high {
+ pub fn in_range(x: i32, low: i32, high: i32) -> bool {
+     let _cse_temp_0 = (x >= low) && (x <= high);
+     if _cse_temp_0 {
          return true;
      }
      return false;
  }
────────────────────────────────────────────────────────────