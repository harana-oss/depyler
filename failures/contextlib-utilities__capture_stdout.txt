  contextlib-utilities.toml :: capture_stdout
    Output mismatch

────────────────────────────────────────────────────────────
− use std::io::Write;
− 
− pub fn test_capture_stdout() -> usize {
−     let mut buffer = Vec::new();
−     writeln!(&mut buffer, "line1").unwrap();
−     writeln!(&mut buffer, "line2").unwrap();
−     String::from_utf8(buffer).unwrap().lines().count()
+ pub fn test_capture_stdout() -> i32 {
+     let f = StringIO::new();
+     let _context = redirect_stdout(f);
+     log::info!("{}", "line1");
+     log::info!("{}", "line2");
+     return f
+         .getvalue()
+         .lines()
+         .map(|s| s.to_string())
+         .collect::<Vec<String>>()
+         .len() as i32 as i32;
  }
────────────────────────────────────────────────────────────