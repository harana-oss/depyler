  cse-optimization.toml :: method_call_list_index
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn find_twice(items: Vec<i32>, value: i32) -> i32 {
−     let _cse_temp_0 = items
+ pub fn find_twice(items: &Vec<i32>, value: i32) -> i32 {
+     let idx = items
          .iter()
−         .position(|x| *x == value)
+         .position(|x| x == &value)
          .map(|i| i as i32)
−         .unwrap_or(-1);
−     let idx = _cse_temp_0;
−     return idx + _cse_temp_0;
+         .expect("ValueError: value is not in list");
+     return idx
+         + items
+             .iter()
+             .position(|x| x == &value)
+             .map(|i| i as i32)
+             .expect("ValueError: value is not in list");
  }
────────────────────────────────────────────────────────────