  classes.toml :: property_with_class_constant
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Copy, Clone]
+ #[derive(Debug, Copy, Clone)]
  pub struct Circle {
      pub radius: f64,
  }
  impl Circle {
      pub const PI: f64 = 3.14159;
      pub fn new(radius: f64) -> Self {
          Self { radius }
      }
      pub fn circumference(&self) -> f64 {
          return 2 * Circle.PI * self.radius;
      }
− }
+ }
────────────────────────────────────────────────────────────