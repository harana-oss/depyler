  async-comprehensions.toml :: await_async_for_comp
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− async fn process(x: i32) -> i32 {
−     x * 2
+ use serde_json;
+ #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncRangeState {
+     state: usize, n: serde_json::Value
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_range(n: & serde_json::Value) -> impl Iterator<Item = i32>{
+     AsyncRangeState {
+     state: 0, n: n
  }
− 
− #[tokio::main]
− async let stream = stream::iter(0..5);
− let result: Vec<i32> = stream
−     .then(|x| async move { process(x).await })
−     .collect()
−     .await;
− log::info!("{:?}", result);
+ } impl Iterator for AsyncRangeState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _i in 0..self.n {
+     return Some(i);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn process(x: i32) -> i32 {
+     return x * 2;
+    
+ }
+ pub async fn test() -> Vec<serde_json::Value>{
+     let result = async_range(5).iter().cloned().map(| x | process(x).await).collect::<Vec<_>>();
+     return result;
+     }
────────────────────────────────────────────────────────────