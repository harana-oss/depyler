  asyncio-tasks.toml :: create_task
    Output mismatch

────────────────────────────────────────────────────────────
− use tokio::task;
− 
− async fn my_coro() -> i32 {
−     42
+ pub async fn my_coro() -> i32 {
+     return 42;
+    
  }
− 
  pub async fn test_create_task() -> i32 {
−     let task = task::spawn(my_coro());
−     task.await.unwrap()
− }
+     let task = asyncio.create_task(my_coro());
+     return task.await;
+     }
────────────────────────────────────────────────────────────