  cli-integration.toml :: marco_polo_annotated
    Output mismatch

────────────────────────────────────────────────────────────
− Error: Statement type not yet supported
+ use clap::Parser;
+ use rand as random;
+ use serde_json;
+ use std as sys;
+ #[derive(Debug, Clone)]
+ pub struct MarcoPoloGame {
+     pub difficulty: String,
+     pub verbose: bool,
+     pub score: i32,
+     pub attempts: i32,
+     pub difficulty_ranges: HashMap<String, serde_json::Value>,
+ }
+ impl MarcoPoloGame {
+     pub fn new(difficulty: String, verbose: bool) -> Self {
+         Self {
+             difficulty,
+             verbose,
+             score: 0,
+             attempts: 0,
+             difficulty_ranges: {
+                 let mut map = HashMap::new();
+                 map.insert("easy".to_string(), (1, 10));
+                 map.insert("medium".to_string(), (1, 50));
+                 map.insert("hard".to_string(), (1, 100));
+                 map
+             },
+         }
+     }
+     pub fn generate_number(&self) -> i32 {
+         let (mut min_val, mut max_val) = self.difficulty_ranges[self.difficulty as usize];
+         return random.randint(min_val, max_val);
+     }
+     pub fn get_hint(&self, guess: i32, target: i32) -> String {
+         if guess < target {
+             let mut distance = target - guess;
+             if distance > 20 {
+                 return "Marco!(Way too low)".to_string();
+             } else {
+                 if distance > 10 {
+                     return "Marco!(Too low)".to_string();
+                 } else {
+                     return "Marco!(A bit low)".to_string();
+                 }
+             }
+         } else {
+             let mut distance = guess - target;
+             if distance > 20 {
+                 return "Marco!(Way too high)".to_string();
+             } else {
+                 if distance > 10 {
+                     return "Marco!(Too high)".to_string();
+                 } else {
+                     return "Marco!(A bit high)".to_string();
+                 }
+             }
+         }
+     }
+     pub fn play_round(&self) -> bool {
+         let mut target = self.generate_number();
+         let (mut min_val, mut max_val) = self.difficulty_ranges[self.difficulty as usize];
+         if self.verbose {
+             log::info!("{}", format!("\n[DEBUG] Target number: {}", target));
+         }
+         log::info!(
+             "{}",
+             format!(
+                 "\nI'm thinking of a number between {} and {}...",
+                 min_val, max_val
+             )
+         );
+         log::info!(
+             "{}",
+             "When you guess, I'll say 'Marco!' and give you a hint.".to_string()
+         );
+         log::info!("{}", "Find the number to hear 'Polo!'".to_string());
+         let mut round_attempts = 0;
+         while true {
+             {
+                 let _result = (|| -> Result<(), Box<dyn std::error::Error>> {
+                     {
+                         let mut guess_str = input("\nYour guess: ".to_string());
+                         let mut guess = int(guess_str);
+                         if guess < min_val || guess > max_val {
+                             log::info!(
+                                 "{}",
+                                 format!("Please guess between {} and {}", min_val, max_val)
+                             );
+                             continue;
+                         }
+                         round_attempts += 1;
+                         self.attempts += 1;
+                         if guess == target {
+                             log::info!("{}", "🎉 Polo! You found it!".to_string());
+                             self.score += 1;
+                             if self.verbose {
+                                 log::info!(
+                                     "{}",
+                                     format!("[DEBUG] Attempts this round: {}", round_attempts)
+                                 );
+                             }
+                             return true;
+                         } else {
+                             let mut hint = self.get_hint(guess, target);
+                             log::info!("{}", hint);
+                         }
+                     }
+                     Ok(())
+                 })();
+                 if let Err(_e) = _result {
+                     {
+                         log::info!("{}", "Please enter a valid number!".to_string());
+                     }
+                 }
+             }
+         }
+     }
+     pub fn calculate_performance(&self) -> String {
+         if self.attempts == 0 {
+             return "No games played".to_string();
+         }
+         let mut avg_attempts = self.attempts / max(self.score, 1);
+         if avg_attempts <= 5 {
+             return "🏆 Expert".to_string();
+         } else {
+             if avg_attempts <= 7 {
+                 return "⭐ Good".to_string();
+             } else {
+                 if avg_attempts <= 10 {
+                     return "👍 Average".to_string();
+                 } else {
+                     return "🎯 Keep practicing".to_string();
+                 }
+             }
+         }
+     }
+ }
+ #[derive(Debug, clap::Parser)]
+ #[command(about = "Marco Polo CLI - A number guessing game")]
+ #[command(after_help = "Example: marco_polo --rounds 5 --difficulty medium")]
+ struct Args {
+     #[arg(short = 'r', long)]
+     #[arg(default_value = "3")]
+     #[doc = "Number of rounds to play(default: 3)"]
+     rounds: i32,
+     #[arg(short = 'd', long)]
+     #[arg(default_value = "medium")]
+     #[arg(value_parser = ["easy", "medium", "hard"])]
+     #[doc = "Game difficulty(default: medium)"]
+     difficulty: String,
+     #[arg(short = 'v', long)]
+     #[arg(action = clap::ArgAction::SetTrue)]
+     #[doc = "Enable verbose output"]
+     verbose: bool,
+     #[arg(long)]
+     version: Option<String>,
+ }
+ #[doc = "Parse command line arguments."]
+ pub fn parse_arguments() -> argparse::Namespace {
+     return ();
+ }
+ #[doc = "Print welcome banner."]
+ pub fn print_welcome() {
+     log::info!("{}", "=".repeat(50 as usize));
+     log::info!("{}", {
+         let s = "🎮 Welcome to Marco Polo CLI! 🎮";
+         let width = 50 as usize;
+         let fillchar = " ";
+         if s.len() >= width {
+             s.to_string()
+         } else {
+             let total_pad = width - s.len();
+             let left_pad = total_pad / 2;
+             let right_pad = total_pad - left_pad;
+             format!(
+                 "{}{}{}",
+                 fillchar.repeat(left_pad),
+                 s,
+                 fillchar.repeat(right_pad)
+             )
+         }
+     });
+     log::info!("{}", "=".repeat(50 as usize));
+ }
+ #[doc = "Print game statistics."]
+ pub fn print_statistics(game: &MarcoPoloGame, rounds: i32) {
+     log::info!("{}", format!("{}{}", "\n", "=".repeat(50 as usize)));
+     log::info!("{}", {
+         let s = "📊 Game Statistics 📊";
+         let width = 50 as usize;
+         let fillchar = " ";
+         if s.len() >= width {
+             s.to_string()
+         } else {
+             let total_pad = width - s.len();
+             let left_pad = total_pad / 2;
+             let right_pad = total_pad - left_pad;
+             format!(
+                 "{}{}{}",
+                 fillchar.repeat(left_pad),
+                 s,
+                 fillchar.repeat(right_pad)
+             )
+         }
+     });
+     log::info!("{}", "=".repeat(50 as usize));
+     log::info!("{}", format!("Rounds played: {}/{}", game.score, rounds));
+     log::info!("{}", format!("Total attempts: {}", game.attempts));
+     if game.score > 0 {
+         log::info!(
+             "{}",
+             format!(
+                 "Average attempts per round: {}",
+                 (game.attempts as f64) / (game.score as f64)
+             )
+         );
+     }
+     log::info!(
+         "{}",
+         format!("Performance: {}", game.calculate_performance())
+     );
+     log::info!("{}", "=".repeat(50 as usize));
+ }
+ #[doc = "Main entry point."]
+ pub fn main() -> i32 {
+     let args = parse_arguments();
+     print_welcome();
+     let game = MarcoPoloGame::new(args.difficulty, args.verbose);
+     log::info!(
+         "{}",
+         format!(
+             "\nStarting {} rounds on {} difficulty...",
+             args.rounds, args.difficulty
+         )
+     );
+     for round_num in 1..args.rounds + 1 {
+         log::info!("{}", format!("\n{}", "=".repeat(30 as usize)));
+         log::info!("{}", format!("Round {} of {}", round_num, args.rounds));
+         log::info!("{}", format!("{}", "=".repeat(30 as usize)));
+         if !game.play_round() {
+             log::info!("{}", "\nGame ended early.");
+             break;
+         }
+     }
+     print_statistics(&game, args.rounds);
+     log::info!("{}", "\nThanks for playing! 👋");
+     return 0;
+ }
────────────────────────────────────────────────────────────