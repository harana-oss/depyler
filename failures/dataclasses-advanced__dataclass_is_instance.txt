  dataclasses-advanced.toml :: dataclass_is_instance
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− struct Point {
−     x: i32,
−     y: i32,
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Point {
+     pub x: i32,
+     pub y: i32,
+ }
+ impl Point {
+     pub fn new(x: i32, y: i32) -> Self {
+         Self { x, y }
+     }
  }
− 
  pub fn test_dataclass_is_instance() -> bool {
−     let _p = Point { x: 1, y: 2 };
−     true
+     let p = Point::new(1, 2);
+     return (is_dataclass(Point)) && (is_dataclass(p));
  }
────────────────────────────────────────────────────────────