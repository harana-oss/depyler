  cargo-integration.toml :: feature_conditional
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn get_implementation(feature: &str) -> String {
+ pub fn get_implementation(feature: String) -> String {
      if feature == "fast" {
          return "optimized".to_string();
−     } else if feature == "debug" {
−         return "verbose".to_string();
+     } else {
+         if feature == "debug" {
+             return "verbose".to_string();
+         }
      }
      return "default".to_string();
  }
────────────────────────────────────────────────────────────