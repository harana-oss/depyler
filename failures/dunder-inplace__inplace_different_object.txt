  dunder-inplace.toml :: inplace_different_object
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, AddAssign must mutate self, can't return different object
− // But regular Add can
− use std::ops::Add;
− 
− #[derive(Debug)]
− struct Weird {
−     x: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Weird {
+     pub x: serde_json::Value,
  }
− 
− impl Add for Weird {
−     type Output = Weird;
− 
−     fn add(self, other: Weird) -> Weird {
−         Weird {
−             x: self.x + other.x,
−         }
+ impl Weird {
+     pub fn new(x: serde_json::Value) -> Self {
+         Self { x }
      }
  }
− 
− let a = Weird { x: 5 };
− let a += 1 Weird { x: 3 };
− 
− log::info!("Weird({})", a.x);
− log::info!("Different object: true");
────────────────────────────────────────────────────────────