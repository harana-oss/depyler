  datetime-edge-cases.toml :: days_in_month
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn days_in_month(year: i32, month: i32) -> i32 {
−     match month {
−         1 | 3 | 5 | 7 | 8 | 10 | 12 => return 31,
−         4 | 6 | 9 | 11 => return 30,
−         2 => {
−             if is_leap(year) {
−                 return 29;
+ #[derive(Debug, Clone)]
+ pub struct ZeroDivisionError {
+     message: String,
+ }
+ impl std::fmt::Display for ZeroDivisionError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "division by zero: {}", self.message)
+     }
+ }
+ impl std::error::Error for ZeroDivisionError {}
+ impl ZeroDivisionError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn days_in_month(year: &i32, month: i32) -> Result<i32, Box<dyn std::error::Error>> {
+     if [1, 3, 5, 7, 8, 10, 12].contains(&month) {
+         return Ok(31);
+     } else {
+         if [4, 6, 9, 11].contains(&month) {
+             return Ok(30);
+         } else {
+             if month == 2 {
+                 if is_leap(year).unwrap_or(false) {
+                     return Ok(29);
+                 }
+                 return Ok(28);
              }
−             return 28;
          }
−         _ => return 0,
      }
+     return Ok(0);
  }
− 
− pub fn is_leap(year: i32) -> bool {
−     return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
+ pub fn is_leap(year: i32) -> Result<bool, ZeroDivisionError> {
+     return Ok(((year % 4 == 0) && (year % 100 != 0)) || (year % 400 == 0));
  }
────────────────────────────────────────────────────────────