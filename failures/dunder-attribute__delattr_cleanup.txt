  dunder-attribute.toml :: delattr_cleanup
    Output mismatch

────────────────────────────────────────────────────────────
− struct Resource {
−     file: Option<String>,
+ #[derive(Debug, Clone)]
+ pub struct Resource {
+     pub file: String,
  }
− 
  impl Resource {
−     fn new() -> Self {
−         Resource {
−             file: Some("opened".to_string()),
+     pub fn new() -> Self {
+         Self {
+             file: "opened".to_string(),
          }
      }
− 
−     fn delete_file(&mut self) {
−         if self.file.is_some() {
−             log::info!("Closing file");
−             self.file = None;
−         }
−     }
  }
− 
− let mut obj = Resource::new();
− obj.delete_file();
────────────────────────────────────────────────────────────