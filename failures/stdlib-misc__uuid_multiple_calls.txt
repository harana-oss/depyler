  stdlib-misc.toml :: uuid_multiple_calls
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn generate_two_uuids() -> (String, String) {
+ pub fn generate_two_uuids() -> () {
      let id1 = {
          use uuid::Uuid;
          Uuid::new_v4().to_string()
      };
      let id2 = {
          use uuid::Uuid;
          Uuid::new_v4().to_string()
      };
−     return (id1.to_string(), id2.to_string());
+     return ((id1).to_string(), (id2).to_string());
  }
────────────────────────────────────────────────────────────