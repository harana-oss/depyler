  chainmap.toml :: chainmap_parents
    Output mismatch

────────────────────────────────────────────────────────────
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_parents() -> usize {
−     let mut dict1 = HashMap::new();
−     dict1.insert("a".to_string(), 1);
− 
−     let mut dict2 = HashMap::new();
−     dict2.insert("b".to_string(), 2);
− 
−     let mut dict3 = HashMap::new();
−     dict3.insert("c".to_string(), 3);
− 
−     let maps = vec![dict1, dict2, dict3];
−     let parents = &maps[1..];
− 
−     return parents.len();
+ pub fn test_chainmap_parents() -> i32 {
+     let dict1 = {
+         let mut map = HashMap::new();
+         map.insert("a".to_string(), 1);
+         map
+     };
+     let dict2 = {
+         let mut map = HashMap::new();
+         map.insert("b".to_string(), 2);
+         map
+     };
+     let dict3 = {
+         let mut map = HashMap::new();
+         map.insert("c".to_string(), 3);
+         map
+     };
+     let c = ChainMap::new(dict1, dict2, dict3);
+     let parents = c.parents;
+     return parents.maps.len() as i32 as i32;
  }
────────────────────────────────────────────────────────────