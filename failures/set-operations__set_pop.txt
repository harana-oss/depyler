  set-operations.toml :: set_pop
    Output mismatch

────────────────────────────────────────────────────────────
− pub const elem: i32 = s
−     .clone()
−     .iter()
−     .next()
−     .cloned()
−     .map(|x| {
−         s.clone().remove(&x);
−         x
−     })
−     .expect("pop from empty set");
− pub const result: (i32, i32) = (elem, s.len() as i32);
− lazy_static! {
−     pub static ref s: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(1);
−         set
−     };
− }
− use lazy_static::lazy_static;
− use serde_json;
− use std::collections::HashSet;
+ 
────────────────────────────────────────────────────────────