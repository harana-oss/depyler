  multiple-inheritance.toml :: mi_isinstance
    Output mismatch

────────────────────────────────────────────────────────────
− // isinstance() checks all bases
−     // Rust uses traits for type checking
−     trait A {}
−     trait B {}
− 
−     struct C;
−     impl A for C {}
−     impl B for C {}
− 
−     pub fn test_mi_isinstance() -> bool {
−         let c = C;
−         // Type checking at compile time
−         let _a: &dyn A = &c;
−         let _b: &dyn B = &c;
−         true
+ #[derive(Debug, Copy, Clone)]
+ pub struct A {}
+ impl A {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct B {}
+ impl B {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct C {}
+ impl C {
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = test_mi_isinstance();
+ }
────────────────────────────────────────────────────────────