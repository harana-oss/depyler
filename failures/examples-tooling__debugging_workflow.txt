  examples-tooling.toml :: debugging_workflow
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  #[derive(Debug, Clone)]
  pub struct IndexError {
      message: String,
  }
  impl std::fmt::Display for IndexError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "index out of range: {}", self.message)
      }
  }
  impl std::error::Error for IndexError {}
  impl IndexError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  #[doc = "Calculate fibonacci number recursively"]
  pub fn fibonacci(n: i32) -> i32 {
      if n <= 1 {
          return n;
      }
−     fibonacci(n - 1) + fibonacci(n - 2)
+     return fibonacci(n - 1) + fibonacci(n - 2);
  }
  #[doc = "Calculate factorial iteratively"]
  pub fn factorial(n: i32) -> i32 {
      let mut result = 1;
      for i in 2..n + 1 {
          result *= i;
      }
−     result
+     return result;
  }
  #[doc = "Find maximum in a list"]
− pub fn find_max(numbers: &Vec<i32>) -> Result<i32, IndexError> {
+ pub fn find_max(numbers: &Vec<serde_json::Value>) -> Result<i32, IndexError> {
      if numbers.is_empty() {
          return Ok(0);
      }
−     let mut max_val = numbers.get(0usize).cloned().unwrap_or_default();
+     let mut max_val = numbers.get(0usize).cloned().unwrap();
      for num in numbers.iter().cloned() {
          if num > max_val {
              max_val = num;
          }
      }
−     Ok(max_val)
+     return Ok(max_val);
  }
  #[doc = "Main function demonstrating various algorithms"]
  pub fn main() {
−     log::info!("{}", format!("Fibonacci(10) = {:?}", fibonacci(10)));
−     log::info!("{}", format!("Factorial(5) = {:?}", factorial(5)));
+     log::info!("{}", format!("Fibonacci(10) = {}", fibonacci(10)));
+     log::info!("{}", format!("Factorial(5) = {}", factorial(5)));
      let numbers = vec![3, 7, 2, 9, 1, 5];
      log::info!(
          "{}",
−         format!("Max of {:?} = {:?}", numbers, find_max(&numbers))
+         format!("Max of {:?} = {}", numbers, find_max(&numbers))
      );
  }
────────────────────────────────────────────────────────────