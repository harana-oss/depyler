  enum-methods.toml :: enum_custom_bool
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Copy, Clone)]
− enum Toggle {
−     Off = 0,
−     On = 1,
− }
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct Toggle {}
  impl Toggle {
−     fn to_bool(&self) -> bool {
−         *self as i32 != 0
+     pub const OFF: i32 = 0;
+     pub const ON: i32 = 1;
+     pub fn new() -> Self {
+         Self {}
      }
  }
− 
− if Toggle::On.to_bool() {
−     log::info!("Toggle is ON");
− }
− 
− if !Toggle::Off.to_bool() {
−     log::info!("Toggle is OFF");
− }
────────────────────────────────────────────────────────────