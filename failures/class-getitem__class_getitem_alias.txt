  class-getitem.toml :: class_getitem_alias
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, type aliases are defined with `type`
− struct Box<T> {
−     value: T,
+ #[derive(Debug, Clone)]
+ pub struct Box {
+     pub value: T,
  }
− 
− impl<T> Box<T> {
−     fn new(value: T) -> Self {
−         Box { value }
+ impl Box {
+     pub fn new(value: T) -> Self {
+         Self { value }
      }
  }
− 
− type IntBox = Box<i32>;
− type StrBox = Box<String>;
− 
− let b1: IntBox = Box::new(42);
− let b2: StrBox = Box::new("hello".to_string());
− log::info!("{}", b1.value);
− log::info!("{}", b2.value);
────────────────────────────────────────────────────────────