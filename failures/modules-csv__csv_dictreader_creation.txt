  modules-csv.toml :: csv_dictreader_creation
    Output mismatch

────────────────────────────────────────────────────────────
  use csv;
  use serde_json;
− pub fn read_csv(filepath: String) -> Vec<i32> {
+ pub fn read_csv(filepath: String) -> Result<Vec<serde_json::Value>, std::io::Error> {
      let mut f = std::fs::File::open(filepath)?;
      let reader = csv::ReaderBuilder::new().has_headers(true).from_reader(f);
−     return reader
+     return Ok(reader
          .deserialize::<HashMap<String, String>>()
−         .collect::<Vec<_>>();
+         .collect::<Vec<_>>());
  }
────────────────────────────────────────────────────────────