  dataclasses-frozen.toml :: frozen_classvar
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: i32 = Point::count;
− #[derive(Debug, Copy, Clone]
+ pub const result: serde_json::Value = Point.count;
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
  pub struct Point {
      pub x: i32,
      pub y: i32,
  }
  impl Point {
−     pub const count: i32 = 0;
+     pub const count: ClassVar<i32> = 0;
      pub fn new(x: i32, y: i32) -> Self {
          Self { x, y }
      }
− }
+ }
────────────────────────────────────────────────────────────