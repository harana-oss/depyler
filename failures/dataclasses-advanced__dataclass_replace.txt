  dataclasses-advanced.toml :: dataclass_replace
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone)]
− struct Point {
−     x: i32,
−     y: i32,
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Point {
+     pub x: i32,
+     pub y: i32,
  }
− 
  impl Point {
−     fn replace_x(&self, x: i32) -> Self {
−         Self { x, y: self.y }
+     pub fn new(x: i32, y: i32) -> Self {
+         Self { x, y }
      }
  }
− 
  pub fn test_dataclass_replace() -> i32 {
−     let p1 = Point { x: 1, y: 2 };
−     let p2 = p1.replace_x(10);
−     p2.x
+     let p1 = Point::new(1, 2);
+     let p2 = replace(p1, 10);
+     return p2.x;
  }
────────────────────────────────────────────────────────────