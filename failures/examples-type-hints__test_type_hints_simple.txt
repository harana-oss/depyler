  examples-type-hints.toml :: test_type_hints_simple
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  #[derive(Debug, Clone)]
  pub struct ZeroDivisionError {
      message: String,
  }
  impl std::fmt::Display for ZeroDivisionError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "division by zero: {}", self.message)
      }
  }
  impl std::error::Error for ZeroDivisionError {}
  impl ZeroDivisionError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  #[doc = "Add two numbers - should infer numeric types."]
− pub fn add_numbers<'a, 'b>(a: &'a serde_json::Value, b: &'b serde_json::Value) -> i32 {
−     a + b
+ pub fn add_numbers(a: i32, b: i32) {
+     return a + b;
  }
  #[doc = "Process text - should infer string type."]
− pub fn process_text(text: &str) -> String {
+ pub fn process_text(text: String) -> String {
      let result = text.to_uppercase();
−     result
+     return result;
  }
  #[doc = "Calculate average - should infer list of numbers."]
− pub fn calculate_average(numbers: &serde_json::Value) -> Result<i32, ZeroDivisionError> {
+ pub fn calculate_average(numbers: &Vec<i32>) -> Result<i32, ZeroDivisionError> {
      let mut total = 0;
      let mut count = 0;
      for num in numbers.iter().cloned() {
          total += num;
          count += 1;
      }
−         if count > 0 {
−         return Ok(total / count);
+     if count > 0 {
+         return Ok((total as f64) / (count as f64));
      }
−     Ok(0)
+     return Ok(0);
  }
  #[doc = "Check string properties."]
− pub fn string_checker(s: &str) -> bool {
+ pub fn string_checker(s: String) -> bool {
      if s.starts_with("hello") {
          return true;
      }
−     false
+     return false;
  }
  #[doc = "Perform list operations."]
  pub fn list_operations(items: &mut serde_json::Value) -> i32 {
      items.push(42);
−     items.len() as i32
− };
+     return items.len() as i32;
+ }
────────────────────────────────────────────────────────────