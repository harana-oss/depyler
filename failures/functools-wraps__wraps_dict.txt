  functools-wraps.toml :: wraps_dict
    Output mismatch

────────────────────────────────────────────────────────────
− // Custom attributes copied
−     // Rust uses attributes for metadata
−     #[allow(dead_code)]
−     fn func() {}
− 
−     let result = true;
+ use serde_json;
+ use std;
+ pub fn my_decorator(f: &serde_json::Value) {
+     fn wrapper() -> () {
+         return f(args);
+     }
+     return wrapper;
+ }
+ pub fn func() {}
────────────────────────────────────────────────────────────