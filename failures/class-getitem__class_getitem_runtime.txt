  class-getitem.toml :: class_getitem_runtime
    Output mismatch

────────────────────────────────────────────────────────────
− struct Container<T> {
−     value: T,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Container {
+     pub value: T,
+     pub type_param: serde_json::Value,
  }
− 
− impl<T> Container<T> {
−     fn new(value: T) -> Self {
−         Container { value }
+ impl Container {
+     pub fn new(value: T) -> Self {
+         Self {
+             value,
+             type_param: T,
+         }
      }
  }
− 
− let c = Container::new(42);
− log::info!("{}", c.value);
− log::info!("i32"); // Type is known at compile time
────────────────────────────────────────────────────────────