  contextlib-utilities.toml :: suppress_multiple
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn test_suppress_multiple() -> i32 {
−     let mut count = 0;
−     let _: Result<(), String> = (|| {
−         count += 1;
−         Err("ignored".to_string())
−     })();
−     let _: Result<(), String> = (|| {
−         count += 1;
−         Err("also ignored".to_string())
−     })();
−     count
+ #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn test_suppress_multiple() -> Result<i32, Box<dyn std::error::Error>> {
+     let count = 0;
+     let _context = suppress(ValueError, TypeError);
+     count += 1;
+     return Err(Box::new(ValueError::new("ignored")));
+     let _context = suppress(ValueError, TypeError);
+     count += 1;
+     return Err(Box::new(TypeError::new("also ignored")));
+     return Ok(count);
  }
────────────────────────────────────────────────────────────