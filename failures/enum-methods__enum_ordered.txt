  enum-methods.toml :: enum_ordered
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, we derive Ord and PartialOrd
− #[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
− enum Priority {
−     Low = 1,
−     Medium = 2,
−     High = 3,
+ #[derive(Debug, Copy, Clone)]
+ pub struct OrderedEnum {}
+ impl OrderedEnum {
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− 
− log::info!("{}", Priority::Low < Priority::High);
− let mut priorities = vec![Priority::High, Priority::Low, Priority::Medium];
− priorities.sort();
− log::info!("{:?}", priorities);
+ #[derive(Debug, Copy, Clone)]
+ pub struct Priority {}
+ impl Priority {
+     pub const LOW: i32 = 1;
+     pub const MEDIUM: i32 = 2;
+     pub const HIGH: i32 = 3;
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
────────────────────────────────────────────────────────────