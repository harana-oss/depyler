  enum-advanced.toml :: enum_type_hint
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− enum Color {
−     Red = 1,
−     Green = 2,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const GREEN: i32 = 2;
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− 
− fn f(color: Color) -> String {
−     format!("{:?}", color)
+ pub fn f(color: &Color) -> String {
+     return color.name;
  }
− 
  pub fn test_enum_type_hint() -> String {
−     f(Color::Red)
+     return f(&Color::RED);
  }
────────────────────────────────────────────────────────────