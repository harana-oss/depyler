  async-generators.toml :: async_gen_yield_value
    Output mismatch

────────────────────────────────────────────────────────────
− use async_stream::stream;
− use tokio_stream::StreamExt;
− 
− async fn test_async_gen_yield_value() -> i32 {
−     let mut g = stream! {
−         yield 42;
−     };
−     g.next().await.unwrap()
− }
+ #[doc = " Generator state struct"] #[derive(Debug)] struct GenState {
+     state: usize ,
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn gen() -> impl Iterator<Item = i32>{
+     GenState {
+     state: 0 ,
+ }
+ } impl Iterator for GenState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1usize;
+     return Some(42);
+    
+ }
+ 1usize =>{
+     self.state = 1usize + 1;
+     None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test_async_gen_yield_value() -> i32 {
+     let g = gen();
+     let value = anext(g).await;
+     return value;
+     }
────────────────────────────────────────────────────────────