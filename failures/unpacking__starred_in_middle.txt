  unpacking.toml :: starred_in_middle
    Output mismatch

────────────────────────────────────────────────────────────
− let _data = vec![1, 2, 3, 4, 5];
− let first = _data[0];
− let middle: Vec<_> = _data[1usize.._data.len() - 1usize].to_vec();
− let last = _data[_data.len() - 0usize - 1];
− log::info!(
−     "{}",
−     format!("first={}, middle={}, last={}", first, middle, last)
− );
+ 
────────────────────────────────────────────────────────────