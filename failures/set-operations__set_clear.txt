  set-operations.toml :: set_clear
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: i32 = s.len() as i32;
− lazy_static! {
−     pub static ref s: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(1);
−         set.insert(2);
−         set.insert(3);
−         set
−     };
− }
− use lazy_static::lazy_static;
− use serde_json;
− use std::collections::HashSet;
+ 
────────────────────────────────────────────────────────────