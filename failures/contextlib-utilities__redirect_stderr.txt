  contextlib-utilities.toml :: redirect_stderr
    Output mismatch

────────────────────────────────────────────────────────────
− use std::io::Write;
− 
+ use std as sys;
  pub fn test_redirect_stderr() -> String {
−     let mut buffer = Vec::new();
−     writeln!(&mut buffer, "error").unwrap();
−     String::from_utf8(buffer).unwrap().trim().to_string()
+     let f = StringIO::new();
+     let _context = redirect_stderr(f);
+     log::info!("{} {}", "error", std::io::stderr());
+     return f.getvalue().trim().to_string();
  }
────────────────────────────────────────────────────────────