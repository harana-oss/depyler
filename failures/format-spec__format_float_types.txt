  format-spec.toml :: format_float_types
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn format_float_types() -> (String, String, String) {
+ pub fn format_float_types() -> () {
      let n = 3.14159;
−     return (format!("{}", n), format!("{:e}", n), format!("{:E}", n));
+     return (
+         format(n, "f".to_string()),
+         format(n, "e".to_string()),
+         format(n, "E".to_string()),
+     );
  }
────────────────────────────────────────────────────────────