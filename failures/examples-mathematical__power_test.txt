  examples-mathematical.toml :: power_test
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ZeroDivisionError {
      message: String,
  }
  impl std::fmt::Display for ZeroDivisionError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "division by zero: {}", self.message)
      }
  }
  impl std::error::Error for ZeroDivisionError {}
  impl ZeroDivisionError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  pub fn test_integer_power() -> (i32, i32, i32, i32) {
−     let _cse_temp_0 = (2 as i32)
−         .checked_pow(3 as u32)
−         .expect("Power operation overflowed");
−     let a = _cse_temp_0;
−     let _cse_temp_1 = (10 as i32)
−         .checked_pow(2 as u32)
−         .expect("Power operation overflowed");
−     let b = _cse_temp_1;
−     let _cse_temp_2 = (5 as i32)
−         .checked_pow(0 as u32)
−         .expect("Power operation overflowed");
−     let c = _cse_temp_2;
+     let a = 2_i32.pow(3 as u32);
+     let b = 10_i32.pow(2 as u32);
+     let c = 5_i32.pow(0 as u32);
      let base = 3;
      let exp = 4;
−     let _cse_temp_3 = {
−         if exp >= 0 && (exp as i64) <= (u32::MAX as i64) {
−             (base as i32)
−                 .checked_pow(exp as u32)
−                 .expect("Power operation overflowed")
−         } else {
−             (base as f64).powf(exp as f64) as i32
−         }
−     };
−     let d = _cse_temp_3;
−     (a, b, c, d)
+     let d = base
+         .checked_pow(exp as u32)
+         .expect("Power operation overflowed");
+     return (a, b, c, d);
  }
  pub fn test_float_power() -> Result<(f64, f64, f64, i32), ZeroDivisionError> {
−     let _cse_temp_0 = (2.5 as f64).powf(2 as f64);
−     let a = _cse_temp_0;
−     let _cse_temp_1 = (10.0 as f64).powf(3 as f64);
−     let b = _cse_temp_1;
−     let _cse_temp_2 = (4 as f64).powf(0.5 as f64);
−     let c = _cse_temp_2;
−     let _cse_temp_3 = (8 as i32)
−         .checked_pow(0 as u32)
−         .expect("Power operation overflowed");
−     let d = _cse_temp_3;
−     Ok((a, b, c, d))
+     let a = (2.5 as f64).powf(2 as f64);
+     let b = (10.0 as f64).powf(3 as f64);
+     let c = (4 as f64).powf(0.5 as f64);
+     let d = 8_i32.pow(0 as u32);
+     return Ok((a, b, c, d));
  }
  pub fn test_negative_exponent() -> (i32, i32, i32) {
−     let _cse_temp_0 = (2 as f64).powf(-1 as f64);
−     let a = _cse_temp_0;
−     let _cse_temp_1 = (10 as f64).powf(-2 as f64);
−     let b = _cse_temp_1;
−     let _cse_temp_2 = (5 as f64).powf(-3 as f64);
−     let c = _cse_temp_2;
−     (a, b, c)
+     let a = (2 as f64).powf(-1 as f64);
+     let b = (10 as f64).powf(-2 as f64);
+     let c = (5 as f64).powf(-3 as f64);
+     return (a, b, c);
  }
  pub fn test_large_powers() -> (i32, i32, i32) {
−     let _cse_temp_0 = (2 as i32)
−         .checked_pow(10 as u32)
−         .expect("Power operation overflowed");
−     let a = _cse_temp_0;
−     let _cse_temp_1 = (2 as i32)
−         .checked_pow(20 as u32)
−         .expect("Power operation overflowed");
−     let b = _cse_temp_1;
−     let _cse_temp_2 = (10 as i32)
−         .checked_pow(6 as u32)
−         .expect("Power operation overflowed");
−     let c = _cse_temp_2;
−     (a, b, c)
+     let a = 2_i32.pow(10 as u32);
+     let b = 2_i32.pow(20 as u32);
+     let c = 10_i32.pow(6 as u32);
+     return (a, b, c);
  }
  pub fn test_mixed_operations() -> (i32, i32, i32, i32) {
−     let _cse_temp_0 = (3 as i32)
−         .checked_pow(2 as u32)
−         .expect("Power operation overflowed");
−     let a = 2 + _cse_temp_0;
−     let _cse_temp_1 = (5 as i32)
−         .checked_pow(2 as u32)
−         .expect("Power operation overflowed");
−     let b = _cse_temp_1;
−     let _cse_temp_2 = (2 as i32)
−         .checked_pow(3 as u32)
−         .expect("Power operation overflowed");
−     let _cse_temp_3 = _cse_temp_2 * 4;
−     let c = _cse_temp_3;
−     let _cse_temp_4 = (2 as i32)
−         .checked_pow(6 as u32)
−         .expect("Power operation overflowed");
−     let d = _cse_temp_4;
−     (a, b, c, d)
+     let _cse_temp_0 = 2 + 3_i32.pow(2 as u32);
+     let a = _cse_temp_0;
+     let b = 5_i32.pow(2 as u32);
+     let _cse_temp_1 = 2_i32.pow(3 as u32) * 4;
+     let c = _cse_temp_1;
+     let d = 2_i32.pow(6 as u32);
+     return (a, b, c, d);
  }
  #[doc = "Test power with function parameters"]
  pub fn compute_power(base: i32, exp: i32) -> i32 {
−     {
−         if exp >= 0 && (exp as i64) <= (u32::MAX as i64) {
−             (base as i32)
−                 .checked_pow(exp as u32)
−                 .expect("Power operation overflowed")
−         } else {
−             (base as f64).powf(exp as f64) as i32
−         }
−     }
+     return base
+         .checked_pow(exp as u32)
+         .expect("Power operation overflowed");
  }
────────────────────────────────────────────────────────────