  chained-comparisons.toml :: chained_with_calls
    Output mismatch

────────────────────────────────────────────────────────────
− // 0 < len(s) < 100 (middle expression evaluated once)
−     let s = "hello";
−     let len_s = s.len();
−     let result = 0 < len_s && len_s < 100;
+ pub const s: &str = "hello";
+ pub const result: serde_json::Value = (0 < s.len() as i32) && ((s.len() as i32) < 100);
+ use serde_json;
────────────────────────────────────────────────────────────