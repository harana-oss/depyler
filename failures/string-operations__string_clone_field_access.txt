  string-operations.toml :: string_clone_field_access
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Container {
      pub text: String,
  }
  impl Container {
      pub fn new(text: String) -> Self {
          Self { text }
      }
  }
  pub fn get_text(c: &Container) -> String {
−     return c.text.clone();
+     return c.text;
  }
────────────────────────────────────────────────────────────