  stdlib-datetime.toml :: naive_datetime_comparison
    Output mismatch

────────────────────────────────────────────────────────────
− let dt1 = chrono::NaiveDate::from_ymd_opt(2024, 1, 1)
−         .unwrap()
−         .and_hms_opt(12, 0, 0)
−         .unwrap();
−     let dt2 = chrono::NaiveDate::from_ymd_opt(2024, 1, 1)
−         .unwrap()
−         .and_hms_opt(13, 0, 0)
−         .unwrap();
−     assert!(dt1 < dt2);
−     assert!(dt2 > dt1);
+ use chrono::DateTime;
────────────────────────────────────────────────────────────