  type-guards.toml :: optional_narrowing_method_chain
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn safe_len(s: &Option<String>) -> i32 {
      if s.is_some() {
−         return s.as_ref().unwrap().len() as i32;
+         return s.as_ref().unwrap().len() as i32 as i32;
      }
      return 0;
  }
────────────────────────────────────────────────────────────