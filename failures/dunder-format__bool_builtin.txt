  dunder-format.toml :: bool_builtin
    Output mismatch

────────────────────────────────────────────────────────────
− struct Status {
−     is_active: bool,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Status {
+     pub is_active: serde_json::Value,
  }
− 
  impl Status {
−     fn new(is_active: bool) -> Self {
−         Status { is_active }
−     }
− 
−     fn to_bool(&self) -> bool {
−         self.is_active
+     pub fn new(is_active: serde_json::Value) -> Self {
+         Self { is_active }
      }
  }
− 
− let active = Status::new(true);
− let inactive = Status::new(false);
− log::info!("{}", active.to_bool());
− log::info!("{}", inactive.to_bool());
────────────────────────────────────────────────────────────