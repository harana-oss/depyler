  parser-pattern-matching.toml :: pattern_dict
    Output mismatch

────────────────────────────────────────────────────────────
+ use serde_json;
  use std::collections::HashMap;
− 
− fn check_dict(d: HashMap<String, i64>) -> String {
−     match (d.get("x"), d.get("y")) {
−         (Some(x), Some(y)) => format!("point: ({}, {})", x, y),
−         (Some(x), None) => format!("x only: {}", x),
−         _ => "no x".to_string(),
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
      }
  }
− 
− fn main() {
−     let mut d = HashMap::new();
−     d.insert("x".to_string(), 1);
−     d.insert("y".to_string(), 2);
−     let result = check_dict(d);
−     println!("{}", result);
+ pub fn check_dict(d: &HashMap<serde_json::Value, serde_json::Value>) -> String {
+     let _cse_temp_0 = (d.get(&"x").is_some()) && (d.get(&"y").is_some());
+     if _cse_temp_0 {
+         return format!(
+             "point:({}, {})",
+             d.get("x").cloned().unwrap(),
+             d.get("y").cloned().unwrap()
+         );
+     } else {
+         if d.get(&"x").is_some() {
+             return format!("x only: {}", d.get("x").cloned().unwrap());
+         } else {
+             return "no x".to_string();
+         }
+     }
  }
────────────────────────────────────────────────────────────