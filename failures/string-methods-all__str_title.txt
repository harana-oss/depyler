  string-methods-all.toml :: str_title
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: String = "hello world"
−     .split_whitespace()
−     .map(|word| {
−         let mut chars = word.chars();
−         match chars.next() {
−             None => String::new(),
−             Some(first) => first.to_uppercase().chain(chars).collect::<String>(),
−         }
−     })
−     .collect::<Vec<_>>()
−     .join(" ");
+ 
────────────────────────────────────────────────────────────