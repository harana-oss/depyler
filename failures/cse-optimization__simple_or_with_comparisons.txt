  cse-optimization.toml :: simple_or_with_comparisons
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn is_boundary(x: i64, low: i64, high: i64) -> bool {
−     if x == low || x == high {
+ pub fn is_boundary(x: i32, low: i32, high: i32) -> bool {
+     let _cse_temp_0 = (x == low) || (x == high);
+     if _cse_temp_0 {
          return true;
      }
      return false;
  }
────────────────────────────────────────────────────────────