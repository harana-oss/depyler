  enum-basic.toml :: enum_string
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust enums can't directly hold string literals as discriminants
−     // Use associated constants or methods instead
−     #[derive(Debug, PartialEq)]
−     enum Status {
−         OK,
−         ERROR,
−     }
− 
−     impl Status {
−         fn value(&self) -> &str {
−             match self {
−                 Status::OK => "ok",
−                 Status::ERROR => "error",
−             }
−         }
+ pub const result: serde_json::Value = Status.OK.value == "ok";
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Status {}
+ impl Status {
+     pub const OK: i32 = "ok".to_string();
+     pub const ERROR: i32 = "error".to_string();
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = Status::OK.value() == "ok";
+ }
────────────────────────────────────────────────────────────