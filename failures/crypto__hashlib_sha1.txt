  crypto.toml :: hashlib_sha1
    Output mismatch

────────────────────────────────────────────────────────────
  use sha2 as hashlib;
  pub fn hash_sha1(data: &bytes) -> String {
      return {
          use sha1::Digest;
          let mut hasher = sha1::Sha1::new();
          hasher.update(data);
          hex::encode(hasher.finalize())
−     }
+     };
+ }
────────────────────────────────────────────────────────────