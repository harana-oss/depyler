  multi-module.toml :: module_function_chain
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn step_one(x: i32) -> i32 {
      return x + 1;
  }
− 
  pub fn step_two(x: i32) -> i32 {
      return x * 2;
  }
− 
  pub fn step_three(x: i32) -> i32 {
      return x - 1;
  }
− 
− pub fn pipeline(x: i32) -> i32 {
+ pub fn pipeline(x: &i32) -> i32 {
      return step_three(step_two(step_one(x)));
  }
────────────────────────────────────────────────────────────