  dataclasses-basic.toml :: dataclass_factory
    Output mismatch

────────────────────────────────────────────────────────────
− pub const c1: Container = Container::new();
− pub const c2: Container = Container::new();
− pub const result: (i32, i32) = (c1.items.len() as i32, c2.items.len() as i32);
+ use serde_json;
  #[derive(Debug, Clone, PartialEq, Default)]
− pub struct Container {}
+ pub struct Container {
+     pub items: Vec<serde_json::Value>,
+ }
  impl Container {
−     pub const items: Vec<i32> = field();
      pub fn new() -> Self {
−         Self {}
+         Self { items: Vec::new() }
      }
  }
────────────────────────────────────────────────────────────