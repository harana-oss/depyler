  chainmap.toml :: chainmap_items
    Output mismatch

────────────────────────────────────────────────────────────
+ use serde_json;
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_items() -> Vec<(String, i32)> {
−     let mut dict1 = HashMap::new();
−     dict1.insert("a".to_string(), 1);
− 
−     let mut dict2 = HashMap::new();
−     dict2.insert("b".to_string(), 2);
− 
−     let maps = vec![dict1, dict2];
− 
−     let mut items = Vec::new();
−     let mut seen_keys = std::collections::HashSet::new();
− 
−     for map in &maps {
−         for (key, value) in map {
−             if seen_keys.insert(key.clone()) {
−                 items.push((key.clone(), *value));
−             }
−         }
−     }
− 
−     return items;
+ pub fn test_chainmap_items() -> Vec<serde_json::Value> {
+     let dict1 = {
+         let mut map = HashMap::new();
+         map.insert("a".to_string(), 1);
+         map
+     };
+     let dict2 = {
+         let mut map = HashMap::new();
+         map.insert("b".to_string(), 2);
+         map
+     };
+     let c = ChainMap::new(dict1, dict2);
+     return c
+         .iter()
+         .map(|(k, v)| (k.clone(), v.clone()))
+         .collect::<Vec<_>>();
  }
────────────────────────────────────────────────────────────