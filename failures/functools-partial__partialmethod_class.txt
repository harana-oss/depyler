  functools-partial.toml :: partialmethod_class
    Output mismatch

────────────────────────────────────────────────────────────
− struct Greeter;
− 
+ use serde_json;
+ use std::partialmethod;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Greeter {}
  impl Greeter {
−     fn greet(&self, greeting: &str, name: &str) -> String {
−         format!("{}, {}!", greeting, name)
+     pub const say_hello: serde_json::Value = partialmethod(greet, "Hello".to_string());
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     fn say_hello(&self, name: &str) -> String {
−         self.greet("Hello", name)
+     pub fn greet(&self, greeting: String, name: String) {
+         return format!("{}, {}!", greeting, name);
      }
  }
− 
− let g = Greeter;
− log::info!("{}", g.say_hello("Alice"));
────────────────────────────────────────────────────────────