  metaclasses.toml :: attribute_injection
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, we use traits or derive macros for attribute injection
− use std::collections::HashMap;
− 
− struct MyClass {
−     x: i32,
−     y: i32,
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct InjectorMeta {}
+ impl InjectorMeta {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Clone)]
+ pub struct MyClass {
+     pub x: serde_json::Value,
+     pub y: serde_json::Value,
  }
− 
  impl MyClass {
−     const CLASS_NAME: &'static str = "MyClass";
− 
−     fn new(x: i32, y: i32) -> Self {
−         MyClass { x, y }
−     }
− 
−     fn to_dict(&self) -> HashMap<String, i32> {
−         let mut map = HashMap::new();
−         map.insert("x".to_string(), self.x);
−         map.insert("y".to_string(), self.y);
−         map
+     pub fn new(x: serde_json::Value, y: serde_json::Value) -> Self {
+         Self { x, y }
      }
  }
− 
− let obj = MyClass::new(10, 20);
− log::info!("{:?}", obj.to_dict());
− log::info!("{}", MyClass::CLASS_NAME);
────────────────────────────────────────────────────────────