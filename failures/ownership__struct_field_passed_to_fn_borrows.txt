  ownership.toml :: struct_field_passed_to_fn_borrows
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct State {
      pub field: String,
  }
  impl State {
      pub fn new(field: String) -> Self {
          Self { field }
      }
  }
  pub fn take_str(s: String) {}
  pub fn main() {
      let state = State::new("hello".to_string());
−     take_str(state.field.clone());
+     take_str(state.field);
  }
────────────────────────────────────────────────────────────