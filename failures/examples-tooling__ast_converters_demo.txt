  examples-tooling.toml :: ast_converters_demo
    Output mismatch

────────────────────────────────────────────────────────────
− Error: Unsupported type annotation
+ use serde_json;
+ use std::collections::HashMap;
+ use std::collections::HashSet;
+ use std::f64 as math;
+ #[derive(Debug, Clone)]
+ pub struct ZeroDivisionError {
+     message: String,
+ }
+ impl std::fmt::Display for ZeroDivisionError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "division by zero: {}", self.message)
+     }
+ }
+ impl std::error::Error for ZeroDivisionError {}
+ impl ZeroDivisionError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[derive(Debug, Clone)]
+ pub struct DemoClass {
+     pub value: serde_json::Value,
+     pub data: Vec<serde_json::Value>,
+ }
+ impl DemoClass {
+     pub fn new(value: serde_json::Value) -> Self {
+         Self {
+             value,
+             data: vec![],
+         }
+     }
+     pub fn method(&self) -> i32 {
+         return self.value * 2;
+     }
+     pub fn chain_example(&self) -> i32 {
+         return self.method() + 10;
+     }
+ }
+ #[doc = "Show various statement types."]
+ pub fn demonstrate_statements() -> Result<(), ZeroDivisionError> {
+     let mut x = 10;
+     let mut y = 20;
+     x += 5;
+     y *= 2;
+     x /= 3;
+     let count: i32 = 0;
+     let name: String = "Python".to_string();
+     let _cse_temp_0 = x + y * 2;
+     let result = _cse_temp_0 - 10;
+     if x > 0 {
+         log::info!("{}", "Positive");
+     } else {
+         if x < 0 {
+             log::info!("{}", "Negative");
+         } else {
+             log::info!("{}", "Zero");
+         }
+     }
+     if x > 0 {
+         if x > 10 {
+             log::info!("{}", "Greater than 10");
+         } else {
+             log::info!("{}", "Between 1 and 10");
+         }
+     }
+     let mut counter = 0;
+     while counter < 5 {
+         log::info!("{}", counter);
+         counter += 1;
+     }
+     for i in 0..10 {
+         if i == 5 {
+             continue;
+         }
+         if i == 8 {
+             break;
+         }
+         log::info!("{}", i);
+     }
+     for i in 0..3 {
+         log::info!("{}", i);
+     }
+     for i in 0..3 {
+         for j in 0..3 {
+             log::info!("{}", format!("({}, {})", i, j));
+         }
+     }
+     if x > 100 {
+         return Ok(x);
+     } else {
+         if x > 50 {
+             return Ok(x * 2);
+         } else {
+             return Ok(());
+         }
+     }
+     Ok(())
+ }
+ #[doc = "Show advanced statement types."]
+ pub fn demonstrate_advanced() -> Result<String, Box<dyn std::error::Error>> {
+     let mut f = std::fs::File::create("file.txt")?;
+     f.write("Hello, World!");
+     if false {
+         return Err(Box::new(ValueError::new("Something went wrong")));
+     }
+     let complex_expr = (0..10).filter(|&x| *x % 2 == 0).collect::<Vec<_>>()
+         + (0..5).map(|x| x * 2).collect::<Vec<_>>();
+     let nested = serde_json::json!({ "users": vec! [serde_json::json!({ "name": "Alice", "age": 30 }), serde_json::json!({ "name": "Bob", "age": 25 })], "count": 2, "active": true });
+     let result = "  Hello World  ".trim().to_lowercase().replace(" ", "_");
+     return Ok(result);
+ }
+ #[doc = "Show various comprehension types."]
+ pub fn demonstrate_comprehensions() -> Vec<(serde_json::Value, i32, i32)> {
+     let squares = (0..10).map(|x| x.pow(2 as u32)).collect::<Vec<_>>();
+     let evens = (0..20).filter(|&x| *x % 2 == 0).collect::<Vec<_>>();
+     let matrix = (0..3)
+         .map(|i| (0..3).map(|j| i + j).collect::<Vec<_>>())
+         .collect::<Vec<_>>();
+     let unique_squares = vec![-2, -1, 0, 1, 2]
+         .iter()
+         .cloned()
+         .map(|x| x.pow(2 as u32))
+         .collect::<HashSet<_>>();
+     let square_map = (0..5)
+         .map(|x| (x, x.pow(2 as u32)))
+         .collect::<HashMap<_, _>>();
+     let filtered = (0..100)
+         .filter(|&x| *x % 2 == 0 && *x % 3 == 0)
+         .collect::<Vec<_>>();
+     let transformed = (0..5)
+         .map(|x| (x, x.pow(2 as u32), x.pow(3 as u32)))
+         .collect::<Vec<_>>();
+     return transformed;
+ }
────────────────────────────────────────────────────────────