  optimizer-inlining.toml :: function_chain_no_inline
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn step1(x: i32) -> i32 {
      return x + 1;
  }
  pub fn step2(x: i32) -> i32 {
      return x * 2;
  }
  pub fn step3(x: i32) -> i32 {
      return x - 3;
  }
− pub fn chain(x: i32) -> i32 {
+ pub fn chain(x: &i32) -> i32 {
      let a = step1(x);
      let b = step2(a);
      let c = step3(b);
      return c;
  }
────────────────────────────────────────────────────────────