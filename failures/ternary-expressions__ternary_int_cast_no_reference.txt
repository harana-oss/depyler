  ternary-expressions.toml :: ternary_int_cast_no_reference
    Output mismatch

────────────────────────────────────────────────────────────
− struct Data {
−     a: f64,
−     b: f64,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Data {
+     pub a: f64,
+     pub b: f64,
  }
− 
  impl Data {
−     fn new() -> Self {
+     pub fn new() -> Self {
          Self { a: 3.14, b: 2.71 }
      }
  }
− 
− let data = Data::new();
− let cond = false;
− let result = (if cond { data.a } else { data.b }) as i32;
− log::info!("{}", result);
────────────────────────────────────────────────────────────