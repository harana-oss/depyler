  enum-basic.toml :: enum_method
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, PartialEq)]
−     enum Color {
−         RED = 1,
−         GREEN = 2,
+ pub const result: serde_json::Value = Color::RED.describe() == "Color: RED";
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const GREEN: i32 = 2;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     impl Color {
−         fn describe(&self) -> String {
−             format!("Color: {:?}", self)
−         }
+     pub fn describe(&self) {
+         return format!("Color: {}", self.name);
      }
− 
−     let result = Color::RED.describe() == "Color: RED";
+ }
────────────────────────────────────────────────────────────