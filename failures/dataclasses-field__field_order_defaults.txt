  dataclasses-field.toml :: field_order_defaults
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
−     struct Person {
−         name: String,
−         age: i32,
−         city: String,
−     }
− 
−     impl Person {
−         fn new(name: String) -> Self {
−             Person {
−                 name,
−                 age: 0,
−                 city: "Unknown".to_string(),
−             }
+ #[derive(Debug, Clone, PartialEq, Default)]
+ pub struct Person {
+     pub name: String,
+     pub age: i32,
+     pub city: String,
+ }
+ impl Person {
+     pub fn new(name: String) -> Self {
+         Self {
+             name,
+             age: 0,
+             city: Default::default(),
          }
      }
− 
−     let p = Person::new("Alice".to_string());
−     let result = p.name == "Alice" && p.age == 0 && p.city == "Unknown";
+ }
────────────────────────────────────────────────────────────