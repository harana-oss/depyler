  abc.toml :: abstractmethod_override
    Output mismatch

────────────────────────────────────────────────────────────
− trait Base {
−     fn compute(&self) -> i32;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Base {}
+ impl Base {
+     pub fn new() -> Self {
+         Self {}
+     }
+     pub fn compute(&self) -> i32 {
+         {}
+     }
  }
  #[derive(Debug, Copy, Clone)]
  pub struct Derived {}
  impl Derived {
      pub fn new() -> Self {
          Self {}
      }
−     pub fn compute(&self) -> i32 {
+ }
+ impl Base for Derived {
+     fn compute(&self) -> i32 {
          return 100;
      }
  }
  pub fn test_abstractmethod_override() -> i32 {
      let obj = Derived::new();
      return obj.compute();
  }
────────────────────────────────────────────────────────────