  modules-regex.toml :: re_search_groups
    Output mismatch

────────────────────────────────────────────────────────────
  use regex as re;
  pub fn extract_groups(text: String) -> Vec<String> {
      let m = regex::Regex::new("(\\d+)-(\\d+)").unwrap().find(text);
      if m.is_some() {
−         return vec![] as Vec<String>;
+         return m.as_ref().unwrap().groups();
      }
      return vec![];
  }
────────────────────────────────────────────────────────────