  parser-pattern-matching.toml :: pattern_or_basic
    Output mismatch

────────────────────────────────────────────────────────────
− fn check_vowel(c: &str) -> String {
−     match c {
−         "a" | "e" | "i" | "o" | "u" => "vowel".to_string(),
−         _ => "consonant".to_string(),
+ pub fn check_vowel(c: String) -> String {
+     let _cse_temp_0 = (c == "a") || (c == "e");
+     let _cse_temp_1 = (_cse_temp_0) || (c == "i");
+     let _cse_temp_2 = (_cse_temp_1) || (c == "o");
+     let _cse_temp_3 = (_cse_temp_2) || (c == "u");
+     if _cse_temp_3 {
+         return "vowel".to_string();
+     } else {
+         return "consonant".to_string();
      }
− }
− 
− fn main() {
−     let result = check_vowel("e");
−     println!("{}", result);
  }
────────────────────────────────────────────────────────────