  unpacking.toml :: double_starred_in_call
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn print_kwargs() {
      for (key, value) in kwargs
          .iter()
          .map(|(k, v)| (k.clone(), v.clone()))
          .collect::<Vec<_>>()
      {
          log::info!("{}", format!("{}={}", key, value));
      }
  }
− let config = {
−     let mut map = HashMap::new();
−     map.insert("x".to_string(), 10);
−     map.insert("y".to_string(), 20);
−     map
− };
− print_kwargs();
────────────────────────────────────────────────────────────