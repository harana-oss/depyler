  examples-stdlib.toml :: test_itertools
    Output mismatch

────────────────────────────────────────────────────────────
− Error: Unsupported type annotation
+ use itertools;
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[doc = "Chain two lists together"]
+ pub fn chain_lists(list1: &Vec<i32>, list2: &Vec<i32>) -> Vec<i32> {
+     return {
+         let a = list1;
+         let b = list2;
+         a.into_iter().chain(b.into_iter())
+     }
+     .into_iter()
+     .collect::<Vec<_>>();
+ }
+ #[doc = "Get all combinations of r items"]
+ pub fn get_combinations(items: &Vec<String>, r: i32) -> Vec<(String, serde_json::Value)> {
+     return {
+         use itertools::Itertools;
+         let items: Vec<_> = items.into_iter().collect();
+         items.into_iter().combinations(r as usize)
+     }
+     .into_iter()
+     .collect::<Vec<_>>();
+ }
+ #[doc = "Group consecutive numbers"]
+ pub fn group_consecutive(numbers: Vec<i32>) -> Vec<Vec<i32>> {
+     let mut groups = vec![];
+     for (_k, g) in {
+         use itertools::Itertools;
+         let items = numbers
+             .iter()
+             .enumerate()
+             .map(|(i, x)| (i as i32, x.clone()));
+         let key_fn = |x| x.get(0usize).cloned().unwrap() - x.get(1usize).cloned().unwrap();
+         items.into_iter().group_by(key_fn)
+     } {
+         groups.push(
+             g.iter()
+                 .cloned()
+                 .map(|x| x.get(1usize).cloned().unwrap())
+                 .collect::<Vec<_>>(),
+         );
+     }
+     return groups;
+ }
+ #[doc = "Take numbers while they are positive"]
+ pub fn take_while_positive(numbers: &Vec<i32>) -> Vec<i32> {
+     return {
+         let pred = |x| x > 0;
+         let items = numbers;
+         items.into_iter().take_while(pred)
+     }
+     .into_iter()
+     .collect::<Vec<_>>();
+ }
────────────────────────────────────────────────────────────