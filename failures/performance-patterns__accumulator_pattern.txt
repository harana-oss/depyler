  performance-patterns.toml :: accumulator_pattern
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn list_sum(items: Vec<i32>) -> i32 {
+ pub fn list_sum(items: &Vec<i32>) -> i32 {
      let mut acc = 0;
−     for item in items {
+     for item in items.iter().cloned() {
          acc += item;
      }
      return acc;
  }
────────────────────────────────────────────────────────────