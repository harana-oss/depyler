  iterators.toml :: for_enumerate_with_value_access
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn indexed_sum(items: Vec<i32>) -> i32 {
      let mut total = 0;
      for (i, x) in items.iter().enumerate().map(|(i, x)| (i as i32, x.clone())) {
+         let i = i as i32;
          total += i + x;
      }
      return total;
  }
────────────────────────────────────────────────────────────