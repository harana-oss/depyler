  io-streams.toml :: file_iteration
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn count_lines(filepath: String) -> i32 {
+ pub fn count_lines(filepath: String) -> Result<i32, std::io::Error> {
      let count = 0;
      let mut f = std::fs::File::open(filepath)?;
      for _line in std::io::BufReader::new(f)
          .lines()
          .map(|l| l.unwrap_or_default())
      {
          count += 1;
      }
−     return count;
+     return Ok(count);
  }
────────────────────────────────────────────────────────────