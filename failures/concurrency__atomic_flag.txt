  concurrency.toml :: atomic_flag
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Copy, Clone)]
  pub struct AtomicFlag {
      pub flag: bool,
  }
− 
  impl AtomicFlag {
      pub fn new() -> Self {
          Self { flag: false }
      }
− 
      pub fn test_and_set(&mut self) -> bool {
−         let old = self.flag;
+         let mut old = self.flag;
          self.flag = true;
          return old;
      }
− 
      pub fn clear(&mut self) {
          self.flag = false;
      }
  }
────────────────────────────────────────────────────────────