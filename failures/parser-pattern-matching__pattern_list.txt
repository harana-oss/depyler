  parser-pattern-matching.toml :: pattern_list
    Output mismatch

────────────────────────────────────────────────────────────
− fn check_list_len(items: Vec<i64>) -> String {
−     match items.len() {
−         0 => "empty".to_string(),
−         1 => "single".to_string(),
−         2 => "pair".to_string(),
−         _ => "many".to_string(),
+ use serde_json;
+ pub fn check_list_len(items: &Vec<serde_json::Value>) -> String {
+     let length = items.len() as i32;
+     if length == 0 {
+         return "empty".to_string();
+     } else {
+         if length == 1 {
+             return "single".to_string();
+         } else {
+             if length == 2 {
+                 return "pair".to_string();
+             } else {
+                 return "many".to_string();
+             }
+         }
      }
− }
− 
− fn main() {
−     let result = check_list_len(vec![1, 2]);
−     println!("{}", result);
  }
────────────────────────────────────────────────────────────