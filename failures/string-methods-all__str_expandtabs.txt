  string-methods-all.toml :: str_expandtabs
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: String = {
−     let s = "a\tb";
−     let tabsize = 4;
−     let mut result = String::new();
−     let mut col = 0;
−     for c in s.chars() {
−         if c == '\t' {
−             let spaces = tabsize - (col % tabsize);
−             result.push_str(&" ".repeat(spaces));
−             col += spaces;
−         } else {
−             result.push(c);
−             if c == '\n' || c == '\r' {
−                 col = 0;
−             } else {
−                 col += 1;
−             }
−         }
−     }
−     result
− };
+ 
────────────────────────────────────────────────────────────