  deque.toml :: deque_extend
    Output mismatch

────────────────────────────────────────────────────────────
− // d.extend([1, 2, 3])
−     use std::collections::VecDeque;
− 
−     let mut d = VecDeque::from(vec![0]);
−     d.extend(vec![1, 2, 3]);
−     let result = d.iter().cloned().collect::<Vec<_>>() == vec![0, 1, 2, 3];
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────