  functions.toml :: email_validation_function
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct IndexError {
      message: String,
  }
  impl std::fmt::Display for IndexError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "index out of range: {}", self.message)
      }
  }
  impl std::error::Error for IndexError {}
  impl IndexError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn is_valid_email(email: String) -> bool {
+ pub fn is_valid_email(email: String) -> Result<bool, IndexError> {
      if !email.contains(&"@") {
−         return false;
+         return Ok(false);
      }
      let parts = email
          .split("@")
          .map(|s| s.to_string())
          .collect::<Vec<String>>();
−     let _cse_temp_1 = parts.len() as i32;
−     if _cse_temp_1 != 2 {
−         return false;
+     if parts.len() as i32 != 2 {
+         return Ok(false);
      }
−     let local = parts.clone().get(0usize).cloned().unwrap();
+     let local = parts.get(0usize).cloned().unwrap();
      let domain = parts.get(1usize).cloned().unwrap();
−     return ((local.len() as i32 > 0) && (domain.len() as i32 > 0)) && (domain.contains(&"."));
+     return Ok(((local.len() as i32 > 0) && (domain.len() as i32 > 0)) && (domain.contains(&".")));
  }
────────────────────────────────────────────────────────────