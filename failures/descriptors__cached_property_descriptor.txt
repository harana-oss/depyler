  descriptors.toml :: cached_property_descriptor
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::OnceCell;
− 
− struct MyClass {
−     base: i32,
−     computed: OnceCell<i32>,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct CachedProperty {
+     pub func: serde_json::Value,
+     pub name: (),
+ }
+ impl CachedProperty {
+     pub fn new(func: serde_json::Value) -> Self {
+         Self { func, name: () }
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct MyClass {
+     pub base: i32,
  }
− 
  impl MyClass {
−     fn new(base: i32) -> Self {
−         MyClass {
−             base,
−             computed: OnceCell::new(),
−         }
+     pub fn new() -> Self {
+         Self { base: 10 }
      }
− 
−     fn computed(&self) -> i32 {
−         *self.computed.get_or_init(|| {
−             log::info!("Computing...");
−             self.base * 2
−         })
+     pub fn computed(&self) -> i32 {
+         log::info!("{}", "Computing...".to_string());
+         return self.base * 2;
      }
  }
− 
− let instance = MyClass::new(10);
− log::info!("{}", instance.computed());
− log::info!("{}", instance.computed());
────────────────────────────────────────────────────────────