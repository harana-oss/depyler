  dataclasses-basic.toml :: dataclass_optional
    Output mismatch

────────────────────────────────────────────────────────────
− pub const c1: Config = Config::new("test".to_string());
− pub const c2: Config = Config::new("test".to_string(), 42);
− pub const result: (i32, i32) = (c1.value, c2.value);
− use serde_json;
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Config {
      pub name: String,
+     pub value: Option<i32>,
  }
  impl Config {
−     pub const value: Option<i32> = ();
      pub fn new(name: String) -> Self {
−         Self { name }
+         Self {
+             name,
+             value: Default::default(),
+         }
      }
  }
────────────────────────────────────────────────────────────