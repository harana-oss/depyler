  chainmap.toml :: chainmap_modify_maps
    Output mismatch

────────────────────────────────────────────────────────────
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_modify_maps() -> i32 {
−     let mut dict1 = HashMap::new();
−     dict1.insert("a".to_string(), 1);
− 
−     let mut dict2 = HashMap::new();
−     dict2.insert("b".to_string(), 2);
− 
−     let mut maps = vec![dict1, dict2];
−     maps[0].insert("x".to_string(), 99);
− 
−     return *maps[0].get("x").unwrap();
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn test_chainmap_modify_maps() -> Result<i32, IndexError> {
+     let dict1 = {
+         let mut map = HashMap::new();
+         map.insert("a".to_string(), 1);
+         map
+     };
+     let dict2 = {
+         let mut map = HashMap::new();
+         map.insert("b".to_string(), 2);
+         map
+     };
+     let c = ChainMap::new(dict1, dict2);
+     c.maps.get_mut(&0).unwrap().insert("x".to_string(), 99);
+     return Ok(c.get("x").cloned().unwrap());
  }
────────────────────────────────────────────────────────────