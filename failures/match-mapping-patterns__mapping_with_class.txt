  match-mapping-patterns.toml :: mapping_with_class
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
− 
−     struct Point {
−         x: i32,
−         y: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Point {
+     pub x: serde_json::Value,
+     pub y: serde_json::Value,
+ }
+ impl Point {
+     pub const __match_args__: serde_json::Value = ("x".to_string(), "y".to_string());
+     pub fn new(x: serde_json::Value, y: serde_json::Value) -> Self {
+         Self { x, y }
      }
− 
−     let mut data = HashMap::new();
−     data.insert("point", Point { x: 3, y: 4 });
− 
−     let result = match data.get("point") {
−         Some(Point { x, y }) => x + y,
−         None => 0,
−     };
+ }
────────────────────────────────────────────────────────────