  cse-optimization.toml :: nested_attribute_comparison
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Clone)]
  pub struct Period {
      pub name: String,
  }
− 
+ #[derive(Debug, Clone)]
  pub struct State {
      pub period: Period,
  }
− 
  pub fn is_extra_time(state: &State) -> bool {
      if state.period.name != "ExtraTime" {
          return true;
      }
      return false;
  }
────────────────────────────────────────────────────────────