  for-loop-iterator-mutation.toml :: for_loop_method_mutation_on_element
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Container {
      pub items: Vec<i32>,
  }
  impl Container {
      pub fn new(items: Vec<i32>) -> Self {
          Self { items }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Data {
      pub containers: Vec<Container>,
  }
  impl Data {
      pub fn new(containers: Vec<Container>) -> Self {
          Self { containers }
      }
  }
− pub fn add_to_all(data: &mut Data, value: i32) {
−     for mut container in data.containers.iter_mut() {
+ pub fn add_to_all(data: &Data, value: i32) {
+     for container in &data.containers {
+         let container = container.clone();
          container.items.push(value);
      }
  }
────────────────────────────────────────────────────────────