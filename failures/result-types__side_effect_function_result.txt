  result-types.toml :: side_effect_function_result
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashMap;
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn set_value(data: &mut HashMap<String, i32>, key: String, value: i32) {
+ pub fn set_value(
+     data: &mut HashMap<String, i32>,
+     key: String,
+     value: i32,
+ ) -> Result<(), Box<dyn std::error::Error>> {
      {
          data.insert(key, value);
−         panic!("{}", "Invalid key");
+         return Err(Box::new(ValueError::new("Invalid key")));
      }
+     Ok(())
  }
────────────────────────────────────────────────────────────