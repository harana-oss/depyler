  optional-types.toml :: optional_arithmetic
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn double(value: &Option<i32>) -> Option<i32> {
      if value.is_none() {
          return None;
      }
−     return Some(value.clone().unwrap() * 2);
+     return Some(value.unwrap() * 2);
  }
────────────────────────────────────────────────────────────