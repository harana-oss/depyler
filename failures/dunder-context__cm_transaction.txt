  dunder-context.toml :: cm_transaction
    Output mismatch

────────────────────────────────────────────────────────────
− struct Transaction {
−     committed: bool,
− }
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct Transaction {}
  impl Transaction {
−     fn new() -> Self {
−         log::info!("BEGIN");
−         Transaction { committed: false }
+     pub fn new() -> Self {
+         Self {}
+     }
+     pub fn __enter__(&self) -> &Self {
+         log::info!("{}", "BEGIN".to_string());
+         return self;
      }
− }
− 
− impl Drop for Transaction {
−     fn drop(&mut self) {
−         if self.committed {
−             log::info!("COMMIT");
+     pub fn __exit__(&self, exc_type: bool) -> bool {
+         if exc_type {
+             log::info!("{}", "ROLLBACK".to_string());
          } else {
−             log::info!("COMMIT"); // For this example
+             log::info!("{}", "COMMIT".to_string());
          }
+         return false;
      }
  }
− 
− let _tx = Transaction::new();
− log::info!("INSERT data");
────────────────────────────────────────────────────────────