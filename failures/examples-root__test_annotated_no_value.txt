  examples-root.toml :: test_annotated_no_value
    Output mismatch

────────────────────────────────────────────────────────────
− pub const counter: i32 = 10;
− pub const name: &str = "test";
− pub const is_valid: bool = true;
− pub const score: f64 = 3.14;
− pub const items: Vec<i32> = vec![1, 2, 3];
− pub const mapping: HashMap<String, i32> = {
−     let mut map = HashMap::new();
−     map.insert("a".to_string(), 1);
−     map.insert("b".to_string(), 2);
−     map
− };
− pub const unique_values: HashSet<&str> = {
−     let mut set = HashSet::new();
−     set.insert("x");
−     set.insert("y");
−     set.insert("z");
−     set
− };
− pub const field_position: FieldPosition = FieldPosition::new(5, 10);
− pub const new_team: &str = "TeamA";
− pub const valid: bool = true;
− pub const maybe_value: &str = "found";
− use std::collections::HashMap;
− use std::collections::HashSet;
− #[derive(Debug, Copy, Clone]
+ #[derive(Debug, Copy, Clone)]
  pub struct FieldPosition {
      pub x: i32,
      pub y: i32,
  }
  impl FieldPosition {
      pub fn new(x: i32, y: i32) -> Self {
          Self { x, y }
      }
− }
+ }
────────────────────────────────────────────────────────────