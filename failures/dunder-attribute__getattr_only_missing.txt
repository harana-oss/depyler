  dunder-attribute.toml :: getattr_only_missing
    Output mismatch

────────────────────────────────────────────────────────────
− struct Obj {
−     real: String,
+ #[derive(Debug, Clone)]
+ pub struct Obj {
+     pub real: String,
  }
− 
  impl Obj {
−     fn new() -> Self {
−         Obj {
+     pub fn new() -> Self {
+         Self {
              real: "real value".to_string(),
          }
      }
− 
−     fn get_attr(&self, name: &str) -> String {
−         match name {
−             "real" => self.real.clone(),
−             _ => format!("fallback: {}", name),
−         }
−     }
  }
− 
− let obj = Obj::new();
− log::info!("{}", obj.real);
− log::info!("{}", obj.get_attr("missing"));
────────────────────────────────────────────────────────────