  performance-patterns.toml :: iterator_no_bounds
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn sum_iter(items: Vec<i32>) -> i32 {
+ pub fn sum_iter(items: &Vec<i32>) -> i32 {
      let mut total = 0;
−     for item in items {
+     for item in items.iter().cloned() {
          total += item;
      }
      return total;
  }
────────────────────────────────────────────────────────────