  performance-patterns.toml :: inline_simple_accessor
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Copy, Clone)]
  pub struct Point {
      pub x: i32,
      pub y: i32,
  }
− 
  impl Point {
      pub fn new(x: i32, y: i32) -> Self {
          Self { x, y }
      }
− 
−     #[inline]
      pub fn get_x(&self) -> i32 {
          return self.x;
      }
− 
−     #[inline]
      pub fn get_y(&self) -> i32 {
          return self.y;
      }
  }
────────────────────────────────────────────────────────────