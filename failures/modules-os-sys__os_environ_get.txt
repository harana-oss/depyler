  modules-os-sys.toml :: os_environ_get
    Output mismatch

────────────────────────────────────────────────────────────
  use std as os;
  pub fn get_env(key: String) -> String {
−     return std::env::var(key)
−         .unwrap_or_else(|_| "".to_string())
−         .unwrap();
+     return std::env::var(key).unwrap_or_else(|_| "".to_string());
  }
────────────────────────────────────────────────────────────