  metaclasses.toml :: metaclass_call
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, we control instantiation via factory functions
− struct MyClass {
−     x: i32,
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct CallMeta {}
+ impl CallMeta {
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− 
+ #[derive(Debug, Clone)]
+ pub struct MyClass {
+     pub x: serde_json::Value,
+ }
  impl MyClass {
−     fn new(x: i32) -> Self {
−         log::info!("Creating instance of MyClass");
−         let instance = MyClass { x };
−         log::info!("Instance created");
−         instance
+     pub fn new(x: serde_json::Value) -> Self {
+         Self { x }
      }
  }
− 
− let obj = MyClass::new(42);
− log::info!("{}", obj.x);
────────────────────────────────────────────────────────────