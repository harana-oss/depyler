  interprocedural-mutation.toml :: index_expression_mutation
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Record {
      pub values: Vec<i32>,
  }
  impl Record {
      pub fn new(values: Vec<i32>) -> Self {
          Self { values }
      }
  }
− pub fn get_values<'a>(record: &'a mut Record) -> &'a mut Vec<i32> {
−     return &mut record.values;
+ pub fn get_values(record: &Record) -> Vec<i32> {
+     return record.values;
  }
− pub fn update_first(record: &mut Record) {
−     get_values(record)[0] = 999;
+ pub fn update_first(record: &Record) {
+     get_values(&record)[0 as usize] = 999;
  }
────────────────────────────────────────────────────────────