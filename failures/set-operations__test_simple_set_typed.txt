  set-operations.toml :: test_simple_set_typed
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashSet;
  pub fn test_simple_set() -> HashSet<i32> {
      let s1 = {
          let mut set = HashSet::new();
          set.insert(1);
          set.insert(2);
          set.insert(3);
          set
      };
      let s2 = {
          let mut set = HashSet::new();
          set.insert(3);
          set.insert(4);
          set.insert(5);
          set
      };
−     let _cse_temp_0 = s1
+     let result = s1
          .union(&s2)
          .cloned()
          .collect::<std::collections::HashSet<_>>();
−     let result = _cse_temp_0;
−     result
+     return result;
  }
  pub fn test_set_method() -> HashSet<String> {
      let mut fruits = {
          let mut set = HashSet::new();
−         set.insert("apple".to_string());
−         set.insert("banana".to_string());
+         set.insert("apple");
+         set.insert("banana");
          set
      };
      fruits.insert("cherry".to_string());
−     fruits
+     return fruits;
  }
────────────────────────────────────────────────────────────