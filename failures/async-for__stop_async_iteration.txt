  async-for.toml :: stop_async_iteration
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− #[tokio::main]
− async let result: Vec<i32> = stream::iter(1..=3).collect().await;
− log::info!("{:?}", result);
+ use serde_json;
+ #[derive(Debug, Clone)] pub struct AsyncLimit {
+     pub limit: serde_json::Value, pub count: i32
+ }
+ impl AsyncLimit {
+     pub fn new(limit: serde_json::Value) -> Self {
+     Self {
+     limit, count: 0
+ }
+ } pub async fn __anext__(&mut self) {
+     if self.count>= self.limit {
+     panic!("Exception: {}", StopAsyncIteration);
+    
+ }
+ self.count += 1;
+     return self.count;
+    
+ }
+ } pub async fn test() -> Vec<serde_json::Value>{
+     let result = vec! [];
+     while let Some(i) = AsyncLimit::new(3).next().await {
+     result.push(i);
+    
+ }
+ return result;
+     }
────────────────────────────────────────────────────────────