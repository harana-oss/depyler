  decorators-basic.toml :: decorator_exception
    Output mismatch

────────────────────────────────────────────────────────────
− fn decorator<F>(f: F) -> impl Fn() -> Result<String, String>
−     where
−         F: Fn() -> Result<String, String>,
−     {
−         move || f()
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
      }
− 
−     fn failing_impl() -> Result<String, String> {
−         Err("error".to_string())
+ }
+ pub fn decorator(f: &serde_json::Value) {
+     fn wrapper() -> () {
+         return f();
      }
− 
−     let failing = decorator(failing_impl);
−     let result = match failing() {
−         Ok(val) => val,
−         Err(e) => e,
−     };
+     return wrapper;
+ }
+ pub fn failing() -> Result<(), ValueError> {
+     return Err(ValueError::new("error"));
+     Ok(())
+ }
────────────────────────────────────────────────────────────