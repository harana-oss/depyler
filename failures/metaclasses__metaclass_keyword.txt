  metaclasses.toml :: metaclass_keyword
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, attribute transformation happens at compile-time via macros
− // We simulate with explicit uppercase constant
− struct MyClass;
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct UpperAttrMeta {}
+ impl UpperAttrMeta {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct MyClass {}
  impl MyClass {
−     const X: i32 = 10; // Uppercase by convention
+     pub const x: i32 = 10;
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− 
− log::info!("{}", true); // MyClass has X
− log::info!("{}", MyClass::X);
────────────────────────────────────────────────────────────