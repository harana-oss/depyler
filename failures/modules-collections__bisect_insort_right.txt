  modules-collections.toml :: bisect_insort_right
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  pub fn insert_right(a: &Vec<serde_json::Value>, x: i32) {
      {
          let arr = &mut (a);
          let val = x;
          let pos = match arr.binary_search(&val) {
              Ok(mut pos) => {
                  pos += 1;
                  while pos < arr.len() && arr[pos] == val {
                      pos += 1;
                  }
                  pos
              }
              Err(pos) => pos,
          };
          arr.insert(pos, val);
−     }
+     };
+ }
────────────────────────────────────────────────────────────