  async-comprehensions.toml :: async_comp_await_cond
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− async fn check(x: i32) -> bool {
−     x % 2 == 0
+ use serde_json;
+ #[derive(Debug, Clone)] pub struct ZeroDivisionError {
+     message: String ,
+ }
+ impl std::fmt::Display for ZeroDivisionError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+     write!(f, "division by zero: {}", self.message)
+ }
+ } impl std::error::Error for ZeroDivisionError {
+    
+ }
+ impl ZeroDivisionError {
+     pub fn new(message: impl Into<String>) -> Self {
+     Self {
+     message: message.into()
+ }
  }
− 
− #[tokio::main]
− async let result: Vec<i32> = stream::iter(0..5)
−     .filter(|x| async move { check(*x).await })
−     .collect()
−     .await;
− log::info!("{:?}", result);
+ }
+ #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncRangeState {
+     state: usize, n: serde_json::Value
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_range(n: & serde_json::Value) -> impl Iterator<Item = i32>{
+     AsyncRangeState {
+     state: 0, n: n
+ }
+ } impl Iterator for AsyncRangeState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _i in 0..self.n {
+     return Some(i);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn check(x: & serde_json::Value) -> Result<bool, ZeroDivisionError>{
+     return Ok(x % 2 == 0);
+    
+ }
+ pub async fn test() -> Vec<serde_json::Value>{
+     let result = async_range(5).iter().cloned().filter(| x | check(& x).await).collect::<Vec<_>>();
+     return result;
+     }
────────────────────────────────────────────────────────────