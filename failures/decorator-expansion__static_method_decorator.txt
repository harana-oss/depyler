  decorator-expansion.toml :: static_method_decorator
    Output mismatch

────────────────────────────────────────────────────────────
− pub struct Math;
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct Math {}
  impl Math {
+     pub fn new() -> Self {
+         Self {}
+     }
      pub fn add(a: i32, b: i32) -> i32 {
          return a + b;
      }
− 
      pub fn multiply(a: i32, b: i32) -> i32 {
          return a * b;
      }
  }
────────────────────────────────────────────────────────────