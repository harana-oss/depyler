  exceptions.toml :: finally_with_variable_assignment
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn track_execution(x: i32) -> i32 {
−     let mut executed = false;
−     {
−         let result = x * 2;
−         return result;
−         executed = true;
−     }
+     let executed = false;
+     let result = x * 2;
+     return result;
+     executed = true;
  }
────────────────────────────────────────────────────────────