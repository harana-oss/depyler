  contextlib-utilities.toml :: exitstack_order
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn test_exitstack_order() -> Vec<i32> {
−     let mut order = Vec::new();
−     {
−         // callbacks registered: 1, 2, 3
−     }
−     // cleanup in LIFO order: 3, 2, 1
−     order.push(3);
−     order.push(2);
−     order.push(1);
−     order
+ use serde_json;
+ pub fn test_exitstack_order() -> Vec<serde_json::Value> {
+     let order = vec![];
+     let _context = ExitStack::new();
+     let mut stack = _context.__enter__();
+     stack.callback(order.append, 1);
+     stack.callback(order.append, 2);
+     stack.callback(order.append, 3);
+     return order;
  }
────────────────────────────────────────────────────────────