  set-operations.toml :: set_disjoint
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: bool = a.clone().is_disjoint(&b.clone());
− lazy_static! {
−     pub static ref a: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(1);
−         set.insert(2);
−         set
−     };
−     pub static ref b: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(3);
−         set.insert(4);
−         set
−     };
− }
− use lazy_static::lazy_static;
− use serde_json;
− use std::collections::HashSet;
+ 
────────────────────────────────────────────────────────────