  dunder-callable.toml :: callable_higher_order
    Output mismatch

────────────────────────────────────────────────────────────
− struct Square;
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct Square {}
  impl Square {
−     fn call(&self, x: i32) -> i32 {
−         x * x
+     pub fn new() -> Self {
+         Self {}
      }
  }
− 
− let square = Square;
− let result: Vec<i32> = vec![1, 2, 3, 4].iter().map(|&x| square.call(x)).collect();
− log::info!("{:?}", result);
────────────────────────────────────────────────────────────