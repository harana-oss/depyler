  string-methods-all.toml :: str_isidentifier
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: bool = {
−     let s = "var_name";
−     let mut chars = s.chars();
−     if let Some(first) = chars.next() {
−         (first.is_alphabetic() || first == '_') && chars.all(|c| c.is_alphanumeric() || c == '_')
−     } else {
−         false
−     }
− };
+ 
────────────────────────────────────────────────────────────