  global-nonlocal.toml :: nonlocal_basic
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::Cell;
− 
− fn outer() {
−     let x = Cell::new(10);
− 
−     let inner = || {
−         x.set(20);
−     };
− 
−     log::info!("Before: {}", x.get());
+ pub fn outer() {
+     let x = 10;
+     fn inner() -> () {
+         x = 20;
+     }
+     log::info!("{}", format!("Before: {}", x));
      inner();
−     log::info!("After: {}", x.get());
+     log::info!("{}", format!("After: {}", x));
  }
− 
− outer();
────────────────────────────────────────────────────────────