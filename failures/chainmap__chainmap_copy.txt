  chainmap.toml :: chainmap_copy
    Output mismatch

────────────────────────────────────────────────────────────
+ use serde_json;
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_copy() -> HashMap<String, i32> {
−     let mut dict1 = HashMap::new();
−     dict1.insert("a".to_string(), 1);
−     dict1.insert("b".to_string(), 2);
− 
−     let mut dict2 = HashMap::new();
−     dict2.insert("c".to_string(), 3);
− 
−     let maps = vec![dict1, dict2];
− 
−     let mut result = HashMap::new();
−     let mut seen_keys = std::collections::HashSet::new();
− 
−     for map in &maps {
−         for (key, value) in map {
−             if seen_keys.insert(key.clone()) {
−                 result.insert(key.clone(), *value);
−             }
−         }
−     }
− 
−     return result;
+ pub fn test_chainmap_copy() -> HashMap<serde_json::Value, serde_json::Value> {
+     let dict1 = {
+         let mut map = HashMap::new();
+         map.insert("a".to_string(), 1);
+         map.insert("b".to_string(), 2);
+         map
+     };
+     let dict2 = {
+         let mut map = HashMap::new();
+         map.insert("c".to_string(), 3);
+         map
+     };
+     let c = ChainMap::new(dict1, dict2);
+     return c.clone().into_iter().collect::<HashMap<_, _>>();
  }
────────────────────────────────────────────────────────────