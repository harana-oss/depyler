  parser-pattern-matching.toml :: pattern_or_binds_same_names
    Output mismatch

────────────────────────────────────────────────────────────
− fn get_first(x: i64) -> i64 {
−     match x {
−         1 => 10,
−         2 => 20,
−         _ => 0,
+ pub fn get_first(x: i32) -> i32 {
+     if x == 1 {
+         return 10;
+     } else {
+         if x == 2 {
+             return 20;
+         } else {
+             return 0;
+         }
      }
− }
− 
− fn main() {
−     let result = get_first(2);
−     println!("{}", result);
  }
────────────────────────────────────────────────────────────