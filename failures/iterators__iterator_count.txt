  iterators.toml :: iterator_count
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn count_positive(items: &Vec<i32>) -> i32 {
      return items
−         .clone()
−         .into_iter()
+         .iter()
+         .cloned()
          .filter(|x| *x > 0)
−         .map(|x| x)
          .collect::<Vec<_>>()
−         .len() as i32;
+         .len() as i32 as i32;
  }
────────────────────────────────────────────────────────────