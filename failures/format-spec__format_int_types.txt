  format-spec.toml :: format_int_types
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn format_int_types() -> (String, String, String, String, String) {
+ pub fn format_int_types() -> () {
      let n = 42;
      return (
−         format!("{}", n),
−         format!("{:b}", n),
−         format!("{:o}", n),
−         format!("{:x}", n),
−         format!("{:X}", n),
+         format(n, "d".to_string()),
+         format(n, "b".to_string()),
+         format(n, "o".to_string()),
+         format(n, "x".to_string()),
+         format(n, "X".to_string()),
      );
  }
────────────────────────────────────────────────────────────