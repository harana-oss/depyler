  for-loop-iterator-mutation.toml :: nested_for_loops_with_mutation
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, PartialEq, Default)]
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
  pub struct Cell {
      pub value: i32,
  }
  impl Cell {
      pub fn new(value: i32) -> Self {
          Self { value }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Row {
      pub cells: Vec<Cell>,
  }
  impl Row {
      pub fn new(cells: Vec<Cell>) -> Self {
          Self { cells }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Grid {
      pub rows: Vec<Row>,
  }
  impl Grid {
      pub fn new(rows: Vec<Row>) -> Self {
          Self { rows }
      }
  }
− pub fn increment_all(grid: &mut Grid) {
−     for mut row in grid.rows.iter_mut() {
−         for mut cell in row.cells.iter_mut() {
−             cell.value = cell.value + 1;
+ pub fn increment_all(grid: &Grid) {
+     for row in &grid.rows {
+         let row = row.clone();
+         for mut cell in &mut row.cells {
+             cell.value += 1;
          }
      }
  }
────────────────────────────────────────────────────────────