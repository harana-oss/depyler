  parser-pattern-matching.toml :: pattern_guard_condition
    Output mismatch

────────────────────────────────────────────────────────────
− fn classify(x: i64) -> String {
−     match x {
−         n if n < 0 => "negative".to_string(),
−         n if n > 0 => "positive".to_string(),
−         _ => "zero".to_string(),
+ pub fn classify(x: i32) -> String {
+     if x < 0 {
+         return "negative".to_string();
+     } else {
+         if x > 0 {
+             return "positive".to_string();
+         } else {
+             return "zero".to_string();
+         }
      }
− }
− 
− fn main() {
−     let result = classify(-5);
−     println!("{}", result);
  }
────────────────────────────────────────────────────────────