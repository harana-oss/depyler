  match-mapping-patterns.toml :: mapping_empty
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
− 
−     let mut data: HashMap<&str, i32> = HashMap::new();
−     data.insert("a", 1);
−     data.insert("b", 2);
  
−     let result = if data.is_empty() || !data.is_empty() {
−         "mapping"
−     } else {
−         "other"
−     };
────────────────────────────────────────────────────────────