  modules-regex.toml :: re_finditer
    Output mismatch

────────────────────────────────────────────────────────────
  use regex as re;
  pub fn find_word_positions(s: String, word: String) -> Vec<i32> {
      return regex::Regex::new(word)
          .unwrap()
          .find_iter(s)
          .map(|m| m.as_str().to_string())
          .collect::<Vec<_>>()
−         .clone()
−         .into_iter()
+         .iter()
+         .cloned()
          .map(|m| m.start())
          .collect::<Vec<_>>();
  }
────────────────────────────────────────────────────────────