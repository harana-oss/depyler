  dunder-lifecycle.toml :: lifecycle_flow
    Output mismatch

────────────────────────────────────────────────────────────
− struct Tracked {
−     value: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Tracked {
+     pub value: serde_json::Value,
  }
− 
  impl Tracked {
−     fn new(value: i32) -> Self {
−         log::info!("__new__ called");
−         log::info!("__init__ called");
−         Tracked { value }
+     pub fn new(value: serde_json::Value) -> Self {
+         Self { value }
      }
  }
− 
  impl Drop for Tracked {
      fn drop(&mut self) {
−         log::info!("__del__ called");
+         log::info!("{}", "__del__ called".to_string());
      }
  }
− 
− let t = Tracked::new(42);
− log::info!("Value: {}", t.value);
− drop(t);
− log::info!("End");
────────────────────────────────────────────────────────────