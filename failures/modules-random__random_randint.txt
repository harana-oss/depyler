  modules-random.toml :: random_randint
    Output mismatch

────────────────────────────────────────────────────────────
  use rand as random;
− use rand::prelude::*;
− use rand::Rng;
  pub fn roll_dice() -> i32 {
−     return rand::thread_rng().gen_range(1..=6);
+     return DEPYLER_RNG.with(|rng| rng.borrow_mut().gen_range(1..=6));
  }
────────────────────────────────────────────────────────────