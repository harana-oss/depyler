  decorators-basic.toml :: decorator_lambda
    Output mismatch

────────────────────────────────────────────────────────────
− fn add_one<F>(f: F) -> impl Fn(i32) -> i32
−     where
−         F: Fn(i32) -> i32,
−     {
−         move |x| f(x) + 1
+ use serde_json;
+ pub fn add_one(f: &serde_json::Value) {
+     fn wrapper(x: ()) -> () {
+         return f(x) + 1;
      }
− 
−     let add_two = add_one(|x| x + 1);
−     let result = add_two(5);
+     return wrapper;
+ }
────────────────────────────────────────────────────────────