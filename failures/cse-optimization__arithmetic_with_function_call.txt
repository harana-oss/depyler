  cse-optimization.toml :: arithmetic_with_function_call
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn calculate_with_len(items: Vec<i32>, x: i32) -> i32 {
−     let _cse_temp_0 = items.len() as i32;
−     return _cse_temp_0 + x + _cse_temp_0 * 2;
+ pub fn calculate_with_len(items: &Vec<i32>, x: i32) -> i32 {
+     return items.len() as i32 + x + items.len() as i32 * 2 as i32;
  }
────────────────────────────────────────────────────────────