  examples-tooling.toml :: simple_migration_demo
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  #[derive(Debug, Clone)]
  pub struct IndexError {
      message: String,
  }
  impl std::fmt::Display for IndexError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "index out of range: {}", self.message)
      }
  }
  impl std::error::Error for IndexError {}
  impl IndexError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  #[doc = "Shows accumulator pattern."]
− pub fn accumulator_example(items: &serde_json::Value) -> Vec<i32> {
+ pub fn accumulator_example(items: &Vec<i32>) -> Vec<serde_json::Value> {
      let mut result = vec![];
      for item in items.iter().cloned() {
          result.push(item * 2);
      }
−     result
+     return result;
  }
  #[doc = "Shows inefficient string building."]
  pub fn string_concat_example(values: &serde_json::Value) -> String {
−     let mut output = "";
+     let mut output = "".to_string();
      for val in values.iter().cloned() {
−         output = format!("{}{}", output, val.to_string());
+         output += (val).to_string();
      }
−     output
+     return output;
  }
  #[doc = "Shows range(len()) antipattern."]
  pub fn enumerate_example(data: &serde_json::Value) -> Result<(), IndexError> {
      for i in 0..data.len() as i32 {
−         log::info!(
−             "{} {}",
−             i,
−             data.get(i as usize).cloned().unwrap_or_default()
−         );
+         log::info!("{} {}", i, data.get(i as usize).cloned().unwrap());
      }
+     Ok(())
  }
  #[doc = "Shows while True pattern."]
  pub fn while_true_example() -> i32 {
      let mut count = 0;
      while true {
          count += 1;
          if count > 10 {
              break;
          }
      }
−     count
+     return count;
  }
────────────────────────────────────────────────────────────