  enum-int.toml :: intenum_as_int
    Output mismatch

────────────────────────────────────────────────────────────
− #[repr(i32)]
−     #[derive(Debug, Clone, Copy)]
−     enum Priority {
−         Low = 1,
−         High = 5,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Priority {}
+ impl Priority {
+     pub const LOW: i32 = 1;
+     pub const HIGH: i32 = 5;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let values: Vec<i32> = ((Priority::Low as i32)..(Priority::High as i32)).collect();
−     assert_eq!(values, vec![1, 2, 3, 4]);
+ }
────────────────────────────────────────────────────────────