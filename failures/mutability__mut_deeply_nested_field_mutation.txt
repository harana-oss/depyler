  mutability.toml :: mut_deeply_nested_field_mutation
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Copy, Clone)]
  pub struct Level3 {}
  impl Level3 {
      pub fn new() -> Self {
          Self {}
      }
  }
  #[derive(Debug, Copy, Clone)]
  pub struct Level2 {}
  impl Level2 {
      pub fn new() -> Self {
          Self {}
      }
  }
  #[derive(Debug, Copy, Clone)]
  pub struct Level1 {}
  impl Level1 {
      pub fn new() -> Self {
          Self {}
      }
  }
  #[doc = "Test that deeply nested field mutation marks the root variable as mutable."]
  pub fn mutate_deeply_nested(root: &Level1, item: String) {
−     let mut obj = root.level2;
+     let obj = root.level2;
      obj.level3.items.push(item);
  }
────────────────────────────────────────────────────────────