  class-methods.toml :: classmethod_basic
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Copy, Clone)]
  pub struct MyClass {}
  impl MyClass {
      pub const count: i32 = 0;
      pub fn new() -> Self {
          Self {}
      }
      pub fn increment() {
          Self::count += 1;
          return Self::count;
      }
  }
− log::info!("{}", MyClass::increment());
− log::info!("{}", MyClass::increment());
────────────────────────────────────────────────────────────