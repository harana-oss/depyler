  lifetimes.toml :: return_field_from_borrowed_param
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct State {
      pub players: Vec<String>,
  }
  impl State {
      pub fn new(players: Vec<String>) -> Self {
          Self { players }
      }
  }
  pub fn get_players(state: &State) -> Vec<String> {
−     return state.players.clone();
+     return state.players;
  }
────────────────────────────────────────────────────────────