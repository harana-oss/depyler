  asyncio-tasks.toml :: asyncio_lock
    Output mismatch

────────────────────────────────────────────────────────────
− use tokio::sync::Mutex;
− 
  pub async fn test_asyncio_lock() -> i32 {
−     let mutex = Mutex::new(42);
−     let guard = mutex.lock().await;
−     *guard
+     let lock = asyncio.Lock();
+     {
+     let _ctx = lock;
+     return 42;
+    
+ }
  }
────────────────────────────────────────────────────────────