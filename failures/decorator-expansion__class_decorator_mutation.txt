  decorator-expansion.toml :: class_decorator_mutation
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Copy, Clone)]
  pub struct Counter {
      pub count: i32,
  }
− 
  impl Counter {
      pub fn new() -> Self {
          Self { count: 0 }
      }
− 
      pub fn increment(&mut self) -> i32 {
          self.count += 1;
          return self.count;
      }
− 
      pub fn get_count(&self) -> i32 {
          return self.count;
      }
  }
────────────────────────────────────────────────────────────