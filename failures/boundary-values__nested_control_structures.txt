  boundary-values.toml :: nested_control_structures
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ZeroDivisionError {
      message: String,
  }
  impl std::fmt::Display for ZeroDivisionError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "division by zero: {}", self.message)
      }
  }
  impl std::error::Error for ZeroDivisionError {}
  impl ZeroDivisionError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn all_operators(a: i32, b: i32) -> bool {
−     let add = a + b;
−     let sub = a - b;
−     let mul = a * b;
−     let div = {
−         let a = a;
−         let b = b;
−         let q = a / b;
−         let r = a % b;
−         let r_negative = r < 0;
−         let b_negative = b < 0;
−         let r_nonzero = r != 0;
−         let signs_differ = r_negative != b_negative;
−         let needs_adjustment = r_nonzero && signs_differ;
−         if needs_adjustment {
−             q - 1
−         } else {
−             q
+ pub fn nested_control(n: i32) -> Result<i32, ZeroDivisionError> {
+     let mut result = 0;
+     for i in 0..n {
+         if i % 2 == 0 {
+             for j in 0..i {
+                 if j % 3 == 0 {
+                     result += j;
+                 }
+             }
          }
−     };
−     let r#mod = a % b;
−     let eq = a == b;
−     let ne = a != b;
−     let lt = a < b;
−     let le = a <= b;
−     let gt = a > b;
−     let ge = a >= b;
−     let and_op = (a != 0) && (b != 0);
−     let or_op = (a != 0) || (b != 0);
−     let not_op = !a;
−     return (((((eq) || (ne)) || (lt)) || (le)) || (gt)) || (ge);
+     }
+     return Ok(result);
  }
────────────────────────────────────────────────────────────