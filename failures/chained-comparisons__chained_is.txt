  chained-comparisons.toml :: chained_is
    Output mismatch

────────────────────────────────────────────────────────────
− // a is b is c
−     use std::rc::Rc;
  
−     let x = Rc::new(vec![1, 2, 3]);
−     let a = Rc::clone(&x);
−     let b = Rc::clone(&x);
−     let c = Rc::clone(&x);
−     let result = Rc::ptr_eq(&a, &b) && Rc::ptr_eq(&b, &c);
────────────────────────────────────────────────────────────