  async-for.toml :: async_for_context
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− #[tokio::main]
− async let mut result = Vec::new();
− let mut stream = stream::iter(0..3);
− while let Some(item) = stream.next().await {
+ use serde_json;
+ #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncIterState {
+     state: usize ,
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_iter() -> impl Iterator<Item = i32>{
+     AsyncIterState {
+     state: 0 ,
+ }
+ } impl Iterator for AsyncIterState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _i in 0..3 {
+     return Some(i);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn process() -> Vec<serde_json::Value>{
+     let result = vec! [];
+     while let Some(item) = async_iter().next().await {
      result.push(item);
+    
  }
− log::info!("{}", result.len());
+ return result;
+     }
────────────────────────────────────────────────────────────