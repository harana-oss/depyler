  match-mapping-patterns.toml :: mapping_non_dict
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
− 
−     trait Mapping {
−         fn get(&self, key: &str) -> Option<i32>;
−     }
− 
−     struct MyMapping {
−         data: HashMap<String, i32>,
+ use std::collections::UserDict;
+ #[derive(Debug, Copy, Clone)]
+ pub struct MyMapping {}
+ impl MyMapping {
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     impl Mapping for MyMapping {
−         fn get(&self, key: &str) -> Option<i32> {
−             self.data.get(key).copied()
−         }
−     }
− 
−     let mut inner = HashMap::new();
−     inner.insert("key".to_string(), 42);
−     let data = MyMapping { data: inner };
− 
−     let result = match data.get("key") {
−         Some(v) => v,
−         None => 0,
−     };
+ }
────────────────────────────────────────────────────────────