  dunder-iteration.toml :: reversed_fallback
    Output mismatch

────────────────────────────────────────────────────────────
− struct MySequence {
−     data: Vec<i32>,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct MySequence {
+     pub data: serde_json::Value,
  }
− 
  impl MySequence {
−     fn new(data: Vec<i32>) -> Self {
−         MySequence { data }
−     }
− 
−     fn len(&self) -> usize {
−         self.data.len()
−     }
− 
−     fn get(&self, index: usize) -> Option<&i32> {
−         self.data.get(index)
+     pub fn new(data: serde_json::Value) -> Self {
+         Self { data }
      }
  }
− 
− let seq = MySequence::new(vec![5, 10, 15]);
− for i in (0..seq.len()).rev() {
−     log::info!("{}", seq.get(i).unwrap());
− }
────────────────────────────────────────────────────────────