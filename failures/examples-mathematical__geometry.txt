  examples-mathematical.toml :: geometry
    Output mismatch

────────────────────────────────────────────────────────────
− Error: Unsupported type annotation
+ #[derive(Debug, Clone)]
+ pub struct ZeroDivisionError {
+     message: String,
+ }
+ impl std::fmt::Display for ZeroDivisionError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "division by zero: {}", self.message)
+     }
+ }
+ impl std::error::Error for ZeroDivisionError {}
+ impl ZeroDivisionError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Point {
+     pub x: f64,
+     pub y: f64,
+ }
+ impl Point {
+     pub fn new(x: f64, y: f64) -> Self {
+         Self { x, y }
+     }
+     pub fn distance_to(&self, other: Point) -> f64 {
+         let mut dx = self.x - other.x;
+         let mut dy = self.y - other.y;
+         let mut distance_squared = dx * dx + dy * dy;
+         if distance_squared == 0 {
+             return 0;
+         }
+         let mut result = distance_squared / 2;
+         for _ in 0..10 {
+             let mut result = result + distance_squared / result / 2;
+         }
+         return result;
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Rectangle {
+     pub width: f64,
+     pub height: f64,
+ }
+ impl Rectangle {
+     pub fn new(width: f64, height: f64) -> Self {
+         Self { width, height }
+     }
+     pub fn area(&self) -> f64 {
+         return self.width * self.height;
+     }
+     pub fn perimeter(&self) -> f64 {
+         return 2 * self.width + self.height;
+     }
+     pub fn is_square(&self) -> bool {
+         return abs(self.width - self.height) < 0.0001;
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Circle {
+     pub radius: f64,
+ }
+ impl Circle {
+     pub fn new(radius: f64) -> Self {
+         Self { radius }
+     }
+     pub fn area(&self) -> f64 {
+         let mut pi = 3.14159;
+         return pi * self.radius * self.radius;
+     }
+     pub fn circumference(&self) -> f64 {
+         let mut pi = 3.14159;
+         return 2 * pi * self.radius;
+     }
+     pub fn contains_point(&self, point: Point) -> bool {
+         let mut distance_squared = point.x * point.x + point.y * point.y;
+         let mut radius_squared = self.radius * self.radius;
+         return distance_squared <= radius_squared;
+     }
+ }
+ #[doc = "Calculate triangle area"]
+ pub fn triangle_area(base: f64, height: f64) -> f64 {
+     return 0.5 * base * height;
+ }
+ #[doc = "Calculate triangle area using Heron's formula"]
+ pub fn triangle_area_heron(a: f64, b: f64, c: f64) -> Result<f64, ZeroDivisionError> {
+     let _cse_temp_0 = a + b <= c;
+     let _cse_temp_1 = a + c <= b;
+     let _cse_temp_2 = b + c <= a;
+     let _cse_temp_3 = ((_cse_temp_0) || (_cse_temp_1)) || (_cse_temp_2);
+     if _cse_temp_3 {
+         return Ok(0.0);
+     }
+     let _cse_temp_4 = a + b + c;
+     let s = _cse_temp_4 / 2.0;
+     let _cse_temp_5 = s * s - a;
+     let _cse_temp_6 = _cse_temp_5 * s - b;
+     let _cse_temp_7 = _cse_temp_6 * s - c;
+     let area_squared = _cse_temp_7;
+     if area_squared <= 0.0 {
+         return Ok(0.0);
+     }
+     let mut result = area_squared / 2.0;
+     for __ in 0..10 {
+         result = (result + area_squared / result) / 2.0;
+     }
+     return Ok(result);
+ }
+ #[doc = "Find intersection of two lines defined by point pairs"]
+ pub fn line_intersection(
+     p1: &Point,
+     p2: &Point,
+     p3: &Point,
+     p4: &Point,
+ ) -> Result<(bool, Point), ZeroDivisionError> {
+     let (x1, y1) = (p1.x, p1.y);
+     let (x2, y2) = (p2.x, p2.y);
+     let (x3, y3) = (p3.x, p3.y);
+     let (x4, y4) = (p4.x, p4.y);
+     let _cse_temp_0 = x1 - x2 * y3 - y4;
+     let _cse_temp_1 = y1 - y2 * x3 - x4;
+     let denominator = _cse_temp_0 - _cse_temp_1;
+     if denominator.abs() < 0.0001 {
+         return Ok((false, Point::new(0.0, 0.0)));
+     }
+     let _cse_temp_2 = x1 - x3 * y3 - y4;
+     let _cse_temp_3 = y1 - y3 * x3 - x4;
+     let _cse_temp_4 = (_cse_temp_2 - _cse_temp_3 as f64) / (denominator as f64);
+     let t = _cse_temp_4;
+     let _cse_temp_5 = t * x2 - x1;
+     let intersection_x = x1 + _cse_temp_5;
+     let _cse_temp_6 = t * y2 - y1;
+     let intersection_y = y1 + _cse_temp_6;
+     return Ok((true, Point::new(intersection_x, intersection_y)));
+ }
────────────────────────────────────────────────────────────