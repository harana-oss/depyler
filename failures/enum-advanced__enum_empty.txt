  enum-advanced.toml :: enum_empty
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− enum Empty {}
− 
− pub fn test_enum_empty() -> usize {
−     0
+ #[derive(Debug, Copy, Clone)]
+ pub struct Empty {}
+ impl Empty {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ pub fn test_enum_empty() -> i32 {
+     return Empty.into_iter().collect::<Vec<_>>().len() as i32 as i32;
  }
────────────────────────────────────────────────────────────