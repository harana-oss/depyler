  class-methods.toml :: staticmethod_instance_call
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Copy, Clone)]
  pub struct Math {}
  impl Math {
      pub fn new() -> Self {
          Self {}
      }
      pub fn multiply(a: i32, b: i32) {
          return a * b;
      }
  }
− let obj = Math::new();
− log::info!("{}", obj.multiply(3, 4));
− log::info!("{}", Math::multiply(3, 4));
────────────────────────────────────────────────────────────