  enum-int.toml :: intenum_calc
    Output mismatch

────────────────────────────────────────────────────────────
− #[repr(i32)]
−     #[derive(Debug, Clone, Copy)]
−     enum Priority {
−         Low = 1,
−         Medium = 2,
−         High = 3,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Priority {}
+ impl Priority {
+     pub const LOW: i32 = 1;
+     pub const MEDIUM: i32 = 2;
+     pub const HIGH: i32 = 3;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let total = (Priority::Low as i32) + (Priority::Medium as i32) + (Priority::High as i32);
−     assert_eq!(total, 6);
+ }
────────────────────────────────────────────────────────────