  async-comprehensions.toml :: await_in_comp
    Output mismatch

────────────────────────────────────────────────────────────
− async fn fetch(x: i32) -> i32 {
−     x * 2
− }
− 
− #[tokio::main]
− async let items = vec![1, 2, 3];
− let mut result = Vec::new();
− for x in items {
−     result.push(fetch(x).await);
+ use serde_json;
+ pub async fn fetch(x: i32) -> i32 {
+     return x * 2;
+    
  }
− log::info!("{:?}", result);
+ pub async fn test() -> Vec<serde_json::Value>{
+     let items = vec! [1, 2, 3];
+     let result = items.iter().cloned().map(| x | fetch(x).await).collect::<Vec<_>>();
+     return result;
+     }
────────────────────────────────────────────────────────────