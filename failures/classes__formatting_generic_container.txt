  classes.toml :: formatting_generic_container
    Output mismatch

────────────────────────────────────────────────────────────
− use serde_json;
  #[derive(Debug, Clone)]
  pub struct Container {
      pub value: T,
  }
  impl Container {
      pub fn new(value: T) -> Self {
          Self { value }
      }
      pub fn get(&self) -> T {
          return self.value.clone();
      }
  }
────────────────────────────────────────────────────────────