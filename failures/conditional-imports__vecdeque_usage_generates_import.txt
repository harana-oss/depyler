  conditional-imports.toml :: vecdeque_usage_generates_import
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::VecDeque;
− pub fn create_deque() -> VecDeque<i32> {
−     let mut d: VecDeque<i32> = VecDeque::new();
−     d.push_back(1);
+ pub fn create_deque() -> deque {
+     let mut d = VecDeque::new();
+     d.push(1);
      return d;
  }
────────────────────────────────────────────────────────────