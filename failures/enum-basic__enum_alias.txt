  enum-basic.toml :: enum_alias
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't support enum aliases directly
−     // All variants must have unique discriminants or be unit variants
−     #[derive(Debug, PartialEq)]
−     enum Color {
−         RED = 1,
+ pub const result: serde_json::Value = Color::RED == Color::CRIMSON;
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const CRIMSON: i32 = 1;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     // Type alias can refer to the same variant
−     type Crimson = Color;
− 
−     let result = true;
+ }
────────────────────────────────────────────────────────────