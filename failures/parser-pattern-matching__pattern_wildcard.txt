  parser-pattern-matching.toml :: pattern_wildcard
    Output mismatch

────────────────────────────────────────────────────────────
− fn classify(x: i64) -> String {
−     match x {
−         0 => "zero".to_string(),
−         _ => "nonzero".to_string(),
+ pub fn classify(x: i32) -> String {
+     if x == 0 {
+         return "zero".to_string();
+     } else {
+         return "nonzero".to_string();
      }
− }
− 
− fn main() {
−     let result = classify(42);
−     println!("{}", result);
  }
────────────────────────────────────────────────────────────