  lifetimes.toml :: owned_param_field_return_needs_clone
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct Container {
      pub items: Vec<i32>,
  }
  impl Container {
      pub fn new(items: Vec<i32>) -> Self {
          Self { items }
      }
  }
  pub fn take_ownership_and_return_field(container: &Container) -> Vec<i32> {
−     return container.items.clone();
+     return container.items;
  }
────────────────────────────────────────────────────────────