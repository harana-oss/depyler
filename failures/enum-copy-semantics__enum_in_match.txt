  enum-copy-semantics.toml :: enum_in_match
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, Copy, PartialEq)]
− pub enum Direction {
−     UP = 1,
−     DOWN = 2,
−     LEFT = 3,
−     RIGHT = 4,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Direction {}
+ impl Direction {
+     pub const UP: i32 = 1;
+     pub const DOWN: i32 = 2;
+     pub const LEFT: i32 = 3;
+     pub const RIGHT: i32 = 4;
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− pub fn get_delta(d: Direction) -> (i32, i32) {
+ pub fn get_delta(d: &Direction) -> (i32, i32) {
      if d == Direction::UP {
          return (0, 1);
−     } else if d == Direction::DOWN {
−         return (0, -1);
−     } else if d == Direction::LEFT {
−         return (-1, 0);
      } else {
−         return (1, 0);
+         if d == Direction::DOWN {
+             return (0, -1);
+         } else {
+             if d == Direction::LEFT {
+                 return (-1, 0);
+             } else {
+                 return (1, 0);
+             }
+         }
      }
  }
────────────────────────────────────────────────────────────