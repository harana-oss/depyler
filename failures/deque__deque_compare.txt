  deque.toml :: deque_compare
    Output mismatch

────────────────────────────────────────────────────────────
− // deque([1,2]) == deque([1,2])
−     use std::collections::VecDeque;
− 
−     let d1 = VecDeque::from(vec![1, 2]);
−     let d2 = VecDeque::from(vec![1, 2]);
−     let d3 = VecDeque::from(vec![1, 3]);
−     let result = d1 == d2 && d1 != d3;
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────