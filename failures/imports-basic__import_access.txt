  imports-basic.toml :: import_access
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust: accessing function from std
−     use std::env;
−     let cwd = env::current_dir().unwrap();
−     let result = cwd.to_str().is_some();
−     assert!(result);
+ use std as os;
────────────────────────────────────────────────────────────