  error-handling.toml :: conditional_error_raising
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn conditional_error(x: i32, strict: bool) -> i32 {
−     let _cse_temp_0 = x < 0;
−     let _cse_temp_1 = (strict) && (_cse_temp_0);
−     if _cse_temp_1 {
−         panic!("{}", "strict mode: negative not allowed");
+ pub fn conditional_error(x: i32, strict: bool) -> Result<i32, ValueError> {
+     let _cse_temp_0 = (strict) && (x < 0);
+     if _cse_temp_0 {
+         return Err(ValueError::new("strict mode: negative not allowed"));
      } else {
−         let _cse_temp_2 = x < -100;
−         if _cse_temp_2 {
−             panic!("{}", "value too small");
+         if x < -100 {
+             return Err(ValueError::new("value too small"));
          }
      }
−     return x;
+     return Ok(x);
  }
────────────────────────────────────────────────────────────