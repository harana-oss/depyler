  cse-optimization.toml :: multiple_period_checks
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Clone)]
  pub struct Period {
      pub name: String,
  }
− 
+ #[derive(Debug, Clone)]
  pub struct State {
      pub period: Period,
  }
− 
  pub fn handle_period(state: &State) -> String {
      if state.period.name == "FirstHalf" {
          return "first".to_string();
−     } else if state.period.name == "SecondHalf" {
−         return "second".to_string();
−     } else if state.period.name == "ExtraTime" {
−         return "extra".to_string();
+     } else {
+         if state.period.name == "SecondHalf" {
+             return "second".to_string();
+         } else {
+             if state.period.name == "ExtraTime" {
+                 return "extra".to_string();
+             }
+         }
      }
      return "unknown".to_string();
  }
────────────────────────────────────────────────────────────