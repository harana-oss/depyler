  asyncio-tasks.toml :: asyncio_event
    Output mismatch

────────────────────────────────────────────────────────────
− use std::sync::Arc;
− use tokio::sync::Notify;
− 
  pub async fn test_asyncio_event() -> bool {
−     let notify = Arc::new(Notify::new());
−     notify.notify_one();
−     notify.notified().await;
−     true
− }
+     let event = asyncio.Event();
+     event.set();
+     event.wait().await;
+     return event.is_set();
+     }
────────────────────────────────────────────────────────────