  dunder-reflected.toml :: reflected_format
    Output mismatch

────────────────────────────────────────────────────────────
− use std::fmt;
− 
− struct Person {
−     name: String,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Person {
+     pub name: serde_json::Value,
  }
− 
− impl fmt::Display for Person {
−     fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
−         write!(f, "{}", self.name)
+ impl Person {
+     pub fn new(name: serde_json::Value) -> Self {
+         Self { name }
      }
  }
− 
− let p = Person {
−     name: "Alice".to_string(),
− };
− let result = format!("Hello, {}!", p);
− log::info!("{}", result);
────────────────────────────────────────────────────────────