  keyword-only-args.toml :: kwonly_with_kwargs
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
− 
− fn kwonly_kwargs(x: i32, kwargs: &HashMap<&str, i32>) {
−     log::info!("x={}", x);
−     for (k, v) in kwargs {
−         log::info!("{}={}", k, v);
+ pub fn kwonly_kwargs() {
+     log::info!("{}", format!("x={}", x));
+     for (k, v) in kwargs
+         .iter()
+         .map(|(k, v)| (k.clone(), v.clone()))
+         .collect::<Vec<_>>()
+     {
+         log::info!("{}", format!("{}={}", k, v));
      }
  }
− 
− let mut kwargs = HashMap::new();
− kwargs.insert("a", 1);
− kwargs.insert("b", 2);
− kwonly_kwargs(5, &kwargs);
────────────────────────────────────────────────────────────