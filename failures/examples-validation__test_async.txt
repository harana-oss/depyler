  examples-validation.toml :: test_async
    Output mismatch

────────────────────────────────────────────────────────────
− #[doc = "Fetch data asynchronously."]
− pub async fn fetch_data(url: String) -> String {
−     let result = async_fetch(url).await;
−     result
+ #[doc = "Fetch data asynchronously."] pub async fn fetch_data(url: String) -> String {
+     let result = async_fetch(& url).await;
+     return result;
+    
  }
− #[doc = "Process items asynchronously."]
− pub async fn process_batch(items: &Vec<String>) -> Vec<String> {
−     let mut results = vec![];
+ #[doc = "Process items asynchronously."] pub async fn process_batch(items: & Vec<String>) -> Vec<String>{
+     let mut results = vec! [];
      for _item in items.iter().cloned() {
−         let data = fetch_data(item).await;
−         results.push(data);
−     }
−     results
− }
+     let data = fetch_data(item).await;
+     results.push(data);
+    
+ }
+ return results;
+     }
────────────────────────────────────────────────────────────