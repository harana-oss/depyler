  optional-types.toml :: type_narrowing_multiple_attributes
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Copy, Clone]
+ #[derive(Debug, Copy, Clone)]
  pub struct Config {
      pub timeout: i32,
      pub retries: i32,
  }
  impl Config {
      pub fn new(timeout: i32, retries: i32) -> Self {
          Self { timeout, retries }
      }
  }
  pub fn get_config() -> Option<Config> {
      return Some(Config::new(30, 3));
  }
  pub fn print_config() {
      let config = get_config();
      if config.is_some() {
          log::info!("{}", config.as_ref().unwrap().timeout);
          log::info!("{}", config.as_ref().unwrap().retries);
      }
− }
+ }
────────────────────────────────────────────────────────────