  field-alias-mutation.toml :: field_alias_attribute_mutation
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, PartialEq, Default)]
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
  pub struct Stats {
      pub score: i32,
  }
  impl Stats {
      pub fn new(score: i32) -> Self {
          Self { score }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct State {
      pub home_statistics: Stats,
  }
  impl State {
      pub fn new(home_statistics: Stats) -> Self {
          Self { home_statistics }
      }
  }
− pub fn update_stats(state: &mut State) {
−     let mut team_stats = state.home_statistics.clone();
+ pub fn update_stats(state: &State) {
+     let mut team_stats = state.home_statistics;
      team_stats.score = 10;
  }
────────────────────────────────────────────────────────────