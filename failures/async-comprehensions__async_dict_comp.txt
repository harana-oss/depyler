  async-comprehensions.toml :: async_dict_comp
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
+ use serde_json;
  use std::collections::HashMap;
− 
− #[tokio::main]
− async let items = vec![("a", 1), ("b", 2), ("c", 3)];
− let result: HashMap<&str, i32> = stream::iter(items).collect().await;
− log::info!("{:?}", result);
+     #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncItemsState {
+     state: usize, items: Vec<serde_json::Value>
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_items() -> impl Iterator<Item = i32>{
+     AsyncItemsState {
+     state: 0, items: Default::default()
+ }
+ } impl Iterator for AsyncItemsState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     self.items = vec! [("a", 1) ,("b", 2) ,("c", 3)];
+     for _item in self.items.iter().cloned() {
+     return Some(item);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test() -> HashMap<serde_json::Value, serde_json::Value>{
+     let result = async_items().iter().cloned().map(|(k, v) |(k, v)).collect::<HashMap<_, _>>();
+     return result;
+     }
────────────────────────────────────────────────────────────