  error-handling.toml :: raise_with_concrete_error
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn check_bounds(x: i32, low: i32, high: i32) -> i32 {
+ pub fn check_bounds(x: i32, low: i32, high: i32) -> Result<i32, ValueError> {
      if x < low {
−         panic!("{}", "below minimum");
+         return Err(ValueError::new("below minimum"));
      }
      if x > high {
−         panic!("{}", "above maximum");
+         return Err(ValueError::new("above maximum"));
      }
−     return x;
+     return Ok(x);
  }
────────────────────────────────────────────────────────────