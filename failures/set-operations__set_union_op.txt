  set-operations.toml :: set_union_op
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: HashSet<i32> = a
−     .clone()
−     .union(&b.clone())
−     .cloned()
−     .collect::<std::collections::HashSet<_>>();
− lazy_static! {
−     pub static ref a: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(1);
−         set.insert(2);
−         set.insert(3);
−         set
−     };
−     pub static ref b: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(3);
−         set.insert(4);
−         set.insert(5);
−         set
−     };
− }
− use lazy_static::lazy_static;
+ pub const a: serde_json::Value = {
+     let mut set = HashSet::new();
+     set.insert(1);
+     set.insert(2);
+     set.insert(3);
+     set
+ };
+ pub const b: serde_json::Value = {
+     let mut set = HashSet::new();
+     set.insert(3);
+     set.insert(4);
+     set.insert(5);
+     set
+ };
+ pub const result: serde_json::Value = a | b;
  use serde_json;
  use std::collections::HashSet;
────────────────────────────────────────────────────────────