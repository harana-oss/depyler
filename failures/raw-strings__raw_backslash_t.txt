  raw-strings.toml :: raw_backslash_t
    Output mismatch

────────────────────────────────────────────────────────────
− let raw = r"\t";
−     let normal = "\t";
−     let result = (raw.len() == 2 && normal.len() == 1);
+ pub const raw: &str = "\\t";
+ pub const normal: &str = "\t";
+ pub const result: serde_json::Value = (raw.len() as i32 == 2) && (normal.len() as i32 == 1);
+ use serde_json;
────────────────────────────────────────────────────────────