  set-operations.toml :: test_set_comprehensions
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashSet;
  #[doc = "Basic set comprehension"]
  pub fn test_simple_set_comp() -> HashSet<i32> {
−     (0..5).map(|x| x * 2).collect::<HashSet<_>>()
+     return (0..5).map(|x| x * 2).collect::<HashSet<_>>();
  }
  #[doc = "Set comprehension with condition"]
  pub fn test_set_comp_with_condition() -> HashSet<i32> {
−     (0..10)
−         .filter(|x| x % 2 == 0)
−         .map(|x| x)
−         .collect::<HashSet<_>>()
+     return (0..10).filter(|x| x % 2 == 0).collect::<HashSet<_>>();
  }
  #[doc = "Set comprehension from list"]
  pub fn test_set_comp_from_list() -> HashSet<String> {
      let words = vec![
          "hello".to_string(),
          "world".to_string(),
          "hello".to_string(),
          "python".to_string(),
      ];
−     words
+     return words
          .iter()
          .cloned()
          .map(|w| w.to_uppercase())
−         .collect::<HashSet<_>>()
+         .collect::<HashSet<_>>();
  }
  #[doc = "Set comprehension with complex expression"]
  pub fn test_set_comp_complex_expr() -> HashSet<i32> {
      let numbers = vec![1, 2, 3, 4, 5];
−     numbers
+     return numbers
          .iter()
          .filter(|&x| x < 4)
          .cloned()
          .map(|x| x * x + 1)
−         .collect::<HashSet<_>>()
+         .collect::<HashSet<_>>();
  }
  #[doc = "Set comprehension with expression"]
  pub fn test_set_comp_with_expression() -> HashSet<i32> {
−     (0..5)
+     return (0..5)
          .filter(|x| x > 0)
          .map(|x| x * x)
−         .collect::<HashSet<_>>()
+         .collect::<HashSet<_>>();
  }
────────────────────────────────────────────────────────────