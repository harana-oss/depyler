  dataclasses-advanced.toml :: dataclass_fields
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− struct Point {
−     x: i32,
−     y: i32,
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Point {
+     pub x: i32,
+     pub y: i32,
+ }
+ impl Point {
+     pub fn new(x: i32, y: i32) -> Self {
+         Self { x, y }
+     }
  }
− 
− pub fn test_dataclass_fields() -> usize {
−     2 // x and y fields
+ pub fn test_dataclass_fields() -> i32 {
+     return fields(Point).len() as i32 as i32;
  }
────────────────────────────────────────────────────────────