  type-inference.toml :: int_str_multiple_calls
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn parse_and_add(s1: String, s2: String) -> i32 {
−     let _cse_temp_0 = s1.parse::<i32>().unwrap();
−     let a = _cse_temp_0;
−     let _cse_temp_1 = s2.parse::<i32>().unwrap();
−     let b = _cse_temp_1;
+     let a = s1.parse::<i32>().unwrap();
+     let b = s2.parse::<i32>().unwrap();
      return a + b;
  }
────────────────────────────────────────────────────────────