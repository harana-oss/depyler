  metaclasses.toml :: metaclass_new
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, we use a static registry for class tracking
− use std::sync::Mutex;
− 
− static TRACKED_CLASSES: Mutex<Vec<&str>> = Mutex::new(Vec::new());
− 
− struct A;
− struct B;
− 
− fn register_class(name: &'static str) {
−     TRACKED_CLASSES.lock().unwrap().push(name);
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct TrackedMeta {}
+ impl TrackedMeta {
+     pub const _classes: Vec<serde_json::Value> = vec![];
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct A {}
+ impl A {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct B {}
+ impl B {
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− 
− register_class("A");
− register_class("B");
− log::info!("{:?}", *TRACKED_CLASSES.lock().unwrap());
────────────────────────────────────────────────────────────