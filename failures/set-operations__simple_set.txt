  set-operations.toml :: simple_set
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashSet;
  #[doc = "Test just set literal"]
  pub fn test_simple_set() -> HashSet<i32> {
      let s = {
          let mut set = HashSet::new();
          set.insert(1);
          set.insert(2);
          set.insert(3);
          set
      };
−     s
+     return s;
  }
────────────────────────────────────────────────────────────