  format-spec.toml :: format_underscore
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn format_underscore_separator() -> String {
−     let n = 1000000;
−     let s = n.to_string();
−     let mut result = String::new();
−     for (i, c) in s.chars().rev().enumerate() {
−         if i > 0 && i % 3 == 0 {
−             result.push('_');
−         }
−         result.push(c);
−     }
−     return result.chars().rev().collect();
+     return format!("{:_}", 1000000);
  }
────────────────────────────────────────────────────────────