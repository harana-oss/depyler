  del-statement.toml :: del_multiple_variables
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust: multiple variables go out of scope
−     {
−         let a = 1;
−         let b = 2;
−         let c = 3;
−         assert_eq!(a, 1);
−         assert_eq!(b, 2);
−         assert_eq!(c, 3);
−         drop(a);
−         drop(b);
−         drop(c);
−     }
+ 
────────────────────────────────────────────────────────────