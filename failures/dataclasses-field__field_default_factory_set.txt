  dataclasses-field.toml :: field_default_factory_set
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashSet;
−     #[derive(Debug)]
−     struct State {
−         executed_events: HashSet<String>,
−     }
− 
−     impl State {
−         fn new() -> Self {
−             State {
−                 executed_events: HashSet::new(),
−             }
+ #[derive(Debug, Clone, PartialEq, Default)]
+ pub struct State {
+     pub executed_events: HashSet<String>,
+ }
+ impl State {
+     pub fn new() -> Self {
+         Self {
+             executed_events: HashSet::new(),
          }
      }
− 
−     let mut s1 = State::new();
−     let s2 = State::new();
−     s1.executed_events.insert("event1".to_string());
−     let result = s1.executed_events.len() == 1 && s2.executed_events.len() == 0;
+ }
────────────────────────────────────────────────────────────