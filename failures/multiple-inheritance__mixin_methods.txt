  multiple-inheritance.toml :: mixin_methods
    Output mismatch

────────────────────────────────────────────────────────────
− // Mixin adds functionality to class
−     trait LogMixin {
−         fn log(&self, message: &str) -> String {
−             format!("LOG: {}", message)
−         }
+ #[derive(Debug, Copy, Clone)]
+ pub struct LogMixin {}
+ impl LogMixin {
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     struct LoggingCalculator;
− 
−     impl LogMixin for LoggingCalculator {}
− 
−     impl LoggingCalculator {
−         fn add(&self, a: i32, b: i32) -> i32 {
−             let result = a + b;
−             self.log(&format!("Added {} + {} = {}", a, b, result));
−             result
−         }
+     pub fn log(&self, message: String) {
+         return format!("LOG: {}", message);
      }
− 
−     pub fn test_mixin_methods() -> bool {
−         let calc = LoggingCalculator;
−         calc.add(2, 3) == 5 && calc.log("test") == "LOG: test"
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Calculator {}
+ impl Calculator {
+     pub fn new() -> Self {
+         Self {}
+     }
+     pub fn add(&self, a: i32, b: i32) {
+         return a + b;
      }
− 
−     let result = test_mixin_methods();
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct LoggingCalculator {}
+ impl LoggingCalculator {
+     pub fn new() -> Self {
+         Self {}
+     }
+     pub fn add(&self, a: String, b: String) {
+         let mut result = { self.add(a, b) };
+         self.log(format!("Added {} + {} = {}", a, b, result));
+         return result;
+     }
+ }
────────────────────────────────────────────────────────────