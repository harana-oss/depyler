  argparse.toml :: argument_type_error_exception
    Output mismatch

────────────────────────────────────────────────────────────
  use clap::Parser;
  #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[derive(Debug, Clone)]
  pub struct ArgumentTypeError {
      message: String,
  }
  impl std::fmt::Display for ArgumentTypeError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "argument type error: {}", self.message)
      }
  }
  impl std::error::Error for ArgumentTypeError {}
  impl ArgumentTypeError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn validate_int(value: String) -> i32 {
+ pub fn validate_int(value: String) -> Result<i32, Box<dyn std::error::Error>> {
      {
          let num = value.parse::<i32>().unwrap();
−         return num;
−         panic!("{}", format!("Must be integer, got {}", value));
+         return Ok(num);
+         return Err(Box::new(ArgumentTypeError::new(format!(
+             "Must be integer, got {}",
+             value
+         ))));
      }
  }
────────────────────────────────────────────────────────────