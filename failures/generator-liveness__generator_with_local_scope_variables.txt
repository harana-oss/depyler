  generator-liveness.toml :: generator_with_local_scope_variables
    Output mismatch

────────────────────────────────────────────────────────────
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct ZeroDivisionError {
+     message: String,
+ }
+ impl std::fmt::Display for ZeroDivisionError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "division by zero: {}", self.message)
+     }
+ }
+ impl std::error::Error for ZeroDivisionError {}
+ impl ZeroDivisionError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
  #[doc = " Generator state struct"]
  #[derive(Debug)]
  struct AlternatingState {
+     state: usize,
      i: i32,
−     val: i32,
+     val: serde_json::Value,
      n: i32,
−     state: usize,
  }
  #[doc = " Generator function - returns Iterator"]
  pub fn alternating(n: i32) -> impl Iterator<Item = i32> {
−     let i = 0;
      AlternatingState {
−         i,
−         val: 0,
−         n,
          state: 0,
+         i: 0,
+         val: Default::default(),
+         n: n,
      }
  }
  impl Iterator for AlternatingState {
      type Item = i32;
      fn next(&mut self) -> Option<Self::Item> {
          match self.state {
              0 => {
−                 self.state = 1usize;
+                 self.i = 0;
+                 self.state = 1;
+                 self.next()
              }
−             _ => {}
−         }
−         loop {
−             if self.i < self.n {
−                 if self.i % 2 == 0 {
−                     self.val = self.i;
+             1 => {
+                 if self.i < self.n {
+                     let result = self.val;
+                     let mut val;
+                     if self.i % 2 == 0 {
+                         self.val = self.i;
+                     } else {
+                         self.val = (-self.i);
+                     }
+                     self.i = self.i + 1;
+                     return Some(result);
                  } else {
−                     self.val = -self.i;
+                     self.state = 2;
+                     None
                  }
−                 let result = self.val;
−                 self.i = self.i + 1;
−                 return Some(result);
−             } else {
−                 return None;
              }
+             _ => None,
          }
      }
  }
────────────────────────────────────────────────────────────