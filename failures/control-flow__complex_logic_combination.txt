  control-flow.toml :: complex_logic_combination
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn complex_logic(items: &Vec<i32>) -> i32 {
−     let mut total = 0;
+ pub fn complex_logic(items: &Vec<serde_json::Value>) -> i32 {
+     let total = 0;
      {
          for item in items.iter().cloned() {
−             if item.clone() > 0 {
−                 total += item.clone();
+             if item > 0 {
+                 total += item;
              } else {
                  if item < 0 {
                      continue;
                  } else {
                      break;
                  }
              }
          }
          if total > 100 {
              panic!("{}", "too large");
          }
          return total;
          return -1;
      }
  }
────────────────────────────────────────────────────────────