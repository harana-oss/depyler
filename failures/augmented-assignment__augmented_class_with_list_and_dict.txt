  augmented-assignment.toml :: augmented_class_with_list_and_dict
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  use std::collections::HashMap;
  #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[derive(Debug, Clone)]
  pub struct Counter {
      pub count: i32,
−     pub values: Vec<serde_json::Value>,
+     pub values: Vec<i32>,
  }
  impl Counter {
      pub fn new() -> Self {
          Self {
              count: 0,
−             values: [1, 2, 3, 4, 5],
+             values: vec![1, 2, 3, 4, 5],
          }
      }
      pub fn increment(&mut self) {
          self.count += 1;
      }
      pub fn add_to_index(&self, index: i32, value: i32) {
−         self.values
−             .insert(index, format!("{}{}", self.values[index as usize], value));
+         self.values[index as usize] += value;
      }
  }
− pub fn test_compound_ops() -> (serde_json::Value, serde_json::Value, i32) {
+ pub fn test_compound_ops() -> Result<(serde_json::Value, i32, i32), IndexError> {
      let mut c = Counter::new();
      c.increment();
      c.add_to_index(0, 10);
      let mut d = {
          let mut map = HashMap::new();
          map.insert("score".to_string(), 100);
          map
      };
−     d.clone()
−         .insert("score".to_string(), d.get("score").cloned().unwrap() + 50);
−     return (
+     {
+         let _key = "score".to_string();
+         let _old_val = d.get(&_key).cloned().unwrap_or_default();
+         d.insert(_key, _old_val + 50);
+     }
+     return Ok((
          c.count,
          c.values.get(0usize).cloned().unwrap(),
          d.get("score").cloned().unwrap(),
−     );
+     ));
  }
− let result = test_compound_ops();
− log::info!("{}", result.get(0usize).cloned().unwrap());
− log::info!("{}", result.get(1usize).cloned().unwrap());
− log::info!("{}", result.get(2usize).cloned().unwrap());
────────────────────────────────────────────────────────────