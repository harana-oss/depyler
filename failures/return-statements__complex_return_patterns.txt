  return-statements.toml :: complex_return_patterns
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  #[doc = "Complex function with multiple return patterns."]
− pub fn complex_logic(items: &Vec<i32>, threshold: i32) -> Option<i32> {
+ pub fn complex_logic(items: &Vec<i32>, threshold: i32) -> Result<Option<i32>, ValueError> {
      if items.len() as i32 == 0 {
−         panic!("{}", "Empty list");
+         return Err(ValueError::new("Empty list"));
      }
      if threshold < 0 {
−         return None;
+         return Ok(None);
      }
      for item in items.iter().cloned() {
          if item > threshold {
−             return Some(item);
+             return Ok(Some(item));
          }
      }
−     return None;
+     return Ok(None);
  }
────────────────────────────────────────────────────────────