  error-handling.toml :: raise_in_nested_if
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn complex_validation(x: i32, y: i32) -> i32 {
+ pub fn complex_validation(x: i32, y: i32) -> Result<i32, ValueError> {
      if x > 0 {
          if y < 0 {
−             panic!("{}", "Invalid combination");
+             return Err(ValueError::new("Invalid combination"));
          }
−         return x + y;
+         return Ok(x + y);
      }
−     panic!("{}", "x must be positive");
+     return Err(ValueError::new("x must be positive"));
  }
────────────────────────────────────────────────────────────