  string-encoding.toml :: string_contains
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn contains_substring(haystack: &str, needle: &str) -> bool {
−     return haystack.contains(needle);
+ pub fn contains_substring(haystack: String, needle: String) -> bool {
+     return haystack.contains(&needle);
  }
────────────────────────────────────────────────────────────