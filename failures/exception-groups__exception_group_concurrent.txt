  exception-groups.toml :: exception_group_concurrent
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− enum ProcessError {
−     Negative(i32),
−     Zero,
+ #[derive(Debug, Clone)]
+ pub struct ZeroDivisionError {
+     message: String,
  }
− 
− fn process_item(item: i32) -> Result<i32, ProcessError> {
−     if item < 0 {
−         Err(ProcessError::Negative(item))
−     } else if item == 0 {
−         Err(ProcessError::Zero)
−     } else {
−         Ok(item * 2)
+ impl std::fmt::Display for ZeroDivisionError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "division by zero: {}", self.message)
      }
  }
− 
− let items = vec![-1, 0, 5];
− let mut errors = Vec::new();
− 
− for item in items {
−     if let Err(e) = process_item(item) {
−         errors.push(e);
+ impl std::error::Error for ZeroDivisionError {}
+ impl ZeroDivisionError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
      }
  }
− 
− if !errors.is_empty() {
−     log::info!("Processing errors: {:?}", errors);
− }
+ #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn process_item(item: i32) -> Result<i32, Box<dyn std::error::Error>> {
+     if item < 0 {
+         return Err(Box::new(ValueError::new(format!("Negative: {}", item))));
+     }
+     if item == 0 {
+         return Err(Box::new(ZeroDivisionError::new("Cannot be zero")));
+     }
+     return Ok(item * 2);
+ }
────────────────────────────────────────────────────────────