  match-as-patterns.toml :: as_scope
    Output mismatch

────────────────────────────────────────────────────────────
− // as bindings available in guard and body
−     let value = vec![1, 2, 3];
−     let result = match value.as_slice() {
−         lst @ [items @ ..] if lst.len() > 0 => items.len() == 3 && lst == &[1, 2, 3],
−         _ => false,
−     };
+ 
────────────────────────────────────────────────────────────