  performance-patterns.toml :: bounds_check_elimination
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn safe_access(items: &Vec<i32>, idx: i32) -> i32 {
−     if 0 <= idx && (idx as usize) < items.len() {
−         return items[idx as usize];
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
      }
−     return 0;
  }
+ pub fn safe_access(items: &Vec<i32>, idx: i32) -> Result<i32, IndexError> {
+     let _cse_temp_0 = (0 <= idx) && (idx < items.len() as i32);
+     if _cse_temp_0 {
+         return Ok(items.get(idx as usize).cloned().unwrap());
+     }
+     return Ok(0);
+ }
────────────────────────────────────────────────────────────