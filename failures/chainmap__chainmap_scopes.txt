  chainmap.toml :: chainmap_scopes
    Output mismatch

────────────────────────────────────────────────────────────
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_scopes() -> i32 {
−     let mut global_vars = HashMap::new();
−     global_vars.insert("x".to_string(), 1);
− 
−     let mut enclosing_vars = HashMap::new();
−     enclosing_vars.insert("x".to_string(), 2);
−     enclosing_vars.insert("y".to_string(), 3);
− 
−     let mut local_vars = HashMap::new();
−     local_vars.insert("x".to_string(), 4);
− 
−     let scopes = vec![local_vars, enclosing_vars, global_vars];
− 
−     for map in &scopes {
−         if let Some(&value) = map.get("x") {
−             return value;
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
          }
      }
−     panic!("Key not found");
  }
+ pub fn test_chainmap_scopes() -> Result<i32, IndexError> {
+     let global_vars = {
+         let mut map = HashMap::new();
+         map.insert("x".to_string(), 1);
+         map
+     };
+     let enclosing_vars = {
+         let mut map = HashMap::new();
+         map.insert("x".to_string(), 2);
+         map.insert("y".to_string(), 3);
+         map
+     };
+     let local_vars = {
+         let mut map = HashMap::new();
+         map.insert("x".to_string(), 4);
+         map
+     };
+     let scopes = ChainMap::new(local_vars, enclosing_vars, global_vars);
+     return Ok(scopes.get("x").cloned().unwrap());
+ }
────────────────────────────────────────────────────────────