  cli-integration.toml :: wordcount
    Output mismatch

────────────────────────────────────────────────────────────
  use clap::Parser;
  use std as sys;
  use std::path::PathBuf;
  #[derive(Debug, Clone)]
  pub struct Stats {
      pub lines: i32,
      pub words: i32,
      pub chars: i32,
      pub filename: String,
  }
− #[doc = "Count statistics for a single file"]
− pub fn count_file(filepath: &Path) -> Stats {
−     {
−         let content = std::fs::read_to_string(filepath).unwrap();
−         let lines = content
−             .lines()
−             .map(|s| s.to_string())
−             .collect::<Vec<String>>()
−             .len() as i32;
−         let words = content
−             .split_whitespace()
−             .map(|s| s.to_string())
−             .collect::<Vec<String>>()
−             .len() as i32;
−         let chars = content.len() as i32;
−         return Stats::new(lines, words, chars, filepath.to_string());
−         log::info!("{}", format!("Error reading {:?}: {:?}", filepath, e));
−         return Stats::new(0, 0, 0, filepath.to_string());
−     }
− }
− #[doc = "Format statistics for output"]
− pub fn format_stats(stats: Stats, show_filename: bool) -> String {
−     let mut result = format!("{:?} {:?} {:?}", stats.lines, stats.words, stats.chars);
−     if show_filename {
−         let _cse_temp_0 = format!("{}{}", result, format!(" {:?}", stats.filename));
−         result = _cse_temp_0;
−     }
−     result
− }
  #[derive(Debug, clap::Parser)]
  #[command(about = "Count lines, words, and characters in files")]
  #[command(after_help = "Similar to wc(1) Unix command")]
  struct Args {
      #[doc = "Files to process"]
      files: Vec<PathBuf>,
      #[arg(short = 'l', long)]
      #[arg(action = clap::ArgAction::SetTrue)]
      #[doc = "Show only line count"]
      lines: bool,
      #[arg(short = 'w', long)]
      #[arg(action = clap::ArgAction::SetTrue)]
      #[doc = "Show only word count"]
      words: bool,
      #[arg(short = 'c', long)]
      #[arg(action = clap::ArgAction::SetTrue)]
      #[doc = "Show only character count"]
      chars: bool,
  }
+ #[doc = "Count statistics for a single file"]
+ pub fn count_file(filepath: &Path) -> Stats {
+     let content = std::fs::read_to_string(filepath).unwrap();
+     let lines = content
+         .lines()
+         .map(|s| s.to_string())
+         .collect::<Vec<String>>()
+         .len() as i32;
+     let words = content
+         .split_whitespace()
+         .map(|s| s.to_string())
+         .collect::<Vec<String>>()
+         .len() as i32;
+     let chars = content.len() as i32;
+     return Stats::new(lines, words, chars, (filepath).to_string());
+ }
+ #[doc = "Format statistics for output"]
+ pub fn format_stats(stats: &Stats, show_filename: bool) -> String {
+     let mut result = format!("{} {} {}", stats.lines, stats.words, stats.chars);
+     if show_filename {
+         result += format!(" {}", stats.filename);
+     }
+     return result;
+ }
  #[doc = "Main entry point"]
  pub fn main() -> i32 {
      let args = Args::parse();
      let mut total_lines = 0;
      let mut total_words = 0;
      let mut total_chars = 0;
−     for filepath in args.files {
−         let stats = count_file(filepath);
+     for filepath in &args.files {
+         let filepath = filepath.clone();
+         let stats = count_file(&filepath);
          total_lines += stats.lines;
          total_words += stats.words;
          total_chars += stats.chars;
          if args.lines {
−             log::info!("{}", format!("{:?} {:?}", stats.lines, stats.filename));
+             log::info!("{}", format!("{} {}", stats.lines, stats.filename));
          } else {
              if args.words {
−                 log::info!("{}", format!("{:?} {:?}", stats.words, stats.filename));
+                 log::info!("{}", format!("{} {}", stats.words, stats.filename));
              } else {
                  if args.chars {
−                     log::info!("{}", format!("{:?} {:?}", stats.chars, stats.filename));
+                     log::info!("{}", format!("{} {}", stats.chars, stats.filename));
                  } else {
−                     log::info!("{}", format_stats(stats));
+                     log::info!("{}", format_stats(&stats));
                  }
              }
          }
      }
−     let _cse_temp_0 = args.files.len() as i32;
−     if _cse_temp_0 > 1 {
+     if args.files.len() as i32 > 1 {
          let total_stats = Stats::new(total_lines, total_words, total_chars, "total".to_string());
−         log::info!("{}", format_stats(total_stats));
+         log::info!("{}", format_stats(&total_stats, true));
      }
−     0
+     return 0;
  }
────────────────────────────────────────────────────────────