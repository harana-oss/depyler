  modules-random.toml :: secrets_token_hex
    Output mismatch

────────────────────────────────────────────────────────────
− use rand::Rng;
− use rand::prelude::*;
  pub fn random_hex(n: i32) -> String {
      return {
          let mut bytes = vec![0u8; n];
          rand::thread_rng().fill(&mut bytes[..]);
          hex::encode(&bytes)
−     }
+     };
+ }
────────────────────────────────────────────────────────────