  deque.toml :: deque_sliding
    Output mismatch

────────────────────────────────────────────────────────────
− // maxlen for fixed-size window
−     use std::collections::VecDeque;
− 
−     let mut window = VecDeque::new();
−     let maxlen = 3;
−     for i in 0..5 {
−         window.push_back(i);
−         if window.len() > maxlen {
−             window.pop_front();
−         }
−     }
−     let result = window.iter().cloned().collect::<Vec<_>>() == vec![2, 3, 4];
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────