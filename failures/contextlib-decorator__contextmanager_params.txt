  contextlib-decorator.toml :: contextmanager_params
    Output mismatch

────────────────────────────────────────────────────────────
− fn with_params<F, R>(prefix: &str, suffix: &str, f: F) -> R
−     where
−         F: FnOnce(String) -> R,
−     {
−         f(format!("{}_value_{}", prefix, suffix))
+ use serde_json;
+ #[doc = " Generator state struct"]
+ #[derive(Debug)]
+ struct WithParamsState {
+     state: usize,
+ }
+ #[doc = " Generator function - returns Iterator"]
+ pub fn with_params(
+     prefix: &serde_json::Value,
+     suffix: &serde_json::Value,
+ ) -> impl Iterator<Item = serde_json::Value> {
+     WithParamsState { state: 0 }
+ }
+ impl Iterator for WithParamsState {
+     type Item = serde_json::Value;
+     fn next(&mut self) -> Option<Self::Item> {
+         match self.state {
+             0 => {
+                 self.state = 1usize;
+                 return Some(format!("{}_value_{}", prefix, suffix));
+             }
+             1usize => {
+                 self.state = 1usize + 1;
+                 None
+             }
+             _ => None,
+         }
      }
− 
−     let result = with_params("start", "end", |value| value);
+ }
────────────────────────────────────────────────────────────