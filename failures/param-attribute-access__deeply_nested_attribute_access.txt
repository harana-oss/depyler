  param-attribute-access.toml :: deeply_nested_attribute_access
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, PartialEq, Default)]
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
  pub struct D {
      pub value: i32,
  }
  impl D {
      pub fn new(value: i32) -> Self {
          Self { value }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct C {
      pub d: D,
  }
  impl C {
      pub fn new(d: D) -> Self {
          Self { d }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct B {
      pub c: C,
  }
  impl B {
      pub fn new(c: C) -> Self {
          Self { c }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct A {
      pub b: B,
  }
  impl A {
      pub fn new(b: B) -> Self {
          Self { b }
      }
  }
− pub fn get_deep(a: &A) -> i32 {
+ pub fn get_deep<A: Clone>(a: A) -> i32 {
      return a.b.c.d.value;
  }
────────────────────────────────────────────────────────────