  concurrency.toml :: threading_mutex_guard
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Copy, Clone)]
  pub struct SafeValue {
      pub value: i32,
  }
− 
  impl SafeValue {
      pub fn new(value: i32) -> Self {
          Self { value }
      }
− 
      pub fn set(&mut self, new_value: i32) {
          self.value = new_value;
      }
− 
      pub fn get(&self) -> i32 {
−         return self.value;
+         return self.value.clone();
      }
  }
────────────────────────────────────────────────────────────