  borrow-checker-violations.toml :: lifetime_escape_struct
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Copy, Clone)]
  pub struct Container {
      pub value: i32,
  }
− 
  impl Container {
      pub fn new(value: i32) -> Self {
          Self { value }
      }
  }
− 
  pub fn create_container(value: i32) -> Container {
      return Container::new(value);
  }
────────────────────────────────────────────────────────────