  mutability.toml :: mut_nested_field_mutation
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Copy, Clone)]
  pub struct Inner {}
  impl Inner {
      pub fn new() -> Self {
          Self {}
      }
  }
  #[derive(Debug, Copy, Clone)]
  pub struct Outer {}
  impl Outer {
      pub fn new() -> Self {
          Self {}
      }
  }
  #[derive(Debug, Copy, Clone)]
  pub struct State {}
  impl State {
      pub fn new() -> Self {
          Self {}
      }
  }
  #[doc = "Test that nested field mutation properly marks the variable as mutable."]
  pub fn mutate_nested_field(state: &State, value: i32) {
−     let mut obj = state.data;
+     let obj = state.data;
      obj.inner.values.push(value);
  }
────────────────────────────────────────────────────────────