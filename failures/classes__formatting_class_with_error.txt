  classes.toml :: formatting_class_with_error
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct CustomError {
      pub message: String,
  }
  impl CustomError {
      pub fn new(message: String) -> Self {
          Self { message }
      }
  }
  #[derive(Debug, Clone)]
  pub struct DataProcessor {
      pub name: String,
      pub threshold: i32,
  }
  impl DataProcessor {
      pub fn new(name: String, threshold: i32) -> Self {
          Self { name, threshold }
      }
      pub fn process(&self, value: i32) -> i32 {
          if value < self.threshold {
              panic!(
                  "Exception: {}",
                  CustomError::new("Value below threshold".to_string())
              );
−         };
+         }
          return value * 2;
      }
  }
────────────────────────────────────────────────────────────