  dunder-context.toml :: with_statement
    Output mismatch

────────────────────────────────────────────────────────────
− struct Resource {
−     value: String,
− }
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct Resource {}
  impl Resource {
−     fn new() -> Self {
−         log::info!("Setup");
−         Resource {
−             value: "resource".to_string(),
−         }
+     pub fn new() -> Self {
+         Self {}
+     }
+     pub fn __enter__(&self) -> String {
+         log::info!("{}", "Setup".to_string());
+         return "resource".to_string();
      }
− }
− 
− impl Drop for Resource {
−     fn drop(&mut self) {
−         log::info!("Cleanup");
+     pub fn __exit__(&self) -> bool {
+         log::info!("{}", "Cleanup".to_string());
+         return false;
      }
  }
− 
− let res = Resource::new();
− log::info!("Using: {}", res.value);
────────────────────────────────────────────────────────────