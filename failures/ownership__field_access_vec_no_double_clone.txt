  ownership.toml :: field_access_vec_no_double_clone
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct GameState {
      pub home_players: Vec<String>,
  }
  impl GameState {
      pub fn new(home_players: Vec<String>) -> Self {
          Self { home_players }
      }
  }
  pub fn get_players(state: &GameState) -> Vec<String> {
−     return state.home_players.clone();
+     return state.home_players;
  }
────────────────────────────────────────────────────────────