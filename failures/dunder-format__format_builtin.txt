  dunder-format.toml :: format_builtin
    Output mismatch

────────────────────────────────────────────────────────────
− use std::fmt;
− 
− struct Temperature {
−     celsius: f64,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Temperature {
+     pub celsius: serde_json::Value,
  }
− 
  impl Temperature {
−     fn new(celsius: f64) -> Self {
−         Temperature { celsius }
+     pub fn new(celsius: serde_json::Value) -> Self {
+         Self { celsius }
      }
− 
−     fn format_spec(&self, spec: &str) -> String {
−         match spec {
−             "F" => format!("{:.1}°F", self.celsius * 9.0 / 5.0 + 32.0),
−             "K" => format!("{:.1}K", self.celsius + 273.15),
−             _ => format!("{:.1}°C", self.celsius),
−         }
−     }
  }
− 
− let temp = Temperature::new(25.0);
− log::info!("{}", temp.format_spec(""));
− log::info!("{}", temp.format_spec("F"));
− log::info!("{}", temp.format_spec("K"));
────────────────────────────────────────────────────────────