  conditional-imports.toml :: hashset_usage_generates_import
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashSet;
  pub fn create_set() -> HashSet<i32> {
−     return [1, 2, 3].into_iter().collect();
+     return {
+         let mut set = HashSet::new();
+         set.insert(1);
+         set.insert(2);
+         set.insert(3);
+         set
+     };
  }
────────────────────────────────────────────────────────────