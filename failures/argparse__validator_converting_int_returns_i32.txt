  argparse.toml :: validator_converting_int_returns_i32
    Output mismatch

────────────────────────────────────────────────────────────
  use clap::Parser;
  #[derive(Debug, Clone)]
  pub struct ArgumentTypeError {
      message: String,
  }
  impl std::fmt::Display for ArgumentTypeError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "argument type error: {}", self.message)
      }
  }
  impl std::error::Error for ArgumentTypeError {}
  impl ArgumentTypeError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  #[derive(Debug, clap::Parser)]
  struct Args {
      #[arg(long)]
      port: Option<String>,
  }
  #[doc = "Validator that converts string to int."]
− pub fn port_number(value: String) -> i32 {
+ pub fn port_number(value: &str) -> Result<i32, Box<dyn std::error::Error>> {
      let port = value.parse::<i32>().unwrap();
      if port < 1 {
−         panic!("{}", "Port must be>= 1");
+         return Err(Box::new(ArgumentTypeError::new("Port must be>= 1")));
      }
−     return port;
+     return Ok(port);
  }
  pub fn main() {}
────────────────────────────────────────────────────────────