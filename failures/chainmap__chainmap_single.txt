  chainmap.toml :: chainmap_single
    Output mismatch

────────────────────────────────────────────────────────────
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_single() -> Vec<HashMap<String, i32>> {
−     let mut one_dict = HashMap::new();
−     one_dict.insert("x".to_string(), 10);
−     return vec![one_dict];
+ pub fn test_chainmap_single() -> ChainMap {
+     let one_dict = {
+         let mut map = HashMap::new();
+         map.insert("x".to_string(), 10);
+         map
+     };
+     return ChainMap::new(one_dict);
  }
────────────────────────────────────────────────────────────