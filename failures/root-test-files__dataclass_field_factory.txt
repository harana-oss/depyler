  root-test-files.toml :: dataclass_field_factory
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashSet;
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct State {
      pub executed_events: HashSet<String>,
  }
  impl State {
      pub fn new() -> Self {
          Self {
              executed_events: HashSet::new(),
          }
      }
  }
────────────────────────────────────────────────────────────