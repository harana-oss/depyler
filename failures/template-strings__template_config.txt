  template-strings.toml :: template_config
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust: config template
−     let host = "localhost";
−     let port = 8080;
−     let result = format!("host={}\nport={}", host, port);
−     assert_eq!(result, "host=localhost\nport=8080");
+ 
────────────────────────────────────────────────────────────