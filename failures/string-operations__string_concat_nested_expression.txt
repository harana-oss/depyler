  string-operations.toml :: string_concat_nested_expression
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn log_message(msg: String) {
      log::info!("{}", msg);
  }
  pub fn log_formatted(prefix: String, value: i32, suffix: String) {
−     log_message(format!("{}{}{}", prefix, value.to_string(), suffix));
+     log_message(format!(
+         "{}{}",
+         format!("{}{}", prefix, (value).to_string()),
+         suffix
+     ));
  }
────────────────────────────────────────────────────────────