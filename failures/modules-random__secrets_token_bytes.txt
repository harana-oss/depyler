  modules-random.toml :: secrets_token_bytes
    Output mismatch

────────────────────────────────────────────────────────────
− use rand::Rng;
− use rand::prelude::*;
  pub fn random_bytes(n: i32) -> bytes {
      return {
          let mut bytes = vec![0u8; n];
          rand::thread_rng().fill(&mut bytes[..]);
          bytes
−     }
+     };
+ }
────────────────────────────────────────────────────────────