  decorators-basic.toml :: decorator_logging
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::RefCell;
−     use std::rc::Rc;
− 
−     fn logging_decorator<F>(f: F, log: Rc<RefCell<Vec<String>>>) -> impl Fn() -> i32
−     where
−         F: Fn() -> i32,
−     {
−         let log_clone = log.clone();
−         move || {
−             log_clone.borrow_mut().push(format!("Calling add"));
−             let result = f();
−             log_clone.borrow_mut().push(format!("Returned {}", result));
−             result
−         }
−     }
− 
−     fn add_impl() -> i32 {
−         42
+ use serde_json;
+ pub fn logging_decorator(f: &serde_json::Value) {
+     fn wrapper() -> () {
+         log.push(format!("Calling {}", f.__name__));
+         let result = f();
+         log.push(format!("Returned {}", result));
+         return result;
      }
− 
−     let log: Rc<RefCell<Vec<String>>> = Rc::new(RefCell::new(Vec::new()));
−     let add = logging_decorator(add_impl, log.clone());
−     let result = add();
−     let log_result = log.borrow().clone();
+     return wrapper;
+ }
+ pub fn add() -> i32 {
+     return 42;
+ }
────────────────────────────────────────────────────────────