  async-for.toml :: async_for_body
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− #[tokio::main]
− async let mut result = Vec::new();
− let mut stream = stream::iter(vec!["a", "b", "c"]);
− while let Some(item) = stream.next().await {
+ use serde_json;
+ #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncItemsState {
+     state: usize ,
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_items() -> impl Iterator<Item = i32>{
+     AsyncItemsState {
+     state: 0 ,
+ }
+ } impl Iterator for AsyncItemsState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _item in vec! ["a".to_string(), "b".to_string(), "c".to_string()] {
+     return Some(item);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test() -> Vec<serde_json::Value>{
+     let result = vec! [];
+     while let Some(item) = async_items().next().await {
      result.push(item.to_uppercase());
+    
  }
− log::info!("{:?}", result);
+ return result;
+     }
────────────────────────────────────────────────────────────