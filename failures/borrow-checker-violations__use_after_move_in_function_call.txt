  borrow-checker-violations.toml :: use_after_move_in_function_call
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn consume(items: Vec<i32>) -> i32 {
      return items.iter().sum::<i32>();
  }
− 
− pub fn caller() -> i32 {
+ pub fn caller() {
      let data = vec![1, 2, 3];
      let result = consume(data);
      return result;
  }
────────────────────────────────────────────────────────────