  modules-random.toml :: random_class_seeded
    Output mismatch

────────────────────────────────────────────────────────────
  use rand as random;
− use rand::prelude::*;
− use rand::rngs::SmallRng;
− use rand::Rng;
− use rand::SeedableRng;
  pub fn create_seeded_rng(seed: i32) -> i32 {
      let rng = SmallRng::seed_from_u64(seed as u64);
      return 0;
  }
────────────────────────────────────────────────────────────