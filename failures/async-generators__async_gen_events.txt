  async-generators.toml :: async_gen_events
    Output mismatch

────────────────────────────────────────────────────────────
− use async_stream::stream;
− use tokio_stream::StreamExt;
− 
− async fn test_async_gen_events() -> String {
−     let mut g = stream! {
−         yield "click".to_string();
−         yield "scroll".to_string();
−         yield "keypress".to_string();
−     };
−     g.next().await.unwrap()
− }
+ #[doc = " Generator state struct"] #[derive(Debug)] struct EventStreamState {
+     state: usize, events: Vec<String>
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn event_stream() -> impl Iterator<Item = i32>{
+     EventStreamState {
+     state: 0, events: Default::default()
+ }
+ } impl Iterator for EventStreamState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     self.events = vec! ["click".to_string(), "scroll".to_string(), "keypress".to_string()];
+     for _event in self.events.iter().cloned() {
+     return Some(event);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test_async_gen_events() -> String {
+     let first_event = None;
+     while let Some(event) = event_stream().next().await {
+     if first_event.is_none() {
+     first_event = event;
+    
+ }
+ break;
+    
+ }
+ return first_event;
+     }
────────────────────────────────────────────────────────────