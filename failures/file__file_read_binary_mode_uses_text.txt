  file.toml :: file_read_binary_mode_uses_text
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn read_binary_as_text(path: String) -> bytes {
+ pub fn read_binary_as_text(path: String) -> Result<bytes, std::io::Error> {
      let mut f = std::fs::File::open(path)?;
      let data = {
          let mut content = String::new();
          f.read_to_string(&mut content)?;
          content
      };
−     return data;
+     return Ok(data);
  }
────────────────────────────────────────────────────────────