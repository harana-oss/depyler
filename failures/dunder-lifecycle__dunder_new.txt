  dunder-lifecycle.toml :: dunder_new
    Output mismatch

────────────────────────────────────────────────────────────
− struct Counter {
−     id: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Counter {
+     pub id: serde_json::Value,
  }
− 
− static mut COUNT: i32 = 0;
− 
  impl Counter {
−     fn new() -> Self {
−         unsafe {
−             COUNT += 1;
−             Counter { id: COUNT }
−         }
+     pub const _count: i32 = 0;
+     pub fn new() -> Self {
+         Self { id: Counter._count }
      }
  }
− 
− let c1 = Counter::new();
− let c2 = Counter::new();
− log::info!("{}", c1.id);
− log::info!("{}", c2.id);
────────────────────────────────────────────────────────────