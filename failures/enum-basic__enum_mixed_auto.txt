  enum-basic.toml :: enum_mixed_auto
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, PartialEq)]
−     enum Number {
−         A = 1,
−         B, // 2
−         C, // 3
+ pub const result: serde_json::Value =
+     ((Number.A.value == 1) && (Number.B.value == 2)) && (Number.C.value == 3);
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Number {}
+ impl Number {
+     pub const A: i32 = 1;
+     pub const B: i32 = auto();
+     pub const C: i32 = auto();
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = Number::A as i32 == 1 && Number::B as i32 == 2 && Number::C as i32 == 3;
+ }
────────────────────────────────────────────────────────────