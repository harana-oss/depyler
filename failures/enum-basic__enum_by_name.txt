  enum-basic.toml :: enum_by_name
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't support runtime string-to-enum lookup natively
−     // Would typically use match or a helper function
−     #[derive(Debug, PartialEq)]
−     enum Color {
−         RED = 1,
−         GREEN = 2,
+ pub const result: serde_json::Value = Color.get("RED").cloned().unwrap() == Color::RED;
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
      }
− 
−     fn from_name(name: &str) -> Option<Color> {
−         match name {
−             "RED" => Some(Color::RED),
−             "GREEN" => Some(Color::GREEN),
−             _ => None,
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
          }
      }
− 
−     let result = from_name("RED") == Some(Color::RED);
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const GREEN: i32 = 2;
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
────────────────────────────────────────────────────────────