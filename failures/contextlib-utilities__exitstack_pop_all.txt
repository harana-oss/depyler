  contextlib-utilities.toml :: exitstack_pop_all
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn test_exitstack_pop_all() -> bool {
−     let mut called = Vec::new();
−     let mut transferred_cleanup = Vec::new();
−     {
−         // stack1 scope
−         transferred_cleanup.push(1);
−     }
−     // Nothing called yet
−     if !called.is_empty() {
+     let called = vec![];
+     let _context = ExitStack::new();
+     let mut stack1 = _context.__enter__();
+     stack1.callback(called.append, 1);
+     let stack2 = stack1.pop_all();
+     if called.len() as i32 > 0 {
          return false;
      }
−     {
−         // stack2 scope
−         called.extend(transferred_cleanup);
−     }
−     called.len() == 1
+     let _context = stack2;
+     return called.len() as i32 == 1;
  }
────────────────────────────────────────────────────────────