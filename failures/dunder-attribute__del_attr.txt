  dunder-attribute.toml :: del_attr
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
− 
− struct MyClass {
−     attrs: HashMap<String, i32>,
+ #[derive(Debug, Copy, Clone)]
+ pub struct MyClass {
+     pub value: i32,
  }
− 
  impl MyClass {
−     fn new() -> Self {
−         let mut attrs = HashMap::new();
−         attrs.insert("value".to_string(), 42);
−         MyClass { attrs }
+     pub fn new() -> Self {
+         Self { value: 42 }
      }
− 
−     fn del_attr(&mut self, name: &str) {
−         log::info!("Removing: {}", name);
−         self.attrs.remove(name);
−     }
  }
− 
− let mut obj = MyClass::new();
− obj.del_attr("value");
────────────────────────────────────────────────────────────