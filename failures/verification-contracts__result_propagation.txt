  verification-contracts.toml :: result_propagation
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn double_parse(s: String) -> Option<i32> {
+ #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn double_parse(s: &str) -> Option<i32> {
      let parsed = parse_safe(s);
      if parsed.is_none() {
          return None;
      }
−     return Some(parsed.unwrap() * 2);
+     return Some(parsed * 2);
  }
  pub fn parse_safe(s: String) -> Option<i32> {
      match s.parse::<i32>() {
          Ok(__parsed_value) => Some(__parsed_value),
          Err(_) => None,
      }
  }
────────────────────────────────────────────────────────────