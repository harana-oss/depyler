  enum-int.toml :: intenum_mul
    Output mismatch

────────────────────────────────────────────────────────────
− #[repr(i32)]
−     #[derive(Debug, Clone, Copy)]
−     enum Priority {
−         Low = 2,
−         High = 5,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Priority {}
+ impl Priority {
+     pub const LOW: i32 = 2;
+     pub const HIGH: i32 = 5;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = (Priority::Low as i32) * 2;
−     assert_eq!(result, 4);
−     let result2 = 3 * (Priority::High as i32);
−     assert_eq!(result2, 15);
+ }
────────────────────────────────────────────────────────────