  mutating-method-names.toml :: dict_update_is_mutating
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashMap;
  pub fn merge_dicts(d: &mut HashMap<String, i32>, other: &HashMap<String, i32>) {
−     d.extend(other.iter().map(|(k, v)| (k.clone(), v.clone())));
+     for (k, v) in other {
+         d.insert(k, v);
+     }
  }
────────────────────────────────────────────────────────────