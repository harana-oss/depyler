  boundary-values.toml :: string_length_boundaries
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn string_boundary_test() -> i32 {
      let empty_string = "".to_string();
      let single_char = "a".to_string();
      let long_string = "a".repeat(1000 as usize);
−     return empty_string.len() as i32 + single_char.len() as i32 + long_string.len() as i32;
+     return empty_string.len() as i32 + single_char.len() as i32 + long_string.len() as i32 as i32;
  }
────────────────────────────────────────────────────────────