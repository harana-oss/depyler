  optional-types.toml :: method_on_optional_string
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Person {
      pub name: Option<String>,
  }
  impl Person {
      pub fn new(name: Option<String>) -> Self {
          Self { name }
      }
  }
  pub fn upper_name(p: &Person) -> String {
−     return p.name.clone().as_ref().unwrap().to_uppercase();
+     return p.name.to_uppercase();
  }
────────────────────────────────────────────────────────────