  optional-types.toml :: option_truthiness_check
    Output mismatch

────────────────────────────────────────────────────────────
  use std as os;
  pub fn check_config() {
      let config_file = std::env::var("CONFIG_FILE").ok();
−     if config_file.is_some() {
+     if config_file {
          log::info!("{}", "Config set");
      } else {
          log::info!("{}", "No config");
      }
  }
────────────────────────────────────────────────────────────