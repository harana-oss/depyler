  dataclasses-field.toml :: field_metadata_multi
    Output mismatch

────────────────────────────────────────────────────────────
− // Metadata in Rust would be attributes or const data
−     #[derive(Debug)]
−     struct Data {
−         x: i32,
+ pub const meta: serde_json::Value = fields(Data).get(0usize).cloned().unwrap().metadata;
+ pub const result: serde_json::Value =
+     (meta.get("a").cloned().unwrap() == 1) && (meta.get("b").cloned().unwrap() == 2);
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
      }
− 
−     let result = true;
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Data {
+     pub x: i32,
+ }
+ impl Data {
+     pub fn new() -> Self {
+         Self {
+             x: Default::default(),
+         }
+     }
+ }
────────────────────────────────────────────────────────────