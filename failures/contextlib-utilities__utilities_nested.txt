  contextlib-utilities.toml :: utilities_nested
    Output mismatch

────────────────────────────────────────────────────────────
− use std::io::Write;
− 
  pub fn test_utilities_nested() -> bool {
−     let mut buffer = Vec::new();
−     let _: Result<(), std::io::Error> = (|| {
−         {
−             {
−                 writeln!(&mut buffer, "test")?;
−             }
−         }
−         Ok(())
−     })();
−     !buffer.is_empty()
+     let f = StringIO::new();
+     let _context = suppress(ValueError);
+     let _context = redirect_stdout(f);
+     let _context = nullcontext();
+     log::info!("{}", "test");
+     return f.getvalue().len() as i32 > 0;
  }
────────────────────────────────────────────────────────────