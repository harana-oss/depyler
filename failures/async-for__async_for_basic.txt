  async-for.toml :: async_for_basic
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− #[tokio::main]
− async let mut result = Vec::new();
− let mut stream = stream::iter(0..5);
− while let Some(item) = stream.next().await {
+ use serde_json;
+ #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncRangeState {
+     state: usize, n: serde_json::Value
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_range(n: & serde_json::Value) -> impl Iterator<Item = i32>{
+     AsyncRangeState {
+     state: 0, n: n
+ }
+ } impl Iterator for AsyncRangeState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _i in 0..self.n {
+     return Some(i);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test() -> Vec<serde_json::Value>{
+     let result = vec! [];
+     while let Some(item) = async_range(5).next().await {
      result.push(item);
+    
  }
− log::info!("{:?}", result);
+ return result;
+     }
────────────────────────────────────────────────────────────