  chainmap.toml :: chainmap_template
    Output mismatch

────────────────────────────────────────────────────────────
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_template() -> String {
−     let mut global_vars = HashMap::new();
−     global_vars.insert("name".to_string(), "World".to_string());
− 
−     let mut local_vars = HashMap::new();
−     local_vars.insert("greeting".to_string(), "Hello".to_string());
− 
−     let maps = vec![local_vars, global_vars];
− 
−     let mut greeting = String::new();
−     let mut name = String::new();
− 
−     for map in &maps {
−         if greeting.is_empty() {
−             if let Some(g) = map.get("greeting") {
−                 greeting = g.clone();
−             }
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
          }
−         if name.is_empty() {
−             if let Some(n) = map.get("name") {
−                 name = n.clone();
−             }
−         }
      }
− 
−     return format!("{} {}", greeting, name);
  }
+ pub fn test_chainmap_template() -> String {
+     let global_vars = {
+         let mut map = HashMap::new();
+         map.insert("name".to_string(), "World".to_string());
+         map
+     };
+     let local_vars = {
+         let mut map = HashMap::new();
+         map.insert("greeting".to_string(), "Hello".to_string());
+         map
+     };
+     let context = ChainMap::new(local_vars, global_vars);
+     return format!(
+         "{} {}",
+         context.get("greeting").cloned().unwrap(),
+         context.get("name").cloned().unwrap()
+     );
+ }
────────────────────────────────────────────────────────────