  enum-advanced.toml :: enum_extend_base
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− enum Color {
−     Red = 1,
−     Green = 2,
+ #[derive(Debug, Copy, Clone)]
+ pub struct NoMembers {}
+ impl NoMembers {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const GREEN: i32 = 2;
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− 
  pub fn test_enum_extend_base() -> i32 {
−     Color::Red as i32
+     return Color.RED.value;
  }
────────────────────────────────────────────────────────────