  optional-types.toml :: optional_var_in_list
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn get_id() -> Option<i32> {
      return Some(42);
  }
  pub fn check_membership(ids: &Vec<i32>) -> bool {
      let id = get_id();
      if id.is_some() {
−         return ids.contains(&id.clone().unwrap());
+         return ids.contains(&id.as_ref().unwrap());
      }
      return false;
  }
────────────────────────────────────────────────────────────