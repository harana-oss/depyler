  deque.toml :: deque_popleft
    Output mismatch

────────────────────────────────────────────────────────────
− // d.popleft()
−     use std::collections::VecDeque;
− 
−     let mut d = VecDeque::from(vec![1, 2, 3]);
−     let x = d.pop_front().unwrap();
−     let result = x == 1 && d.iter().cloned().collect::<Vec<_>>() == vec![2, 3];
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────