  dataclasses-advanced.toml :: dataclass_astuple
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− struct Point {
−     x: i32,
−     y: i32,
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Point {
+     pub x: i32,
+     pub y: i32,
  }
− 
  impl Point {
−     fn as_tuple(&self) -> (i32, i32) {
−         (self.x, self.y)
+     pub fn new(x: i32, y: i32) -> Self {
+         Self { x, y }
      }
  }
− 
− pub fn test_dataclass_astuple() -> (i32, i32) {
−     let p = Point { x: 1, y: 2 };
−     p.as_tuple()
+ pub fn test_dataclass_astuple() -> () {
+     let p = Point::new(1, 2);
+     return astuple(p);
  }
────────────────────────────────────────────────────────────