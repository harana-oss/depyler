  await-expressions.toml :: await_in_try
    Output mismatch

────────────────────────────────────────────────────────────
− async fn f() -> Result<i32, String> {
−     Err("error".to_string())
+ #[derive(Debug, Clone)] pub struct ValueError {
+     message: String ,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+     write!(f, "value error: {}", self.message)
+ }
+ } impl std::error::Error for ValueError {
+    
+ }
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+     Self {
+     message: message.into()
+ }
+ }
+ }
+ pub async fn f() -> Result<i32, ValueError>{
+     return Err(ValueError::new("error"));
+    
  }
− 
− async fn test_await_in_try() -> i32 {
−     match f().await {
−         Ok(_) => 0,
−         Err(_) => 1,
−     }
+ pub async fn test_await_in_try() -> i32 {
+     { f().await;
+     return 0;
+     return 1;
+    
+ }
  }
────────────────────────────────────────────────────────────