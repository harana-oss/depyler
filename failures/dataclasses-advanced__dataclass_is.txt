  dataclasses-advanced.toml :: dataclass_is
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− struct Point {
−     x: i32,
−     y: i32,
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Point {
+     pub x: i32,
+     pub y: i32,
  }
− 
− struct Regular {}
− 
+ impl Point {
+     pub fn new(x: i32, y: i32) -> Self {
+         Self { x, y }
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Regular {}
+ impl Regular {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
  pub fn test_dataclass_is() -> bool {
−     true // Point is a dataclass-like struct, Regular is not
+     return (is_dataclass(Point)) && (!is_dataclass(Regular));
  }
────────────────────────────────────────────────────────────