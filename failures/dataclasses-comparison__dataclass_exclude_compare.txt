  dataclasses-comparison.toml :: dataclass_exclude_compare
    Output mismatch

────────────────────────────────────────────────────────────
− // In Rust, manually implement PartialEq/Ord to exclude fields
−     #[derive(Debug)]
−     struct Record {
−         key: i32,
−         timestamp: f64,
+ pub const result: serde_json::Value = Record::new(1, 100.0) == Record::new(1, 200.0);
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Record {
+     pub key: i32,
+ }
+ impl Record {
+     pub const timestamp: f64 = field();
+     pub fn new(key: i32) -> Self {
+         Self { key }
      }
− 
−     impl PartialEq for Record {
−         fn eq(&self, other: &Self) -> bool {
−             self.key == other.key
−         }
−     }
− 
−     let result = true;
+ }
────────────────────────────────────────────────────────────