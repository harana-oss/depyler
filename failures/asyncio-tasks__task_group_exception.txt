  asyncio-tasks.toml :: task_group_exception
    Output mismatch

────────────────────────────────────────────────────────────
− use tokio::task::JoinSet;
− 
− async fn failing_coro() -> Result<i32, String> {
−     Err("Failed".to_string())
+ #[derive(Debug, Clone)] pub struct ValueError {
+     message: String ,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+     write!(f, "value error: {}", self.message)
+ }
+ } impl std::error::Error for ValueError {
+    
+ }
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+     Self {
+     message: message.into()
+ }
  }
− 
+ }
+ pub async fn failing_coro() -> Result<i32, ValueError>{
+     return Err(ValueError::new("Failed"));
+    
+ }
  pub async fn test_task_group_exception() -> bool {
−     let mut set = JoinSet::new();
−     set.spawn(failing_coro());
− 
−     while let Some(result) = set.join_next().await {
−         if result.unwrap().is_err() {
−             return true;
−         }
−     }
−     false
+     { {
+     let tg = asyncio.TaskGroup();
+     tg.create_task(failing_coro());
+    
+ }
+ return false;
+     return true;
+    
+ }
  }
────────────────────────────────────────────────────────────