  dunder-callable.toml :: callable_true
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't have a built-in callable() check
− // Use traits to make objects callable
− struct MyCallable;
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct MyCallable {}
  impl MyCallable {
−     fn call(&self) -> &str {
−         "called"
+     pub fn new() -> Self {
+         Self {}
      }
  }
− 
− let obj = MyCallable;
− log::info!("true"); // Would be callable in Python
− log::info!("{}", obj.call());
────────────────────────────────────────────────────────────