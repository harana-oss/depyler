  file.toml :: file_readline_single
    Output mismatch

────────────────────────────────────────────────────────────
− use std::io::BufRead;
− pub fn read_first_line(path: String) -> String {
+ pub fn read_first_line(path: String) -> Result<String, std::io::Error> {
      let mut f = std::fs::File::open(path)?;
−     let mut line = String::new();
−     std::io::BufReader::new(&f).read_line(&mut line)?;
−     return line;
+     let line = f.readline();
+     return Ok(line);
  }
────────────────────────────────────────────────────────────