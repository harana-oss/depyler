  dunder-comparison.toml :: hash_in_dict
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
− use std::hash::{Hash, Hasher};
− 
− #[derive(PartialEq, Eq, Hash)]
− struct Hashable {
−     x: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Hashable {
+     pub x: serde_json::Value,
  }
− 
  impl Hashable {
−     fn new(x: i32) -> Self {
−         Hashable { x }
+     pub fn new(x: serde_json::Value) -> Self {
+         Self { x }
      }
  }
− 
− let obj1 = Hashable::new(1);
− let obj2 = Hashable::new(2);
− let mut d = HashMap::new();
− d.insert(obj1, "first");
− d.insert(obj2, "second");
− let key = Hashable::new(1);
− log::info!("{}", d.get(&key).unwrap());
────────────────────────────────────────────────────────────