  modules-csv.toml :: csv_row_iteration
    Output mismatch

────────────────────────────────────────────────────────────
  use csv;
− pub fn print_rows(filepath: String) {
+ pub fn print_rows(filepath: String) -> Result<(), std::io::Error> {
      let mut f = std::fs::File::open(filepath)?;
      let reader = csv::ReaderBuilder::new().has_headers(true).from_reader(f);
      for result in reader.deserialize::<HashMap<String, String>>() {
          let row = result?;
          log::info!("{}", row);
      }
+     Ok(())
  }
────────────────────────────────────────────────────────────