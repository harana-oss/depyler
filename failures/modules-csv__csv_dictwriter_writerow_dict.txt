  modules-csv.toml :: csv_dictwriter_writerow_dict
    Output mismatch

────────────────────────────────────────────────────────────
  use csv;
− pub fn write_single_row(filename: String) {
+ use std::collections::HashMap;
+ pub fn write_single_row(filename: String) -> Result<(), std::io::Error> {
      let mut f = std::fs::File::create(filename)?;
      let writer = csv::Writer::from_writer(f);
−     writer.writerow(HashMap::from([
−         ("name".to_string(), "Alice".to_string()),
−         ("age".to_string(), "30".to_string()),
−     ]));
+     writer.writerow({
+         let mut map = HashMap::new();
+         map.insert("name".to_string(), "Alice".to_string());
+         map.insert("age".to_string(), "30".to_string());
+         map
+     });
+     Ok(())
  }
────────────────────────────────────────────────────────────