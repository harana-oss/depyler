  descriptors.toml :: descriptor_slots
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't have __slots__, but similar memory efficiency
− // is achieved through careful struct design
− 
− struct Descriptor {
−     value: Option<i32>,
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Descriptor {
+     pub value: (),
  }
− 
  impl Descriptor {
−     fn new() -> Self {
−         Descriptor { value: None }
−     }
− 
−     fn get(&self) -> Option<i32> {
−         self.value
−     }
− 
−     fn set(&mut self, val: i32) {
−         self.value = Some(val);
+     pub fn new() -> Self {
+         Self { value: () }
      }
  }
− 
− struct MyClass {
−     x: Descriptor,
+ #[derive(Debug, Copy, Clone)]
+ pub struct MyClass {}
+ impl MyClass {
+     pub const __slots__: Vec<String> = vec!["_x".to_string()];
+     pub const x: serde_json::Value = Descriptor::new();
+     pub fn new() -> Self {
+         Self {}
+     }
  }
− 
− let mut instance = MyClass {
−     x: Descriptor::new(),
− };
− instance.x.set(42);
− log::info!("{}", instance.x.get().unwrap());
────────────────────────────────────────────────────────────