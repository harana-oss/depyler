  enum-int.toml :: intenum_sort
    Output mismatch

────────────────────────────────────────────────────────────
− #[repr(i32)]
−     #[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
−     enum Priority {
−         Low = 1,
−         Medium = 2,
−         High = 3,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Priority {}
+ impl Priority {
+     pub const LOW: i32 = 1;
+     pub const MEDIUM: i32 = 2;
+     pub const HIGH: i32 = 3;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let mut values = vec![Priority::High, Priority::Low, Priority::Medium];
−     values.sort();
−     assert_eq!(
−         values,
−         vec![Priority::Low, Priority::Medium, Priority::High]
−     );
+ }
────────────────────────────────────────────────────────────