  chainmap.toml :: chainmap_pop
    Output mismatch

────────────────────────────────────────────────────────────
+ use std::collections::ChainMap;
  use std::collections::HashMap;
− 
− pub fn test_chainmap_pop() -> i32 {
−     let mut outer = HashMap::new();
−     outer.insert("x".to_string(), 1);
− 
−     let mut inner = HashMap::new();
−     inner.insert("x".to_string(), 2);
− 
−     let maps = vec![inner, outer];
−     let parents = &maps[1..];
− 
−     for map in parents {
−         if let Some(&value) = map.get("x") {
−             return value;
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
          }
      }
−     panic!("Key not found");
  }
+ pub fn test_chainmap_pop() -> Result<i32, IndexError> {
+     let outer = {
+         let mut map = HashMap::new();
+         map.insert("x".to_string(), 1);
+         map
+     };
+     let inner = {
+         let mut map = HashMap::new();
+         map.insert("x".to_string(), 2);
+         map
+     };
+     let mut c = ChainMap::new(inner, outer);
+     c = c.parents;
+     return Ok(c.get("x").cloned().unwrap());
+ }
────────────────────────────────────────────────────────────