  collection-edge-cases.toml :: iter_slice
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn sum_range(items: &Vec<i32>, start: i32, end: i32) -> i32 {
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn sum_range(items: &Vec<i32>, start: i32, end: i32) -> Result<i32, IndexError> {
      let mut total = 0;
−     let end = std::cmp::min(end as usize, items.len());
−     for i in (start as usize)..end {
−         total += items[i];
+     for i in start..std::cmp::min(end, items.len() as i32) {
+         total += items.get(i as usize).cloned().unwrap();
      }
−     return total;
+     return Ok(total);
  }
────────────────────────────────────────────────────────────