  deque.toml :: deque_maxlen_right
    Output mismatch

────────────────────────────────────────────────────────────
− // append to full deque drops left
−     use std::collections::VecDeque;
− 
−     let mut d = VecDeque::from(vec![1, 2, 3]);
−     let maxlen = 3;
−     d.push_back(4);
−     while d.len() > maxlen {
−         d.pop_front();
−     }
−     let result = d.iter().cloned().collect::<Vec<_>>() == vec![2, 3, 4];
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────