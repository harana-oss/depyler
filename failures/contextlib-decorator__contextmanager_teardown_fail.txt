  contextlib-decorator.toml :: contextmanager_teardown_fail
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::RefCell;
−     use std::rc::Rc;
− 
−     let log: Rc<RefCell<Vec<&str>>> = Rc::new(RefCell::new(Vec::new()));
−     let log_clone = log.clone();
− 
−     fn fail_in_teardown<F, R>(log: Rc<RefCell<Vec<&'static str>>>, f: F) -> Result<R, String>
−     where
−         F: FnOnce() -> R,
−     {
−         let result = f();
−         log.borrow_mut().push("cleanup attempted");
−         Err("teardown failed".to_string())
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[doc = " Generator state struct"]
+ #[derive(Debug)]
+ struct FailInTeardownState {
+     state: usize,
+ }
+ #[doc = " Generator function - returns Iterator"]
+ pub fn fail_in_teardown() -> impl Iterator<Item = serde_json::Value> {
+     FailInTeardownState { state: 0 }
+ }
+ impl Iterator for FailInTeardownState {
+     type Item = serde_json::Value;
+     fn next(&mut self) -> Option<Self::Item> {
+         match self.state {
+             0 => {
+                 self.state = 1;
+                 return None;
+                 log.push("cleanup attempted");
+                 panic!("{}", "teardown failed");
+                 None
+             }
+             _ => None,
+         }
      }
− 
−     let _ = fail_in_teardown(log_clone, || {
−         log.borrow_mut().push("body executed");
−     });
− 
−     let result = log.borrow().clone();
+ }
────────────────────────────────────────────────────────────