  optional-types.toml :: none_placeholder_function_parameter
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn compute(x: i32) -> String {
−     let mut result: Option<String> = None;
+     let mut result = None;
      if x > 0 {
−         result = Some("positive".to_string());
+         result = "positive".to_string();
      } else {
          if x < 0 {
−             result = Some("negative".to_string());
+             result = "negative".to_string();
          } else {
−             result = Some("zero".to_string());
+             result = "zero".to_string();
          }
      }
−     result.unwrap()
+     return result;
  }
────────────────────────────────────────────────────────────