  contextlib-decorator.toml :: contextmanager_teardown
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::RefCell;
−     use std::rc::Rc;
− 
−     let cleanup_done: Rc<RefCell<Vec<bool>>> = Rc::new(RefCell::new(Vec::new()));
−     let cleanup_clone = cleanup_done.clone();
− 
−     fn with_cleanup<F, R>(cleanup: Rc<RefCell<Vec<bool>>>, f: F) -> R
−     where
−         F: FnOnce() -> R,
−     {
−         let result = f();
−         cleanup.borrow_mut().push(true);
−         result
+ use serde_json;
+ #[doc = " Generator state struct"]
+ #[derive(Debug)]
+ struct WithCleanupState {
+     state: usize,
+ }
+ #[doc = " Generator function - returns Iterator"]
+ pub fn with_cleanup() -> impl Iterator<Item = serde_json::Value> {
+     WithCleanupState { state: 0 }
+ }
+ impl Iterator for WithCleanupState {
+     type Item = serde_json::Value;
+     fn next(&mut self) -> Option<Self::Item> {
+         match self.state {
+             0 => {
+                 self.state = 1;
+                 return None;
+                 cleanup_done.push(true);
+                 None
+             }
+             _ => None,
+         }
      }
− 
−     with_cleanup(cleanup_clone, || ());
−     let result = cleanup_done.borrow().len();
+ }
────────────────────────────────────────────────────────────