  verification-contracts.toml :: postcondition_assert
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn absolute(n: i32) -> i32 {
−     let result;
+     let mut result;
      if n < 0 {
          result = (-n);
      } else {
          result = n;
      }
−     assert!(result >= 0, "result must be non-negative");
+     assert!(result >= 0, "{}", "result must be non-negative");
      return result;
  }
────────────────────────────────────────────────────────────