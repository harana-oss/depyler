  ternary-expressions.toml :: ternary_multiple_args
    Output mismatch

────────────────────────────────────────────────────────────
− fn func(a: i32, b: i32) -> i32 {
−     a + b
+ pub fn func(a: i32, b: i32) {
+     return a + b;
  }
− let x = true;
− let y = false;
− let result = func(if x { 10 } else { 20 }, if y { 30 } else { 40 });
− log::info!("{}", result);
────────────────────────────────────────────────────────────