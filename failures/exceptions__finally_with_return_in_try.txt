  exceptions.toml :: finally_with_return_in_try
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn process_with_cleanup(x: i32) -> i32 {
−     let mut count = 0;
−     {
−         count = x * 2;
−         return count;
−         count += 1;
−     }
+     let count = 0;
+     count = x * 2;
+     return count;
+     count += 1;
  }
────────────────────────────────────────────────────────────