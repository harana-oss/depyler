  iterators.toml :: range_with_variables
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn dynamic_range(start: i32, end: i32, step: i32) -> Vec<i32> {
+ use serde_json;
+ pub fn dynamic_range(start: i32, end: i32, step: i32) -> Vec<serde_json::Value> {
      let mut result = vec![];
      for i in {
          let step = step as usize;
          if step == 0 {
              panic!("range() arg 3 must not be zero");
          }
          (start..end).step_by(step)
      } {
          result.push(i);
      }
      return result;
  }
────────────────────────────────────────────────────────────