  cargo-integration.toml :: cargo_dependency_version
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn parse_version(version_str: &str) -> (i32, i32, i32) {
−     let parts: Vec<&str> = version_str.split('.').collect();
−     let major = parts.get(0).and_then(|s| s.parse().ok()).unwrap_or(0);
−     let minor = parts.get(1).and_then(|s| s.parse().ok()).unwrap_or(0);
−     let patch = parts.get(2).and_then(|s| s.parse().ok()).unwrap_or(0);
+ #[derive(Debug, Clone)]
+ pub struct IndexError {
+     message: String,
+ }
+ impl std::fmt::Display for IndexError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "index out of range: {}", self.message)
+     }
+ }
+ impl std::error::Error for IndexError {}
+ impl IndexError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ pub fn parse_version(version_str: String) -> (i32, i32, i32) {
+     let parts = version_str
+         .split(".")
+         .map(|s| s.to_string())
+         .collect::<Vec<String>>();
+     let major = if parts.len() as i32 > 0 {
+         (parts.get(0usize).cloned().unwrap()) as i32
+     } else {
+         0
+     };
+     let minor = if parts.len() as i32 > 1 {
+         (parts.get(1usize).cloned().unwrap()) as i32
+     } else {
+         0
+     };
+     let patch = if parts.len() as i32 > 2 {
+         (parts.get(2usize).cloned().unwrap()) as i32
+     } else {
+         0
+     };
      return (major, minor, patch);
  }
− 
  pub fn format_version(major: i32, minor: i32, patch: i32) -> String {
      return format!("{}.{}.{}", major, minor, patch);
  }
────────────────────────────────────────────────────────────