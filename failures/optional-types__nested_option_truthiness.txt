  optional-types.toml :: nested_option_truthiness
    Output mismatch

────────────────────────────────────────────────────────────
  use std as os;
  pub fn check_multiple() -> String {
      let var1 = std::env::var("VAR1").ok();
      let var2 = std::env::var("VAR2").ok();
−     if var1.is_some() {
−         if var2.is_some() {
+     if var1 {
+         if var2 {
              return "both".to_string();
          }
          return "only_var1".to_string();
      }
      return "none".to_string();
  }
────────────────────────────────────────────────────────────