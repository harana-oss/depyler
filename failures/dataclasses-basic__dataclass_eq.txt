  dataclasses-basic.toml :: dataclass_eq
    Output mismatch

────────────────────────────────────────────────────────────
− pub const p1: Point = Point::new(1, 2);
− pub const p2: Point = Point::new(1, 2);
− pub const result: bool = p1 == p2;
− 
  #[derive(Debug, Copy, Clone, PartialEq, Default)]
  pub struct Point {
      pub x: i32,
      pub y: i32,
  }
  impl Point {
      pub fn new(x: i32, y: i32) -> Self {
          Self { x, y }
      }
  }
────────────────────────────────────────────────────────────