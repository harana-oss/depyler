  contextlib-decorator.toml :: contextmanager_reuse
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::RefCell;
−     use std::rc::Rc;
− 
−     let call_count: Rc<RefCell<Vec<i32>>> = Rc::new(RefCell::new(Vec::new()));
− 
−     fn counting_cm<F, R>(call_count: Rc<RefCell<Vec<i32>>>, f: F) -> R
−     where
−         F: FnOnce(usize) -> R,
−     {
−         call_count.borrow_mut().push(1);
−         f(call_count.borrow().len())
+ use serde_json;
+ #[doc = " Generator state struct"]
+ #[derive(Debug)]
+ struct CountingCmState {
+     state: usize,
+ }
+ #[doc = " Generator function - returns Iterator"]
+ pub fn counting_cm() -> impl Iterator<Item = serde_json::Value> {
+     CountingCmState { state: 0 }
+ }
+ impl Iterator for CountingCmState {
+     type Item = serde_json::Value;
+     fn next(&mut self) -> Option<Self::Item> {
+         match self.state {
+             0 => {
+                 self.state = 1usize;
+                 return Some(call_count.len() as i32);
+             }
+             1usize => {
+                 self.state = 1usize + 1;
+                 None
+             }
+             _ => None,
+         }
      }
− 
−     let first = counting_cm(call_count.clone(), |count| count);
−     let second = counting_cm(call_count.clone(), |count| count);
− 
−     let result = (first, second);
+ }
────────────────────────────────────────────────────────────