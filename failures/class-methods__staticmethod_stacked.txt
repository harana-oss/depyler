  class-methods.toml :: staticmethod_stacked
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  #[derive(Debug, Copy, Clone)]
  pub struct Math {}
  impl Math {
      pub fn new() -> Self {
          Self {}
      }
      pub fn add_five(x: i32) -> i32 {
          return x + 5;
      }
  }
  pub fn double_decorator(func: &serde_json::Value) {
      fn wrapper() -> () {
          let result = func(args);
          return result * 2;
      }
      return wrapper;
  }
− log::info!("{}", Math::add_five(10));
────────────────────────────────────────────────────────────