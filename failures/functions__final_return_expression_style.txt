  functions.toml :: final_return_expression_style
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn add(a: i32, b: i32) -> i32 {
−     a + b
+     return a + b;
  }
−             let result1 = add(a.clone(), b.clone());
−             let result2 = add(b.clone(), a.clone());
−             if result1 != result2 {
−                 return TestResult::failed();
−             }
────────────────────────────────────────────────────────────