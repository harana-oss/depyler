  modules-os-sys.toml :: subprocess_run_basic
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn run_command(cmd: &Vec<String>) -> i32 {
−     let result = std::process::Command::new(&cmd[0])
−         .args(&cmd[1..])
−         .status()
−         .expect("Failed to execute command");
−     return result.code().unwrap_or(-1);
+     let result = {
+         let cmd_list = cmd;
+         let mut cmd = std::process::Command::new(&cmd_list[0]);
+         cmd.args(&cmd_list[1..]);
+         let status = cmd.status().expect("subprocess.run() failed");
+         struct SubprocessResult {
+             returncode: i32,
+             stdout: String,
+             stderr: String,
+         }
+         SubprocessResult {
+             returncode: status.code().unwrap_or(-1),
+             stdout: String::new(),
+             stderr: String::new(),
+         }
+     };
+     return result.returncode;
  }
────────────────────────────────────────────────────────────