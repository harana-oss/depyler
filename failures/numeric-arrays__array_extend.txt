  numeric-arrays.toml :: array_extend
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn extend_array(mut arr: Vec<i32>, values: Vec<i32>) -> Vec<i32> {
−     arr.extend(values);
+ pub fn extend_array(mut arr: Vec<i32>, values: &Vec<i32>) -> Vec<i32> {
+     arr.extend(values.iter().cloned());
      return arr;
  }
────────────────────────────────────────────────────────────