  contextlib-utilities.toml :: redirect_stdout
    Output mismatch

────────────────────────────────────────────────────────────
− use std::io::Write;
− 
  pub fn test_redirect_stdout() -> String {
−     let mut buffer = Vec::new();
−     writeln!(&mut buffer, "hello").unwrap();
−     String::from_utf8(buffer).unwrap().trim().to_string()
+     let f = StringIO::new();
+     let _context = redirect_stdout(f);
+     log::info!("{}", "hello");
+     return f.getvalue().trim().to_string();
  }
────────────────────────────────────────────────────────────