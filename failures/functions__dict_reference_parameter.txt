  functions.toml :: dict_reference_parameter
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashMap;
  #[doc = "Count keys in dict."]
  pub fn count_keys(data: &HashMap<String, i32>) -> i32 {
−     return data.len() as i32;
+     return data.len() as i32 as i32;
  }
  #[doc = "Pass owned dict."]
  pub fn main() {
      let info = {
          let mut map = HashMap::new();
          map.insert("a".to_string(), 1);
          map.insert("b".to_string(), 2);
          map.insert("c".to_string(), 3);
          map
      };
      let count = count_keys(&info);
      log::info!("{}", count);
  }
────────────────────────────────────────────────────────────