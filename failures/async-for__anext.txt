  async-for.toml :: anext
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− #[tokio::main]
− async let result: Vec<i32> = stream::iter(0..3).collect().await;
− log::info!("{:?}", result);
+ use serde_json;
+ #[derive(Debug, Clone)] pub struct AsyncCounter {
+     pub current: serde_json::Value, pub end: serde_json::Value
+ }
+ impl AsyncCounter {
+     pub fn new(start: serde_json::Value, end: serde_json::Value) -> Self {
+     Self {
+     current: start, end
+ }
+ } pub async fn __anext__(&mut self) {
+     if self.current>= self.end {
+     panic!("Exception: {}", StopAsyncIteration);
+    
+ }
+ let mut value = self.current;
+     self.current += 1;
+     return value;
+    
+ }
+ } pub async fn test() -> Vec<serde_json::Value>{
+     let result = vec! [];
+     while let Some(i) = AsyncCounter::new(0, 3).next().await {
+     result.push(i);
+    
+ }
+ return result;
+     }
────────────────────────────────────────────────────────────