  deque.toml :: deque_queue
    Output mismatch

────────────────────────────────────────────────────────────
− // append right, popleft
−     use std::collections::VecDeque;
− 
−     let mut queue = VecDeque::new();
−     queue.push_back(1);
−     queue.push_back(2);
−     let result = queue.pop_front().unwrap() == 1 && queue.pop_front().unwrap() == 2;
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────