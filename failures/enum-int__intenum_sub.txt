  enum-int.toml :: intenum_sub
    Output mismatch

────────────────────────────────────────────────────────────
− #[repr(i32)]
−     #[derive(Debug, Clone, Copy)]
−     enum Priority {
−         Low = 1,
−         High = 3,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Priority {}
+ impl Priority {
+     pub const LOW: i32 = 1;
+     pub const HIGH: i32 = 3;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = (Priority::High as i32) - 1;
−     assert_eq!(result, 2);
−     let result2 = 10 - (Priority::High as i32);
−     assert_eq!(result2, 7);
+ }
────────────────────────────────────────────────────────────