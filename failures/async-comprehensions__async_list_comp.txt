  async-comprehensions.toml :: async_list_comp
    Output mismatch

────────────────────────────────────────────────────────────
− // Async comprehensions map to stream collectors
− use futures::stream::{self, StreamExt};
− 
− #[tokio::main]
− async let result: Vec<i32> = stream::iter(0..5).collect().await;
− log::info!("{:?}", result);
+ use serde_json;
+ #[doc = " Generator state struct"] #[derive(Debug)] struct AsyncRangeState {
+     state: usize, n: serde_json::Value
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn async_range(n: & serde_json::Value) -> impl Iterator<Item = i32>{
+     AsyncRangeState {
+     state: 0, n: n
+ }
+ } impl Iterator for AsyncRangeState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _i in 0..self.n {
+     return Some(i);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test() -> Vec<serde_json::Value>{
+     let result = async_range(5).iter().cloned().collect::<Vec<_>>();
+     return result;
+     }
────────────────────────────────────────────────────────────