  control-flow.toml :: nested_index_assignment
    Output mismatch

────────────────────────────────────────────────────────────
+ use serde_json;
  use std::collections::HashMap;
− pub fn nested_dict() -> HashMap<String, HashMap<String, i32>> {
−     let mut d = {
+ pub fn nested_dict() -> HashMap<String, serde_json::Value> {
+     let d = {
          let mut map = HashMap::new();
          map.insert("a".to_string(), {
              let mut map = HashMap::new();
              map.insert("b".to_string(), 1);
              map
          });
          map
      };
      d.get_mut(&"a").unwrap().insert("b".to_string(), 2);
      return d;
  }
────────────────────────────────────────────────────────────