  dataclasses-frozen.toml :: frozen_original_unchanged
    Output mismatch

────────────────────────────────────────────────────────────
− pub const p: Point = Point::new(1, 2);
− pub const p2: Point = replace(p, 10);
− pub const result: (i32, i32) = (p.x, p2.x);
− #[derive(Debug, Copy, Clone]
+ #[derive(Debug, Copy, Clone)]
  pub struct Point {
      pub x: i32,
      pub y: i32,
  }
  impl Point {
      pub fn new(x: i32, y: i32) -> Self {
          Self { x, y }
      }
− }
+ }
────────────────────────────────────────────────────────────