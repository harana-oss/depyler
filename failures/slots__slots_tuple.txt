  slots.toml :: slots_tuple
    Output mismatch

────────────────────────────────────────────────────────────
− struct Point {
−     x: i32,
−     y: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Point {
+     pub x: serde_json::Value,
+     pub y: serde_json::Value,
  }
− 
  impl Point {
−     fn new(x: i32, y: i32) -> Self {
−         Point { x, y }
+     pub const __slots__: serde_json::Value = ("x".to_string(), "y".to_string());
+     pub fn new(x: serde_json::Value, y: serde_json::Value) -> Self {
+         Self { x, y }
      }
  }
− 
− let p = Point::new(3, 4);
− log::info!("{} {}", p.x, p.y);
────────────────────────────────────────────────────────────