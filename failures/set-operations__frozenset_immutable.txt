  set-operations.toml :: frozenset_immutable
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashSet;
  
−     let fs: HashSet<i32> = vec![1, 2, 3].into_iter().collect();
−     // Rust HashSet with immutable binding is also immutable
−     let result = fs.contains(&1) && fs.len() == 3;
────────────────────────────────────────────────────────────