  dataclasses-field.toml :: field_metadata_immutable
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust's metadata would be immutable by design at compile time
−     let result = true;
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Item {
+     pub value: i32,
+ }
+ impl Item {
+     pub fn new() -> Self {
+         Self {
+             value: Default::default(),
+         }
+     }
+ }
────────────────────────────────────────────────────────────