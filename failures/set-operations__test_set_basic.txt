  set-operations.toml :: test_set_basic
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashSet;
  pub fn test_set_creation() -> HashSet<i32> {
      let s1 = HashSet::<i32>::new();
      let s2 = {
          let mut set = HashSet::new();
          set.insert(1);
          set.insert(2);
          set.insert(3);
          set.insert(4);
          set.insert(5);
          set
      };
      let s3 = {
          let mut set = HashSet::new();
−         set.insert("apple".to_string());
−         set.insert("banana".to_string());
−         set.insert("cherry".to_string());
+         set.insert("apple");
+         set.insert("banana");
+         set.insert("cherry");
          set
      };
      let s4 = vec![1, 2, 3, 3, 4, 4, 5]
          .into_iter()
          .collect::<HashSet<_>>();
−     s2
+     return s2;
  }
  pub fn test_set_with_duplicates() -> HashSet<i32> {
      let s = {
          let mut set = HashSet::new();
          set.insert(1);
          set.insert(2);
          set.insert(2);
          set.insert(3);
          set.insert(3);
          set.insert(3);
          set.insert(4);
          set
      };
−     s
+     return s;
  }
────────────────────────────────────────────────────────────