  slots.toml :: slots_attribute_access
    Output mismatch

────────────────────────────────────────────────────────────
− struct Point {
−     x: i32,
−     y: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Point {
+     pub x: serde_json::Value,
+     pub y: serde_json::Value,
+ }
+ impl Point {
+     pub const __slots__: Vec<String> = vec!["x".to_string(), "y".to_string()];
+     pub fn new(x: serde_json::Value, y: serde_json::Value) -> Self {
+         Self { x, y }
+     }
  }
− 
− let mut p = Point { x: 1, y: 2 };
− log::info!("{} {}", p.x, p.y);
− p.x = 10;
− p.y = 20;
− log::info!("{} {}", p.x, p.y);
────────────────────────────────────────────────────────────