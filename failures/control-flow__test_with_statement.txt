  control-flow.toml :: test_with_statement
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  #[derive(Debug, Clone)]
  pub struct FileManager {
      pub filename: String,
      pub file: (),
  }
  impl FileManager {
      pub fn new(filename: String) -> Self {
−         Self {
−             filename,
−             file: Default::default(),
−         }
+         Self { filename, file: () }
      }
      pub fn __enter__(&mut self) -> &Self {
          self.file = self.filename;
          return self;
      }
      pub fn __exit__(
          &mut self,
          exc_type: serde_json::Value,
          exc_val: serde_json::Value,
          exc_tb: serde_json::Value,
      ) -> bool {
          self.file = ();
          return false;
      }
      pub fn write(&self, data: String) {
          return data.len();
      }
  }
  #[doc = "Test basic with statement"]
− pub fn test_simple_with() -> i32 {
−     let _context = FileManager::new("test.txt".to_string().to_string());
−     let fm = _context.__enter__();
−     let result = fm.write("Hello, World!".to_string());
−     result
+ pub fn test_simple_with() {
+     let _context = FileManager::new("test.txt".to_string());
+     let mut fm = _context.__enter__();
+     let result = fm.write("Hello, World!");
+     return result;
  }
  #[doc = "Test with built-in open"]
− pub fn test_with_builtin() -> i32 {
−     let f = std::fs::File::create("test.txt".to_string())?;
−     f.write("Hello, World!".to_string());
−     1
+ pub fn test_with_builtin() -> Result<i32, std::io::Error> {
+     let mut f = std::fs::File::create("test.txt")?;
+     f.write("Hello, World!");
+     return Ok(1);
  }
────────────────────────────────────────────────────────────