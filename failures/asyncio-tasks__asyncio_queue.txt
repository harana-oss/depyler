  asyncio-tasks.toml :: asyncio_queue
    Output mismatch

────────────────────────────────────────────────────────────
− #[tokio::main]
− async fn main() {
−     // Producer-consumer pattern with Vec
−     let mut items: Vec<i32> = Vec::new();
+ pub async fn producer_consumer() {
+     let mut items = vec! [];
      items.push(42);
−     let value = items.remove(0);
−     log::info!("{}", value);
− }
+     let value = items.remove(0 as usize);
+     return value;
+     }
────────────────────────────────────────────────────────────