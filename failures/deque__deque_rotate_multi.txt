  deque.toml :: deque_rotate_multi
    Output mismatch

────────────────────────────────────────────────────────────
− // d.rotate(3)
−     use std::collections::VecDeque;
− 
−     let mut d = VecDeque::from(vec![1, 2, 3, 4, 5]);
−     d.rotate_right(3);
−     let result = d.iter().cloned().collect::<Vec<_>>() == vec![3, 4, 5, 1, 2];
+ use std::collections::VecDeque;
────────────────────────────────────────────────────────────