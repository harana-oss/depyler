  reference-return-types.toml :: struct_literal_return_direct_value
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, Clone, PartialEq, Default)]
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
  pub struct Config {
      pub enabled: bool,
      pub count: i32,
  }
  impl Config {
      pub fn new(enabled: bool, count: i32) -> Self {
          Self { enabled, count }
      }
  }
  pub fn default_config() -> Config {
      return Config::new(true, 10);
  }
────────────────────────────────────────────────────────────