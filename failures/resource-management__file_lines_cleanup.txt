  resource-management.toml :: file_lines_cleanup
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn count_lines(path: String) -> i32 {
−     match std::fs::read_to_string(path) {
−         Ok(content) => return content.lines().count() as i32,
−         Err(_) => return 0,
+ pub fn count_lines(path: String) -> Result<i32, std::io::Error> {
+     let count = 0;
+     let mut f = std::fs::File::open(path)?;
+     for __ in std::io::BufReader::new(f)
+         .lines()
+         .map(|l| l.unwrap_or_default())
+     {
+         count += 1;
      }
+     return Ok(count);
  }
────────────────────────────────────────────────────────────