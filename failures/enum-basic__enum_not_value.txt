  enum-basic.toml :: enum_not_value
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust enums are distinct types from their discriminants
−     #[derive(Debug, PartialEq)]
−     enum Color {
−         RED = 1,
−         GREEN = 2,
+ pub const result: serde_json::Value = Color::RED != 1;
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const GREEN: i32 = 2;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     // Cannot directly compare enum to integer in Rust (type safety)
−     let result = true;
+ }
────────────────────────────────────────────────────────────