  functools-wraps.toml :: wraps_signature
    Output mismatch

────────────────────────────────────────────────────────────
− // inspect.signature works correctly
−     // Rust type information is compile-time
−     fn func(x: i32, y: &str) -> String {
−         format!("{}: {}", x, y)
+ use serde_json;
+ use std;
+ pub fn my_decorator(f: &serde_json::Value) {
+     fn wrapper() -> () {
+         return f(args);
      }
− 
−     let result = true;
+     return wrapper;
+ }
+ pub fn func(x: i32, y: String) -> String {
+     return format!("{}: {}", x, y);
+ }
────────────────────────────────────────────────────────────