  regressions.toml :: type_inference_ownership
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
− pub fn process_data(data: &Vec<i32>) -> Vec<i32> {
+ pub fn process_data(data: &Vec<serde_json::Value>) -> Vec<serde_json::Value> {
      let mut result = vec![];
      for item in data.iter().cloned() {
          result.push(item * 2);
      }
      return result;
  }
────────────────────────────────────────────────────────────