  enum-methods.toml :: enum_custom_format
    Output mismatch

────────────────────────────────────────────────────────────
− enum Size {
−     Small = 1,
−     Medium = 2,
−     Large = 3,
− }
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct Size {}
  impl Size {
−     fn format_spec(&self, spec: &str) -> String {
−         let name = match self {
−             Size::Small => "SMALL",
−             Size::Medium => "MEDIUM",
−             Size::Large => "LARGE",
−         };
−         if spec == "short" {
−             name.chars().next().unwrap().to_string()
−         } else {
−             name.to_string()
−         }
+     pub const SMALL: i32 = 1;
+     pub const MEDIUM: i32 = 2;
+     pub const LARGE: i32 = 3;
+     pub fn new() -> Self {
+         Self {}
      }
  }
− 
− let s = Size::Medium;
− log::info!("{}", s.format_spec(""));
− log::info!("{}", s.format_spec("short"));
────────────────────────────────────────────────────────────