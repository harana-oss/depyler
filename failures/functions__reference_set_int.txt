  functions.toml :: reference_set_int
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashSet;
  pub fn f(x: &HashSet<i32>) -> i32 {
−     return x.len() as i32;
+     return x.len() as i32 as i32;
  }
  pub fn main() {
      let v = {
          let mut set = HashSet::new();
          set.insert(1);
          set.insert(2);
          set.insert(3);
          set
      };
      f(&v);
  }
────────────────────────────────────────────────────────────