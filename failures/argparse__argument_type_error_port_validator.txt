  argparse.toml :: argument_type_error_port_validator
    Output mismatch

────────────────────────────────────────────────────────────
  use clap::Parser;
  #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
+         }
+     }
+ }
+ #[derive(Debug, Clone)]
  pub struct ArgumentTypeError {
      message: String,
  }
  impl std::fmt::Display for ArgumentTypeError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "argument type error: {}", self.message)
      }
  }
  impl std::error::Error for ArgumentTypeError {}
  impl ArgumentTypeError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn port_number(value: String) -> i32 {
+ pub fn port_number(value: String) -> Result<i32, Box<dyn std::error::Error>> {
      {
          let port = value.parse::<i32>().unwrap();
          if (port < 1) || (port > 65535) {
−             panic!(
−                 "{}",
−                 format!("Port must be between 1 and 65535, got {}", port)
−             );
+             return Err(Box::new(ArgumentTypeError::new(format!(
+                 "Port must be between 1 and 65535, got {}",
+                 port
+             ))));
          }
−         return port;
−         panic!("{}", format!("Port must be an integer, got '{}'", value));
+         return Ok(port);
+         return Err(Box::new(ArgumentTypeError::new(format!(
+             "Port must be an integer, got '{}'",
+             value
+         ))));
      }
  }
────────────────────────────────────────────────────────────