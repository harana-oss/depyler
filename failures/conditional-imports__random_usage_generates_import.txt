  conditional-imports.toml :: random_usage_generates_import
    Output mismatch

────────────────────────────────────────────────────────────
− use rand::prelude::*;
− use rand::Rng;
+ use rand as random;
  pub fn get_random() -> i32 {
−     return rand::rng().random_range(1..=100);
+     return DEPYLER_RNG.with(|rng| rng.borrow_mut().gen_range(1..=100));
  }
────────────────────────────────────────────────────────────