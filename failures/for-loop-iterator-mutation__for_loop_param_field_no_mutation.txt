  for-loop-iterator-mutation.toml :: for_loop_param_field_no_mutation
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Player {
      pub name: String,
  }
  impl Player {
      pub fn new(name: String) -> Self {
          Self { name }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct State {
      pub players: Vec<Player>,
  }
  impl State {
      pub fn new(players: Vec<Player>) -> Self {
          Self { players }
      }
  }
  pub fn print_names(state: &State) -> i32 {
      let mut count = 0;
−     for player in state.players.iter() {
+     for _player in &state.players {
          count += 1;
      }
      return count;
  }
────────────────────────────────────────────────────────────