  argparse.toml :: validator_identity_returns_string
    Output mismatch

────────────────────────────────────────────────────────────
  use clap::Parser;
  use regex as re;
  #[derive(Debug, Clone)]
  pub struct ArgumentTypeError {
      message: String,
  }
  impl std::fmt::Display for ArgumentTypeError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "argument type error: {}", self.message)
      }
  }
  impl std::error::Error for ArgumentTypeError {}
  impl ArgumentTypeError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
  #[derive(Debug, clap::Parser)]
  struct Args {
      #[arg(long)]
      email: Option<String>,
  }
  #[doc = "Validator that returns the original string parameter."]
− pub fn email_address(value: String) {
+ pub fn email_address(value: &str) -> Result<(), Box<dyn std::error::Error>> {
      if regex::Regex::new("^[a-z]+@[a-z]+\\.com$")
          .unwrap()
−         .find(value.clone())
+         .find(value)
          .is_none()
      {
−         panic!("{}", "Invalid email");
+         return Err(Box::new(ArgumentTypeError::new("Invalid email")));
      }
−     return value;
+     return Ok(value);
  }
  pub fn main() {
      let args = Args::parse();
  }
────────────────────────────────────────────────────────────