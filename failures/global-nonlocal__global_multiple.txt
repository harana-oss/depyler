  global-nonlocal.toml :: global_multiple
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::Cell;
− 
− thread_local! {
−     static A: Cell<i32> = Cell::new(1);
−     static B: Cell<i32> = Cell::new(2);
−     static C: Cell<i32> = Cell::new(3);
− }
− 
− fn modify_all() {
−     A.with(|a| a.set(10));
−     B.with(|b| b.set(20));
−     C.with(|c| c.set(30));
+ pub fn modify_all() {
+     let a = 10;
+     let b = 20;
+     let c = 30;
  }
− 
− log::info!(
−     "Before: {}, {}, {}",
−     A.with(|a| a.get()),
−     B.with(|b| b.get()),
−     C.with(|c| c.get())
− );
− modify_all();
− log::info!(
−     "After: {}, {}, {}",
−     A.with(|a| a.get()),
−     B.with(|b| b.get()),
−     C.with(|c| c.get())
− );
────────────────────────────────────────────────────────────