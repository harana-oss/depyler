  concurrency.toml :: threading_locks
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Copy, Clone)]
  pub struct Counter {
      pub count: i32,
  }
− 
  impl Counter {
      pub fn new() -> Self {
          Self { count: 0 }
      }
− 
      pub fn increment(&mut self) {
          self.count += 1;
      }
− 
      pub fn get(&self) -> i32 {
          return self.count;
      }
  }
────────────────────────────────────────────────────────────