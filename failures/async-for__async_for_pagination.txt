  async-for.toml :: async_for_pagination
    Output mismatch

────────────────────────────────────────────────────────────
− use futures::stream::{self, StreamExt};
− 
− #[tokio::main]
− async let pages = vec![vec![1, 2, 3], vec![4, 5, 6], vec![7, 8, 9]];
− let result: Vec<i32> = stream::iter(pages)
−     .flat_map(|page| stream::iter(page))
−     .collect()
−     .await;
− log::info!("{:?}", result);
+ use serde_json;
+ #[doc = " Generator state struct"] #[derive(Debug)] struct PaginatedResultsState {
+     state: usize, pages: Vec<Vec<i32>>
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn paginated_results() -> impl Iterator<Item = i32>{
+     PaginatedResultsState {
+     state: 0, pages: Default::default()
+ }
+ } impl Iterator for PaginatedResultsState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     self.pages = vec! [vec! [1, 2, 3], vec! [4, 5, 6], vec! [7, 8, 9]];
+     for page in self.pages.iter().cloned() {
+     for _item in page.iter().cloned() {
+     return Some(item);
+    
+ }
+ } None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test() -> Vec<serde_json::Value>{
+     let result = vec! [];
+     while let Some(item) = paginated_results().next().await {
+     result.push(item);
+    
+ }
+ return result;
+     }
────────────────────────────────────────────────────────────