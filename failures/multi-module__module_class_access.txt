  multi-module.toml :: module_class_access
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Clone)]
  pub struct Config {
      pub name: String,
  }
− 
  impl Config {
      pub fn new(name: String) -> Self {
          Self { name }
      }
− 
      pub fn get_name(&self) -> String {
          return self.name.clone();
      }
  }
− 
  pub fn create_config(name: String) -> Config {
      return Config::new(name);
  }
────────────────────────────────────────────────────────────