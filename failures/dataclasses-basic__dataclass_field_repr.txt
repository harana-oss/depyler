  dataclasses-basic.toml :: dataclass_field_repr
    Output mismatch

────────────────────────────────────────────────────────────
− pub const p: Person = Person::new("Alice".to_string(), "secret".to_string());
− pub const result: bool = !format!("{:?}", p).contains(&"secret");
− use serde_json;
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Person {
      pub name: String,
+     pub password: String,
  }
  impl Person {
−     pub const password: String = field();
      pub fn new(name: String) -> Self {
−         Self { name }
+         Self {
+             name,
+             password: Default::default(),
+         }
      }
  }
────────────────────────────────────────────────────────────