  async-generators.toml :: async_gen_break_cleanup
    Output mismatch

────────────────────────────────────────────────────────────
− use async_stream::stream;
− use tokio_stream::StreamExt;
− 
− async fn test_async_gen_break_cleanup() -> usize {
−     let mut g = stream! {
−         yield 1;
−         yield 2;
−         yield 3;
−     };
− 
−     while let Some(val) = g.next().await {
−         if val == 2 {
−             break;
−         }
−     }
−     1
− }
+ pub const cleanup: serde_json::Value = vec! [];
+     use serde_json;
+     #[doc = " Generator state struct"] #[derive(Debug)] struct GenState {
+     state: usize ,
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn gen() -> impl Iterator<Item = i32>{
+     GenState {
+     state: 0 ,
+ }
+ } impl Iterator for GenState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1usize;
+     return Some(1);
+    
+ }
+ 1usize =>{
+     self.state = 2usize;
+     return Some(2);
+    
+ }
+ 2usize =>{
+     self.state = 3usize;
+     return Some(3);
+    
+ }
+ 3usize =>{
+     self.state = 3usize + 1;
+     None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test_async_gen_break_cleanup() -> i32 {
+     while let Some(val) = gen().next().await {
+     if val == 2 {
+     break;
+    
+ }
+ } return cleanup.len() as i32 as i32;
+     }
────────────────────────────────────────────────────────────