  decorators-basic.toml :: decorator_validation
    Output mismatch

────────────────────────────────────────────────────────────
− fn validate_positive<F>(f: F) -> impl Fn(i32) -> Result<i32, String>
−     where
−         F: Fn(i32) -> i32,
−     {
−         move |x| {
−             if x < 0 {
−                 Err("Must be positive".to_string())
−             } else {
−                 Ok(f(x))
−             }
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct ValueError {
+     message: String,
+ }
+ impl std::fmt::Display for ValueError {
+     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
+         write!(f, "value error: {}", self.message)
+     }
+ }
+ impl std::error::Error for ValueError {}
+ impl ValueError {
+     pub fn new(message: impl Into<String>) -> Self {
+         Self {
+             message: message.into(),
          }
      }
− 
−     fn square_impl(x: i32) -> i32 {
−         x * x
+ }
+ pub fn validate_positive(f: &serde_json::Value) {
+     fn wrapper(x: ()) -> () {
+         if x < 0 {
+             panic!("{}", "Must be positive");
+         }
+         return f(x);
      }
− 
−     let square = validate_positive(square_impl);
−     let result = square(5).unwrap();
+     return wrapper;
+ }
+ pub fn square(x: i32) {
+     return x * x;
+ }
────────────────────────────────────────────────────────────