  async-generators.toml :: async_gen_expression
    Output mismatch

────────────────────────────────────────────────────────────
− use async_stream::stream;
− use tokio_stream::StreamExt;
− 
− async fn test_async_gen_expression() -> i32 {
−     let g = stream! {
−         for i in 0..3 {
−             yield i;
−         }
−     };
− 
−     let doubled: Vec<i32> = g.map(|x| x * 2).collect().await;
−     doubled.iter().sum()
− }
+ #[doc = " Generator state struct"] #[derive(Debug)] struct GenState {
+     state: usize ,
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn gen() -> impl Iterator<Item = i32>{
+     GenState {
+     state: 0 ,
+ }
+ } impl Iterator for GenState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     for _i in 0..3 {
+     return Some(i);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test_async_gen_expression() -> i32 {
+     let doubled = gen().iter().cloned().map(| x | x * 2).collect::<Vec<_>>();
+     return doubled.iter().sum::<i32>();
+     }
────────────────────────────────────────────────────────────