  enum-basic.toml :: enum_str
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug, PartialEq)]
−     enum Color {
−         RED = 1,
−         GREEN = 2,
+ pub const result: serde_json::Value = (Color::RED).to_string() == "Color.RED";
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const GREEN: i32 = 2;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let s = format!("{:?}", Color::RED);
−     let result = s == "RED";
+ }
────────────────────────────────────────────────────────────