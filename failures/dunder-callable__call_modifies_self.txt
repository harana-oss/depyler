  dunder-callable.toml :: call_modifies_self
    Output mismatch

────────────────────────────────────────────────────────────
− struct Accumulator {
−     total: i32,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Accumulator {
+     pub total: i32,
  }
− 
  impl Accumulator {
−     fn new() -> Self {
−         Accumulator { total: 0 }
+     pub fn new() -> Self {
+         Self { total: 0 }
      }
− 
−     fn call(&mut self, x: i32) -> i32 {
−         self.total += x;
−         self.total
−     }
  }
− 
− let mut acc = Accumulator::new();
− log::info!("{}", acc.call(10));
− log::info!("{}", acc.call(5));
− log::info!("{}", acc.call(3));
────────────────────────────────────────────────────────────