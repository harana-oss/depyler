  context-managers.toml :: cm_enter_exception
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust: Drop not called if constructor fails
−     struct Resource {
−         dropped: bool,
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct FailingEnter {
+     pub exit_called: bool,
+ }
+ impl FailingEnter {
+     pub fn new() -> Self {
+         Self { exit_called: false }
      }
− 
−     impl Resource {
−         fn new() -> Result<Self, String> {
−             Err("Constructor failed".to_string())
−         }
+     pub fn __enter__(&self) {
+         panic!("Exception: {}", ValueError::new("Enter failed".to_string()));
      }
− 
−     impl Drop for Resource {
−         fn drop(&mut self) {
−             self.dropped = true;
−         }
+     pub fn __exit__(
+         &mut self,
+         exc_type: serde_json::Value,
+         exc_val: serde_json::Value,
+         exc_tb: serde_json::Value,
+     ) -> bool {
+         self.exit_called = true;
+         return false;
      }
− 
−     let result = Resource::new();
−     assert!(result.is_err());
−     // Drop not called because resource was never created
+ }
────────────────────────────────────────────────────────────