  descriptors.toml :: lazy_descriptor
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::OnceCell;
− 
− struct LazyDescriptor {
−     value: OnceCell<i32>,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct LazyDescriptor {
+     pub func: serde_json::Value,
+     pub name: (),
  }
− 
  impl LazyDescriptor {
−     fn new() -> Self {
−         LazyDescriptor {
−             value: OnceCell::new(),
−         }
+     pub fn new(func: serde_json::Value) -> Self {
+         Self { func, name: () }
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct MyClass {}
+ impl MyClass {
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     fn get(&self) -> i32 {
−         *self.value.get_or_init(|| {
−             log::info!("Computing expensive value");
−             42
−         })
+     pub fn expensive(&self) -> i32 {
+         log::info!("{}", "Computing expensive value".to_string());
+         return 42;
      }
− }
− 
− struct MyClass {
−     expensive: LazyDescriptor,
  }
− 
− let instance = MyClass {
−     expensive: LazyDescriptor::new(),
− };
− log::info!("First access:");
− log::info!("{}", instance.expensive.get());
− log::info!("Second access:");
− log::info!("{}", instance.expensive.get());
────────────────────────────────────────────────────────────