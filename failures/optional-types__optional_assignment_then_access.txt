  optional-types.toml :: optional_assignment_then_access
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct IndexError {
      message: String,
  }
  impl std::fmt::Display for IndexError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "index out of range: {}", self.message)
      }
  }
  impl std::error::Error for IndexError {}
  impl IndexError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− #[derive(Debug, Copy, Clone]
+ #[derive(Debug, Copy, Clone)]
  pub struct Scores {
      pub total: i32,
      pub tries: i32,
  }
  impl Scores {
      pub fn new(total: i32, tries: i32) -> Self {
          Self { total, tries }
      }
  }
  #[derive(Debug, Clone)]
  pub struct PeriodStatistic {
      pub scores: Scores,
  }
− impl PeriodStatistic {
− }
  #[derive(Debug, Clone)]
  pub struct State {
      pub period_statistics: Vec<PeriodStatistic>,
  }
− impl State {
− }
− pub fn test_function(state: &State) {
+ pub fn test_function(state: &State) -> Result<(), IndexError> {
      let mut extra_time_stats: Option<PeriodStatistic> = None;
      if state.is_some() {
−         extra_time_stats = Some(
−             state
−                 .period_statistics
−                 .clone()
−                 .get(2usize)
−                 .cloned()
−                 .unwrap(),
−         );
+         extra_time_stats = Some(state.period_statistics.get(2usize).cloned().unwrap());
          log::info!("{}", extra_time_stats.as_ref().unwrap().scores.tries);
      }
− }
+     Ok(())
+ }
────────────────────────────────────────────────────────────