  error-handling.toml :: complex_exception_patterns
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn complex_process(items: &Vec<i32>, threshold: i32) -> i32 {
−     let _cse_temp_0 = items.len() as i32;
−     if _cse_temp_0 == 0 {
−         panic!("{}", "Empty list");
+ pub fn complex_process(
+     items: &Vec<i32>,
+     threshold: i32,
+ ) -> Result<i32, Box<dyn std::error::Error>> {
+     if items.len() as i32 == 0 {
+         return Err(Box::new(ValueError::new("Empty list")));
      }
      let mut total = 0;
      for item in items.iter().cloned() {
          if item < 0 {
−             panic!("{}", "Negative value");
+             return Err(Box::new(ValueError::new("Negative value")));
          }
          if item > threshold {
−             panic!("{}", "Value exceeds threshold");
+             return Err(Box::new(ValueError::new("Value exceeds threshold")));
          }
          total += item;
      }
      if total == 0 {
−         panic!("{}", RuntimeError::new("Sum is zero"));
+         return Err(Box::new(RuntimeError::new("Sum is zero")));
      }
−     return total;
+     return Ok(total);
  }
────────────────────────────────────────────────────────────