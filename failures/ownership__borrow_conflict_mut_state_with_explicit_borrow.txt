  ownership.toml :: borrow_conflict_mut_state_with_explicit_borrow
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct Config {
      pub setting: String,
  }
  impl Config {
      pub fn new(setting: String) -> Self {
          Self { setting }
      }
  }
  #[derive(Debug, Clone, PartialEq, Default)]
  pub struct App {
      pub config: Config,
  }
  impl App {
      pub fn new(config: Config) -> Self {
          Self { config }
      }
  }
− pub fn apply_config(app: &mut App, cfg: Config) {
+ pub fn apply_config(app: &mut App, cfg: &Config) {
      app.config = cfg;
  }
  pub fn update_app(app: &mut App) {
−     let cfg = app.config.clone();
−     apply_config(app, cfg);
+     apply_config(&app, &app.config);
  }
────────────────────────────────────────────────────────────