  enum-basic.toml :: enum_immutable
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust enums are immutable by definition at compile time
−     #[derive(Debug, PartialEq)]
−     enum Color {
−         RED = 1,
−         GREEN = 2,
+ pub const result: bool = true;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = 1;
+     pub const GREEN: i32 = 2;
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = true;
+ }
────────────────────────────────────────────────────────────