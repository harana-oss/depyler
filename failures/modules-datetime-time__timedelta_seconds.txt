  modules-datetime-time.toml :: timedelta_seconds
    Output mismatch

────────────────────────────────────────────────────────────
  use chrono::Duration;
− pub fn ninety_seconds() -> chrono::Duration {
−     return chrono::Duration::seconds(90);
+ pub fn ninety_seconds() -> timedelta {
+     return chrono::Duration::zero();
  }
────────────────────────────────────────────────────────────