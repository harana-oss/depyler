  match-class-patterns.toml :: builtin_str_pattern
    Output mismatch

────────────────────────────────────────────────────────────
− use std::any::Any;
  
−     let value: Box<dyn Any> = Box::new("hello".to_string());
−     let result = if value.is::<String>() {
−         "string"
−     } else if value.is::<i32>() {
−         "integer"
−     } else {
−         "other"
−     };
────────────────────────────────────────────────────────────