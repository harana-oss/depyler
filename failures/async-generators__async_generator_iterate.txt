  async-generators.toml :: async_generator_iterate
    Output mismatch

────────────────────────────────────────────────────────────
− use async_stream::stream;
− use tokio_stream::StreamExt;
− 
+ #[doc = " Generator state struct"] #[derive(Debug)] struct GenState {
+     state: usize ,
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn gen() -> impl Iterator<Item = i32>{
+     GenState {
+     state: 0 ,
+ }
+ } impl Iterator for GenState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1usize;
+     return Some(1);
+    
+ }
+ 1usize =>{
+     self.state = 2usize;
+     return Some(2);
+    
+ }
+ 2usize =>{
+     self.state = 3usize;
+     return Some(3);
+    
+ }
+ 3usize =>{
+     self.state = 3usize + 1;
+     None
+ }
+ _ =>None
+ }
+ }
+ }
  pub async fn test_async_generator_iterate() -> i32 {
−     let s = stream! {
−         yield 1;
−         yield 2;
−         yield 3;
−     };
− 
−     let mut total = 0;
−     tokio::pin!(s);
−     while let Some(x) = s.next().await {
−         total += x;
−     }
−     total
− }
+     let total = 0;
+     while let Some(x) = gen().next().await {
+     total += x;
+    
+ }
+ return total;
+     }
────────────────────────────────────────────────────────────