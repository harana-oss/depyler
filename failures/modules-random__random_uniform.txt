  modules-random.toml :: random_uniform
    Output mismatch

────────────────────────────────────────────────────────────
  use rand as random;
− use rand::prelude::*;
− use rand::Rng;
  pub fn random_in_range(low: f64, high: f64) -> f64 {
−     return rand::thread_rng().gen_range((low as f64)..=(high as f64));
+     return DEPYLER_RNG.with(|rng| rng.borrow_mut().gen_range((low as f64)..=(high as f64)));
  }
────────────────────────────────────────────────────────────