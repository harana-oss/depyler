  boundary-values.toml :: empty_lists_and_dicts
    Output mismatch

────────────────────────────────────────────────────────────
− pub fn single_chars(a: i32, b: i32, c: i32) -> i32 {
−     let x = a;
−     let y = b;
−     let z = c;
−     return x + y + z;
+ use std::collections::HashMap;
+ pub fn empty_collections() -> i32 {
+     let empty_list = vec![];
+     let empty_dict = {
+         let map = HashMap::new();
+         map
+     };
+     return empty_list.len() as i32 + empty_dict.len() as i32 as i32;
  }
────────────────────────────────────────────────────────────