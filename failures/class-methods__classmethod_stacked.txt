  class-methods.toml :: classmethod_stacked
    Output mismatch

────────────────────────────────────────────────────────────
  use serde_json;
  #[derive(Debug, Copy, Clone)]
  pub struct MyClass {}
  impl MyClass {
      pub fn new() -> Self {
          Self {}
      }
      pub fn greet() -> String {
          return "hello".to_string();
      }
  }
  pub fn uppercase_decorator(func: &serde_json::Value) {
      fn wrapper() -> () {
          let result = func(args);
          return result.to_uppercase();
      }
      return wrapper;
  }
− log::info!("{}", MyClass::greet());
────────────────────────────────────────────────────────────