  set-operations.toml :: set_proper_subset
    Output mismatch

────────────────────────────────────────────────────────────
− pub const result: bool = a.clone() < b.clone();
− lazy_static! {
−     pub static ref a: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(1);
−         set.insert(2);
−         set
−     };
−     pub static ref b: HashSet<i32> = {
−         let mut set = HashSet::new();
−         set.insert(1);
−         set.insert(2);
−         set.insert(3);
−         set
−     };
− }
− use lazy_static::lazy_static;
+ pub const a: serde_json::Value = {
+     let mut set = HashSet::new();
+     set.insert(1);
+     set.insert(2);
+     set
+ };
+ pub const b: serde_json::Value = {
+     let mut set = HashSet::new();
+     set.insert(1);
+     set.insert(2);
+     set.insert(3);
+     set
+ };
+ pub const result: serde_json::Value = a < b;
  use serde_json;
  use std::collections::HashSet;
────────────────────────────────────────────────────────────