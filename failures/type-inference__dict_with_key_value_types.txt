  type-inference.toml :: dict_with_key_value_types
    Output mismatch

────────────────────────────────────────────────────────────
  use std::collections::HashMap;
  pub fn lookup(mapping: &HashMap<String, i32>, key: String) -> i32 {
−     return mapping.get(&key).cloned().unwrap_or(0).unwrap();
+     return *mapping.get(&key).unwrap_or(&0);
  }
────────────────────────────────────────────────────────────