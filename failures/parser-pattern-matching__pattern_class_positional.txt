  parser-pattern-matching.toml :: pattern_class_positional
    Output mismatch

────────────────────────────────────────────────────────────
− struct Point {
−     x: i64,
−     y: i64,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Point {
+     pub x: i32,
+     pub y: i32,
  }
− 
− fn describe_point(p: &Point) -> String {
−     match p {
−         Point { x, y } => format!("Point({}, {})", x, y),
+ impl Point {
+     pub fn new(x: i32, y: i32) -> Self {
+         Self { x, y }
      }
  }
− 
− fn main() {
−     let p = Point { x: 3, y: 4 };
−     let result = describe_point(&p);
−     println!("{}", result);
+ pub fn describe_point(p: &Point) -> String {
+     return format!("Point({}, {})", p.x, p.y);
  }
────────────────────────────────────────────────────────────