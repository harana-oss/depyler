  dataclasses-field.toml :: field_kw_only_reorder
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust builder pattern for flexible initialization
−     #[derive(Debug)]
−     struct Config {
−         name: String,
−         port: i32,
−         host: String,
−     }
− 
−     impl Config {
−         fn new(name: String, host: String) -> Self {
−             Config {
−                 name,
−                 port: 8080,
−                 host,
−             }
+ #[derive(Debug, Clone, PartialEq, Default)]
+ pub struct Config {
+     pub name: String,
+     pub port: i32,
+     pub host: String,
+ }
+ impl Config {
+     pub fn new(name: String) -> Self {
+         Self {
+             name,
+             port: 8080,
+             host: Default::default(),
          }
      }
− 
−     let c = Config::new("app".to_string(), "localhost".to_string());
−     let result = c.name == "app" && c.port == 8080 && c.host == "localhost";
+ }
────────────────────────────────────────────────────────────