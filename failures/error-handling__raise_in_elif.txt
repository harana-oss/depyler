  error-handling.toml :: raise_in_elif
    Output mismatch

────────────────────────────────────────────────────────────
  #[derive(Debug, Clone)]
  pub struct ValueError {
      message: String,
  }
  impl std::fmt::Display for ValueError {
      fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
          write!(f, "value error: {}", self.message)
      }
  }
  impl std::error::Error for ValueError {}
  impl ValueError {
      pub fn new(message: impl Into<String>) -> Self {
          Self {
              message: message.into(),
          }
      }
  }
− pub fn categorize_or_fail(x: i32) -> String {
+ pub fn categorize_or_fail(x: i32) -> Result<String, ValueError> {
      if x < 0 {
−         return "negative".to_string();
+         return Ok("negative".to_string());
      } else {
          if x == 0 {
−             panic!("{}", "Zero not allowed");
+             return Err(ValueError::new("Zero not allowed"));
          } else {
−             return "positive".to_string();
+             return Ok("positive".to_string());
          }
      }
  }
────────────────────────────────────────────────────────────