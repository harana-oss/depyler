  match-mapping-patterns.toml :: mapping_rest
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
  
−     let mut data = HashMap::new();
−     data.insert("key", 1);
−     data.insert("a", 2);
−     data.insert("b", 3);
− 
−     let result = match data.get("key") {
−         Some(&v) => {
−             let rest_count = data.len() - 1;
−             (v, rest_count)
−         }
−         None => (0, 0),
−     };
────────────────────────────────────────────────────────────