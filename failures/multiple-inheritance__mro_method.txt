  multiple-inheritance.toml :: mro_method
    Output mismatch

────────────────────────────────────────────────────────────
− // Class.mro() returns list
−     // Rust doesn't have runtime MRO, but we can verify trait bounds
−     trait A {}
−     trait B: A {}
−     trait C: A {}
− 
−     struct D;
−     impl A for D {}
−     impl B for D {}
−     impl C for D {}
− 
−     pub fn test_mro() -> bool {
−         // Verify D implements all traits
−         let _d: &dyn A = &D;
−         true
+ #[derive(Debug, Copy, Clone)]
+ pub struct A {}
+ impl A {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct B {}
+ impl B {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct C {}
+ impl C {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct D {}
+ impl D {
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = test_mro();
+ }
────────────────────────────────────────────────────────────