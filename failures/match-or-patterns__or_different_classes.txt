  match-or-patterns.toml :: or_different_classes
    Output mismatch

────────────────────────────────────────────────────────────
− enum Result {
−         Success,
−         Failure,
+ #[derive(Debug, Copy, Clone)]
+ pub struct Success {}
+ impl Success {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
+ pub struct Failure {}
+ impl Failure {
+     pub fn new() -> Self {
+         Self {}
      }
−     let x = Result::Success;
−     let result = match x {
−         Result::Success | Result::Failure => true,
−     };
+ }
────────────────────────────────────────────────────────────