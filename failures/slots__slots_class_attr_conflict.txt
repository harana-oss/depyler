  slots.toml :: slots_class_attr_conflict
    Output mismatch

────────────────────────────────────────────────────────────
− struct Conflict {
−     x: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Conflict {
+     pub x: serde_json::Value,
  }
− 
  impl Conflict {
−     const CLASS_X: i32 = 10;
− 
−     fn new(val: i32) -> Self {
−         Conflict { x: val }
+     pub const __slots__: Vec<String> = vec!["x".to_string()];
+     pub const x: i32 = 10;
+     pub fn new(val: serde_json::Value) -> Self {
+         Self { x: val }
      }
  }
− 
− let c = Conflict::new(20);
− log::info!("{}", c.x);
− log::info!("{}", Conflict::CLASS_X);
────────────────────────────────────────────────────────────