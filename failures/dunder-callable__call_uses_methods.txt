  dunder-callable.toml :: call_uses_methods
    Output mismatch

────────────────────────────────────────────────────────────
− struct Processor;
− 
+ #[derive(Debug, Copy, Clone)]
+ pub struct Processor {}
  impl Processor {
−     fn process(&self, x: i32) -> i32 {
−         x * 2
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     fn call(&self, x: i32) -> i32 {
−         self.process(x) + 1
+     pub fn process(&self, x: i32) -> i32 {
+         return x * 2;
      }
  }
− 
− let proc = Processor;
− log::info!("{}", proc.call(5));
────────────────────────────────────────────────────────────