  format-spec.toml :: format_nested
    Output mismatch

────────────────────────────────────────────────────────────
  pub fn format_nested_spec() -> String {
      let x = 3.14159;
−     let width = 10;
−     let prec = 2;
−     return format!("{:width$.prec$}", x, width = width, prec = prec);
+     return format!("{:{width}.{prec}}", x);
  }
────────────────────────────────────────────────────────────