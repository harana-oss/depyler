  contextlib-decorator.toml :: contextmanager_try_finally
    Output mismatch

────────────────────────────────────────────────────────────
− use std::cell::RefCell;
−     use std::rc::Rc;
− 
−     let log: Rc<RefCell<Vec<&str>>> = Rc::new(RefCell::new(Vec::new()));
−     let log_clone = log.clone();
− 
−     fn with_finally<F, R>(log: Rc<RefCell<Vec<&'static str>>>, f: F) -> R
−     where
−         F: FnOnce() -> R,
−     {
−         log.borrow_mut().push("enter");
−         let result = f();
−         log.borrow_mut().push("exit");
−         result
+ use serde_json;
+ #[doc = " Generator state struct"]
+ #[derive(Debug)]
+ struct WithFinallyState {
+     state: usize,
+ }
+ #[doc = " Generator function - returns Iterator"]
+ pub fn with_finally() -> impl Iterator<Item = serde_json::Value> {
+     WithFinallyState { state: 0 }
+ }
+ impl Iterator for WithFinallyState {
+     type Item = serde_json::Value;
+     fn next(&mut self) -> Option<Self::Item> {
+         match self.state {
+             0 => {
+                 self.state = 1;
+                 log.push("enter");
+                 return None;
+                 log.push("exit");
+                 None
+             }
+             _ => None,
+         }
      }
− 
−     with_finally(log_clone, || {
−         log.borrow_mut().push("body");
−     });
− 
−     let result = log.borrow().clone();
+ }
────────────────────────────────────────────────────────────