  dataclasses-advanced.toml :: dataclass_make_bases
    Output mismatch

────────────────────────────────────────────────────────────
− #[derive(Debug)]
− struct Base {
−     x: i32,
+ #[derive(Debug, Copy, Clone, PartialEq, Default)]
+ pub struct Base {
+     pub x: i32,
  }
− 
− #[derive(Debug)]
− struct Child {
−     x: i32,
−     y: i32,
+ impl Base {
+     pub fn new(x: i32) -> Self {
+         Self { x }
+     }
  }
− 
  pub fn test_dataclass_make_bases() -> i32 {
−     let c = Child { x: 1, y: 2 };
−     c.x + c.y
+     let Child = make_dataclass("Child".to_string(), vec![("y", int)], (Base));
+     let c = Child::new(1, 2);
+     return c.x + c.y;
  }
────────────────────────────────────────────────────────────