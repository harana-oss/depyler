  async-generators.toml :: async_gen_streaming
    Output mismatch

────────────────────────────────────────────────────────────
− use async_stream::stream;
− use tokio_stream::StreamExt;
− 
− async fn test_async_gen_streaming() -> usize {
−     let g = stream! {
−         yield b"Hello".to_vec();
−         yield b" ".to_vec();
−         yield b"World".to_vec();
−     };
− 
−     let mut result = Vec::new();
−     tokio::pin!(g);
−     while let Some(chunk) = g.next().await {
−         result.extend_from_slice(&chunk);
−     }
−     result.len()
− }
+ #[doc = " Generator state struct"] #[derive(Debug)] struct StreamResponseState {
+     state: usize, chunks: Vec<bytes>
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn stream_response() -> impl Iterator<Item = i32>{
+     StreamResponseState {
+     state: 0, chunks: Default::default()
+ }
+ } impl Iterator for StreamResponseState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1;
+     self.chunks = vec! [b"Hello", b" ", b"World"];
+     for _chunk in self.chunks.iter().cloned() {
+     return Some(chunk);
+    
+ }
+ None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test_async_gen_streaming() -> i32 {
+     let result = b"";
+     while let Some(chunk) = stream_response().next().await {
+     result += chunk;
+    
+ }
+ return result.len() as i32 as i32;
+     }
────────────────────────────────────────────────────────────