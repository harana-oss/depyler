  parser-pattern-matching.toml :: pattern_literal
    Output mismatch

────────────────────────────────────────────────────────────
− fn check(x: i64) -> String {
−     match x {
−         1 => "one".to_string(),
−         2 => "two".to_string(),
−         _ => "other".to_string(),
+ pub fn check(x: i32) -> String {
+     if x == 1 {
+         return "one".to_string();
+     } else {
+         if x == 2 {
+             return "two".to_string();
+         } else {
+             return "other".to_string();
+         }
      }
− }
− 
− fn main() {
−     let result = check(1);
−     println!("{}", result);
  }
────────────────────────────────────────────────────────────