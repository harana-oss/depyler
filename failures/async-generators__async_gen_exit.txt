  async-generators.toml :: async_gen_exit
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't have direct GeneratorExit equivalent
− // Dropping the stream handles cleanup
− async fn test_async_gen_exit() -> usize {
−     1
− }
+ pub const exit_caught: serde_json::Value = vec! [];
+     use serde_json;
+     #[doc = " Generator state struct"] #[derive(Debug)] struct GenState {
+     state: usize ,
+ }
+ #[doc = " Generator function - returns Iterator"] pub fn gen() -> impl Iterator<Item = i32>{
+     GenState {
+     state: 0 ,
+ }
+ } impl Iterator for GenState {
+     type Item = i32;
+     fn next(&mut self) -> Option<Self::Item>{
+     match self.state {
+     0 =>{
+     self.state = 1usize;
+     return Some(1);
+    
+ }
+ 1usize =>{
+     self.state = 2usize;
+     return Some(2);
+    
+ }
+ 2usize =>{
+     self.state = 2usize + 1;
+     None
+ }
+ _ =>None
+ }
+ }
+ }
+ pub async fn test_async_gen_exit() -> Result<i32, Box<dyn std::error::Error>>{
+     let g = gen() ?;
+     anext(g).await;
+     g.aclose().await;
+     return Ok(exit_caught.len() as i32 as i32);
+     }
────────────────────────────────────────────────────────────