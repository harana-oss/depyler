  match-class-patterns.toml :: class_pattern_guard
    Output mismatch

────────────────────────────────────────────────────────────
− struct Point {
−         x: i32,
−         y: i32,
+ use serde_json;
+ #[derive(Debug, Clone)]
+ pub struct Point {
+     pub x: serde_json::Value,
+     pub y: serde_json::Value,
+ }
+ impl Point {
+     pub const __match_args__: serde_json::Value = ("x".to_string(), "y".to_string());
+     pub fn new(x: serde_json::Value, y: serde_json::Value) -> Self {
+         Self { x, y }
      }
− 
−     let value = Point { x: 3, y: 4 };
−     let result = match value {
−         Point { x, y } if x > 0 && y > 0 => "quadrant I",
−         Point { x, y } if x > 0 => "quadrant IV",
−         Point { x, y } if y > 0 => "quadrant II",
−         _ => "quadrant III",
−     };
+ }
────────────────────────────────────────────────────────────