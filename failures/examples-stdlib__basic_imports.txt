  examples-stdlib.toml :: basic_imports
    Output mismatch

────────────────────────────────────────────────────────────
+ use serde_json;
  use std as os;
  use std as sys;
  #[doc = "Get current working directory"]
  pub fn get_current_directory() -> String {
−     std::env::current_dir()
+     return std::env::current_dir()
          .unwrap()
          .to_string_lossy()
−         .to_string()
+         .to_string();
  }
  #[doc = "Get command line arguments"]
− pub fn get_args() -> i32 {
−     std::env::args().collect::<Vec<String>>()
+ pub fn get_args() -> Vec<serde_json::Value> {
+     return std::env::args().collect::<Vec<String>>();
  }
  #[doc = "Exit program with code"]
  pub fn exit_program(code: i32) {
      std::process::exit(code);
  }
────────────────────────────────────────────────────────────