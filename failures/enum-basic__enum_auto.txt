  enum-basic.toml :: enum_auto
    Output mismatch

────────────────────────────────────────────────────────────
− // Rust doesn't have auto(), but discriminants auto-increment
−     #[derive(Debug, PartialEq)]
−     enum Color {
−         RED,   // 0
−         GREEN, // 1
−         BLUE,  // 2
+ pub const result: serde_json::Value = (Color.RED.value == 1) && (Color.GREEN.value == 2);
+ use serde_json;
+ #[derive(Debug, Copy, Clone)]
+ pub struct Color {}
+ impl Color {
+     pub const RED: i32 = auto();
+     pub const GREEN: i32 = auto();
+     pub const BLUE: i32 = auto();
+     pub fn new() -> Self {
+         Self {}
      }
− 
−     let result = Color::RED as i32 == 0 && Color::GREEN as i32 == 1;
+ }
────────────────────────────────────────────────────────────