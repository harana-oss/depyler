  abc.toml :: abc_register
    Output mismatch

────────────────────────────────────────────────────────────
− trait MyABC {}
  #[derive(Debug, Copy, Clone)]
+ pub struct MyABC {}
+ impl MyABC {
+     pub fn new() -> Self {
+         Self {}
+     }
+ }
+ #[derive(Debug, Copy, Clone)]
  pub struct SomeClass {}
  impl SomeClass {
      pub fn new() -> Self {
          Self {}
      }
  }
  pub fn test_abc_register() -> bool {
      return issubclass(SomeClass, MyABC);
− }
− fn main() {
−     MyABC::register(SomeClass);
  }
────────────────────────────────────────────────────────────