  concurrency.toml :: shared_state
    Output mismatch

────────────────────────────────────────────────────────────
+ #[derive(Debug, Copy, Clone)]
  pub struct SharedCounter {
      pub value: i32,
  }
− 
  impl SharedCounter {
      pub fn new() -> Self {
          Self { value: 0 }
      }
− 
      pub fn increment(&mut self) -> i32 {
          self.value += 1;
−         return self.value;
+         return self.value.clone();
      }
− 
      pub fn decrement(&mut self) -> i32 {
          self.value -= 1;
−         return self.value;
+         return self.value.clone();
      }
  }
────────────────────────────────────────────────────────────