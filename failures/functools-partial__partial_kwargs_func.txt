  functools-partial.toml :: partial_kwargs_func
    Output mismatch

────────────────────────────────────────────────────────────
− use std::collections::HashMap;
− 
− fn process(kwargs: HashMap<&str, i32>) -> i32 {
−     kwargs.values().sum()
+ use std;
+ pub fn process() -> i32 {
+     return kwargs.values().cloned().sum::<i32>();
  }
− 
− // Rust doesn't have **kwargs, but we can simulate with HashMap
− let with_a = |mut extra: HashMap<&str, i32>| {
−     extra.insert("a", 10);
−     process(extra)
− };
− let mut args = HashMap::new();
− args.insert("b", 20);
− args.insert("c", 30);
− log::info!("{}", with_a(args));
────────────────────────────────────────────────────────────