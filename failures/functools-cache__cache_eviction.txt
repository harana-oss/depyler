  functools-cache.toml :: cache_eviction
    Output mismatch

────────────────────────────────────────────────────────────
− // LRU eviction when maxsize reached
−     use lru::LruCache;
− 
−     let mut cache: LruCache<i32, i32> = LruCache::new(2.try_into().unwrap());
−     cache.put(1, 2);
−     cache.put(2, 4);
−     cache.put(3, 6); // evicts 1
−     let result = cache.len() == 2;
+ use std;
+ pub fn func(x: i32) -> i32 {
+     return x * 2;
+ }
────────────────────────────────────────────────────────────