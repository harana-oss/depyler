  stdlib-misc.toml :: uuid_in_data_structure
    Output mismatch

────────────────────────────────────────────────────────────
+ use serde_json;
  use std::collections::HashMap;
− pub fn create_item_with_id() -> HashMap<String, String> {
−     return HashMap::from([
−         (
+ pub fn create_item_with_id() -> HashMap<serde_json::Value, serde_json::Value> {
+     return {
+         let mut map = HashMap::new();
+         map.insert(
              "id".to_string(),
−             {
+             ({
                  use uuid::Uuid;
                  Uuid::new_v4().to_string()
−             }
+             })
              .to_string(),
−         ),
−         ("name".to_string(), "item".to_string()),
−     ]);
+         );
+         map.insert("name".to_string(), "item".to_string());
+         map
+     };
  }
────────────────────────────────────────────────────────────